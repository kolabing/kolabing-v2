# BACKLOG (kolabing-v2 / backend)

> **Single source of truth for outstanding BACKEND work.** `CLAUDE.md` requires this
> file to be read at the start of every session and kept in sync (see "Maintenance
> rules" at the bottom).
>
> **Scope:** this file tracks **backend (Laravel / API / DB / ops)** work only. App
> (Flutter) work is tracked in the **kolabing-app** repo's `BACKLOG.md`, which stays
> the authoritative app backlog. The live cross-repo board is **Kolabing Engineering**
> (GitHub Project 4, owner `kolabing`).
>
> Last updated: 2026-08-25 (**BE-FX-22 — işletmeler artık ödeme yapmadan bir topluluğun bilgilerini göremiyor, ve bu ilk kez *gerçekten* bir kapı**: Volkan bunu panelde bildirdi ama mesele panel değildi. Blur'u **istemciler çiziyordu**: API bir topluluğun gerçek adını, logosunu, about'unu, iletişim linklerini, yorumlarını ve portföyünü kimliği doğrulanmış herkese gönderiyordu; mobil üstüne bulanıklık boyuyordu, web paneli hiçbir şey boyamıyordu. İstemci tarafı bir blur asla kapı olamaz — ad, `GET /profiles/{id}` ile bir istek uzaklıktaydı (BE-FX-22, BE-NF-39'un güvenlik incelemesinde tespit edilmiş, o zamandan beri açık). Artık `App\Support\CommunityIdentityMask` **tek yerde** karar veriyor ve **iki** profil resource'u da kimliği sunucudan çıkmadan önce null'lıyor. Gizlenen: ad, logo, handle, about, Instagram/TikTok/website, `public_url` (handle'dan üretiliyor, yani maskeledi sanılan alanı ele veriyor), yorum listesi, galeri, geçmiş etkinlikler ve iş birlikleri. **Kalan** (çünkü bu bir blur, blok değil): tür, şehir, tamamlanan Kolab sayısı, toplu itibar. **Hiçbir topluluk asla maskelenmiyor** — guard `isBusiness()` ile kısa devre yapıyor, çünkü `hasActiveSubscription()` her işletme-olmayan için false döner; bu dosyanın yarısı o tek satır için var. Panel sunucunun `identity_masked` bayrağını okuyup /suggestions'daki aynı muameleyi veriyor (bulanık yer tutucu + abone ol kartı), kuralı asla yeniden türetmiyor. 10 yeni test; 3 mevcut test **abonelikli** izleyiciye çevrildi (niyetleri "payload şunu taşıyor"du, "ücretsiz işletme bunu görebilir" değil). Docs: ROLES §2.5, map §26. **Hâlâ açık: BE-FX-21** — Explore'un kendi payload'ı sunucu tarafında maskeli değil. Önceki: 2026-08-25 (**BE-FX-31 — panelin "linki kopyala" butonu ölü bir URL veriyordu**: Volkan production'dan aldığı `kolabing.com/kolabs/{id}` linkini açtı, 404. Sebep o Kolab değil — kontrol ettim, production'da `publishable()` kapılarının **hepsini** geçiyor (published, seçilebilir tarih, 67 karakter açıklama). Sebep panelin `copyLink()`'i: **yayınlanmış her Kolab için** marketing host'lu public URL'i kopyalıyordu, ama açık web'deki Kolab yüzeyi `public_kolabs.enabled` ile **kapalı** (BE-FX-24) ve kapalı olmak *yok* demek — route'lar 404 veriyor. Yani paylaşan kişi ölü bir link dağıtıyor ve bunu bilmesinin bir yolu yok. Marketing sitesindeki her link (nav, footer, anasayfa şeridi, sitemap) bu bayrağı zaten soruyordu; **tek kontrolsüz yayıcı bu butondu**. Artık `KB_CONFIG.publicKolabs` panele taşınıyor ve public URL üç koşulun hepsini istiyor: yayınlanmış + marketing host biliniyor + **marketplace açık**. Kapalıyken taslakta olduğu gibi in-app URL kopyalanıyor. Bu arada: switch'i açmak bugün vitrine 13 Kolab çıkarıyor (102'den) ve dördü açıkça test (`Volkan`, `Clark Biz Test`, `WebBusiness`, `[TEST] Finish-flow collaboration`), üçünün başlığı da işletme adının kendisi — BE-FX-24'ün beklediği insan kararı tam olarak bu. Önceki: 2026-08-25 (**BE-NF-47 — dört event kitlesi, XP tavanı kalktı, peer challenge kütüphanesi doldu**: `integration/all-open-prs` dalında oturan ve **PR'ı hiç açılmamış** iki commit master'a alındı. (1) `events.max_challenges_per_attendee` **10 ile default'luydu** — yani hiç kimsenin seçmediği, ürünün hiçbir yerinde yazmayan bir tavan her event'te vardı; artık **null = sınırsız** ve default bu. Kritik olan kısım: bu migration production'a **kodu merge edilmeden** uygulanmış (`migrations` batch 74), dolayısıyla prod null satırları eski `?? 10` yedeğinden geçirip tam da migration'ın kaldırmak istediği sayıda kapalı kalmıştı — bu merge o drift'i kapatıyor. Bir şey kırılmadı çünkü yedek vardı; sebep duruyor: repodaki yerel `.env` `DB_*` production'a bakıyor. (2) `events.visibility` artık **dört** cevap taşıyor — `public` / `followers` / `members` / `active_members` (+ `tier_gate`) — ve kural **tek bir yerde**: `EventSignupService::audienceRefusal()`. Önce üç ayrı kopyası vardı (`assertEligible` / `canAccess` / `canAccessFromLoadedState`), ki liste görünümü ile detay görünümünün "kim ne görür" konusunda ayrışmasının yolu tam olarak budur. Şema değişikliği gerekmedi: prod'da `events.visibility` check constraint'i olmayan düz `varchar`. (3) kolabing-v2#150 `community_challenges` tablosunu açıp boş bırakmıştı — `PeerChallengeLibrarySeeder` dolduruyor. Hiçbir kapı kımıldamadı, hiçbir şey ücretli olmadı (ROLES §8.4). Docs: ROLES §8.8, map §25. Önceki: 2026-08-22 (**BE-NF-43 tamamlandı — attendee paneli**: `app.kolabing.com`'da kayıt artık sadece hesap açıyor ve `/onboarding/attendee`'ye devrediyor; bu akış **mobildeki dört adımın aynısı** (Sen → Şehir → İlgi alanları → Katıl; sadece 1. adım zorunlu, canlı handle kontrolü + öneriler, boş alanlar gönderilmiyor, foto data-URI). Ayrıca attendee dashboard'u (önce bir sonraki bilet, sonra şehrinde ne var), `/tickets` cüzdanı ve host tarafındaki `/admit/{code}` kapısı. Menü role göre şekilleniyor: attendee'de Explore/Kolabs/Suggestions/plan/Kolab oluştur yok, Chats var (topluluk kanalları üyelik bazlı). Önceki: 2026-08-25 (**BE-NF-46 / BE-FX-29 — topluluk dashboard'u artık bir şey söylüyor**: `GET /me/dashboard` topluluk için **3**, işletme için **7** anahtar dönüyordu ve bu bir karar değil, eksiklikti. En keskin sonucu kozmetik değil: topluluklar da Kolab yayınlıyor, dolayısıyla **başvuru alıyor** — ama `applications_received` yalnızca işletmeye hesaplanıyordu; üstelik Flutter istemcisi bu alanı topluluk dashboard'unda yazıldığı günden beri **okuyor** ve gelmediği için sessizce sıfıra düşürüyordu, yani cevap bekleyen işletmeleri olan bir topluluğa "0" gösteriliyordu (**BE-FX-29**). Artık topluluk da `opportunities`, `applications_received` ve **topluluğa göre şekillenmiş** bir `next_action` alıyor — ilk adım *yayınla* değil **başvur**, çünkü gezinmek ve başvurmak ücretsiz (ROLES §3.5) ve yayınlamak bu rol için daha nadir yol. Panelde: Next-up kartı (profil-gücü ölçeri açıkken kendini gizliyor, ikisi aynı şeyi farklı ölçtüğü için), yalnızca doluyken görünen "received" karosu, kabuğun zaten çektiği istatistiklerden topluluk özeti kartı ve `/kolabs?tab=requests&sub=received` desteği. Bonus: `next_action` metinleri artık panelde `key` üzerinden çevriliyor (sunucu metni yedek) — işletme kartı da ES/CA'da İngilizce kalmıyor. Hiçbir kapı kımıldamadı, hiçbir şey ücretli olmadı; şema değişikliği yok. Docs: ROLES §3.7, map §24. Önceki: 2026-08-25 (**BE-NF-45 — bir Kolab artık web panelinden yürütülebiliyor**: panelde `/collaborations` listesi vardı ve arkasında hiçbir şey yoktu — yalnızca web kullanan biri bir başvuruyu kabul ettikten sonra o işi **başlatamıyor, olduğunu onaylayamıyor, bitiremiyor, iptal edemiyor ve değerlendirme bırakamıyordu**; üstelik dashboard zaten "Leave your review" diyordu ve bırakacak ekran yoktu. Yeni sayfa: `app.kolabing.com/collaborations/{id}` — başlat/iptal, `/complete`'i kapatan zorunlu **evet / henüz değil / hayır** onayı (iki tarafın cevabı yan yana), 5 yıldızlı herkese açık değerlendirme ve role göre şekillenen **özel** etki formu (işletme: story + ciro, topluluk: üye faydası). Hiçbir yeni endpoint yok, hiçbir izin değişmedi: her buton sunucunun kendi `actions.can_*` / `viewer_must_*` bayraklarından geliyor. ROLES §2.8 sayfada aynen geçerli — planı biten bir işletme **devam eden** bir Kolab'da yeniden abonelik istemine düşüyor, topluluk tarafı asla etkilenmiyor, biten Kolab "devam eden" sayılmadığı için değerlendirme erişilebilir kalıyor. Yolda iki sessiz tuzak yakalandı: `ProfileSummaryResource` alanı `display_name` (ilk sürüm `name` okuyordu → her ortak "Partner" görünürdü) ve lifecycle hataları bağlamı `errors` içinde döndürdüğü için `errorText()` kullanıcıya düz "no" yazardı. Docs: ROLES §4.4, map §23. Önceki: 2026-08-24 (**BE-FX-28 düzeltildi — panelin telefon önizlemesi Flutter ile hizalandı**: önizleme `GET /profiles/{id}/public-profile` okuyordu, ama o resource'ta `reputation` / `recent_reviews` / `completed_kolabs_count` / `type_label` yok — yani itibar kartı her profilde kalıcı olarak "— · 0 reviews · 0 completed" gösteriyordu ve "Recent Reviews" bölümü hiç yoktu. Artık uygulamanın okuduğu **üç** uç noktayı okuyor (`/profiles/{id}` + `/profiles/{id}/collaborations` + `/events?profile_id=`). Görsel taraf da Dart ölçüleriyle yeniden yazıldı: eksik yorumlar kartı eklendi, etkinlik kartları 180×220 tam kapak + gradient oldu, geçmiş Kolab'lar 110'luk rayda 240'lık kartlar + boş durum, itibar kartının iki durumu (yorum yoksa EmptyStateCard), bölüm başlıkları 20/700, sayaç pill'leri ve app paletinin gerçek hex'leri (#FAF5EA / #EDE5D5 / #3F3A32 / #8C8A82). Attendee önizlemesi, yazarken canlı önizleme ve 1280px altı görünürlük **kapsam dışı**. 15 yeni koruma testi; spec: `docs/superpowers/specs/2026-08-24-phone-preview-flutter-parity-design.md`. Numara notu: BE-FX-26/27 uçmakta olan attendee dalında ayrılmış, o yüzden bu 28.) Önceki: 2026-08-22 (**BE-NF-42: Explore yeniden tasarlandı** — Luma referansıyla: Kolab'lar *en yakın rezerve edilebilir güne* göre gruplanıyor (sabit tarihi olmayanlar en üstte "Open now"), kart artık teklifle başlıyor, ilan sahibi altta; bir Kolab'a tıklayınca sağdan çekmece açılıyor ve iki okla liste yerinde geziliyor. Hiçbir kapı/sorgu/endpoint değişmedi. **BE-NF-40 başladı**: panelin *Events* menü girişi kaldırıldı — `events` satırları kapının mekanizması olarak duruyor, rotalar da duruyor. Ayrıca ana sayfa Kolab şeridi artık veritabanı erişilemezse sayfayı düşürmüyor. Önceki: 2026-08-22 (**BE-NF-41: Kolab pazar yeri herkese açık web'e çıktı + BE-NF-40 açıldı**: `kolabing.com/kolabs` + `/kolabs/{slug}` ve ana sayfada canlı Kolab kartları; ana menüye "what's on" ve "kolabs" girişleri. Kolab'ın *özü* herkese açık (ROLES §2.5 zaten ücretsiz işletmenin "her Kolab'ın detaylarını gördüğünü" söylüyor — paywall'lı olan şey terimler değil), ama **ilanı veren topluluğun adı yazılmıyor**: §2.5'teki bulanıklık, çıkış yapıp açık webden okunabiliyorsa hiçbir şey ifade etmez. İşletme adı yazılıyor (§3.3: asla bulanık değil). Kapı üç yerde tek servisten geçiyor (liste, detay, sitemap) — sitemap'in reklamını yaptığı ama sayfanın 404 verdiği URL olamaz. Yol boyunca bulunan gerçek hata: `requireAuth()` girişte hedefi unutuyordu, yani kolabing.com'dan gelen `?rsvp=1` / `?apply=1` niyeti login'de kayboluyordu — artık `?next=` ile taşınıyor. Yeni: BE-FX-21 (production'daki test Kolab'ları, kod filtresiyle ayrılamaz — sayfalar `noindex`), BE-FX-22 (`KolabFactory` API'nin reddedeceği şekli yazıyor), BE-NF-40 (attendee hunisi ayrı events sistemi değil, Kolab üstünden yürüyecek). Prior: 2026-08-22 (**BE-NF-37 — attendee'nin web yüzeyi**: attendee rolünün webde hiç girişi yoktu (kayıt ekranında rol olarak bile yok, tüm yüzey yayınlanmamış mobilde). `kolabing.com/events` + `/events/{slug}` herkese açık ve indekslenebilir oldu; yalnızca `EventVisibility::Public` — hem listelemede hem çözümlemede zorlanıyor, çünkü listelenmemek okunamamak değil. Detay açık, duvar eylemde: RSVP panele `?rsvp=1` ile devrediliyor (herkese açık origin token'ı okuyamaz). Kayıtta üçüncü rol kartı, panelde ev sahibi olmayan için RSVP/bekleme listesi. Yeni endpoint ve şema değişikliği yok — discovery ve RSVP uygunluk kuralları zaten doğruydu, yeniden yazılmadı. Prior: 2026-08-22 (**Check-in artık iki istemci için sözleşmeli** (BE-NF-35 güncellemesi): mobil de aynı yüzeyi açacağı için senkron kuralları yazıldı ve zorlandı. Bir gün canlı kalan tuzak düzeltildi — `generate-qr` her çağrıda yeni token üretiyordu, yani ev sahibi ikinci cihazda kapıyı açınca ilkindeki QR sessizce ölüyordu; açmak idempotent, retire etmek açık bir eylem oldu. Gelenler artık yayınlanıyor (`AttendeeCheckedIn` → `private-event.{id}.door`, varış + yürüyen toplam), polling yedek; kanal yetkisi üç yerde tekrarlanan sahiplik kontrolü yerine tek `Event::isHostedBy()` ile. Tek QR'ın hem uygulamayı hem tarayıcıyı açması için app host'una Universal Links / App Links dosyaları eklendi (mobil kimlikler gelene kadar 404). Yeni karar kalemi: BE-NF-36 çevrimdışı check-in ve `checked_in_at`'in kime ait olduğu. Prior: 2026-08-21 (**BE-NF-35 — etkinlik + check-in kapısı panele geldi, BE-FX-20 kırık QR URL'i düzeltildi**: check-in API'si 13 uç noktayla hazırdı ama hiçbir istemcisi yoktu (yalnızca mobil, o da yayında değil), ve QR URL'i kuran tek yer var olmayan bir route'a işaret ediyordu — okutan telefon 404 alıyordu. Panelde `/events`, `/events/{id}` (kapı: QR + yazılabilir kod + canlı sayaç + gelenler) ve `/checkin/{token}` (QR'ın açtığı katılımcı ekranı, `?next=` ile giriş sonrası geri dönüş). QR bağımlılık eklenmeden sunucuda üretiliyor ve bağımsız bir çözücüyle doğrulanmış dondurulmuş matrislerle test ediliyor. Token artık süreli ve yalnızca ev sahibine dönüyor. Beyan/gerçek katılım oranı hem listede hem kapı ekranında. Prior: 2026-08-20 (**BE-NF-38 — the invite page never worked, now fixed and given a join form**: its CTA sat in `<template x-if>` blocks on a layout with no Alpine, and the CSP grants 'unsafe-eval' only to the app host, so nothing was ever painted. Moved the page to the app host rather than weakening a security header; added Google sign-up plus a name/phone/photo form so an invitee joins in place. The regression test now asserts the page can RUN its Alpine, not just that the markup is there. Prior: 2026-08-20 (**BE-NF-37 phone preview built**: a read-only replica of the mobile app's profile screen in a device frame beside every Profile-section tab, refreshed after each edit — so reordering a gallery shows immediately which photo leads on a phone. Mirrors three named Dart files in kolabing-app; the pairing is recorded in backend map §17.5. No API, schema or role change. Prior: 2026-08-20 (**BE-NF-36 public portfolio panel built**: business and community accounts can finally manage the portfolio their public profile renders — gallery, past events, per-event photos — from a tabbed Profile section, with a Preview of their own public page. The find that shaped it: 'past events' lives in two unrelated stores and the profile read only the smaller one, so **60 past events and 173 photographs were invisible**; both are merged now. Also fixed: the gallery had `caption` and `sort_order` columns no endpoint ever wrote. No migration. Prior: 2026-08-19 (**SEO denetimi + düzeltmeleri**: kolabing.com canlı olarak denetlendi (16 sayfa) ve bulunan sekiz kod hatası aynı gün düzeltildi — BE-FX-19. En büyüğü ana sayfanın **24,58 MB görsel** yükü (→ 860 KB WebP, %96,5); ayrıca `www` ikinci bir kanonik site olarak yayındaydı, ana sayfa hiçbir ticari sayfaya link vermiyordu, pazarlama HTML'i hiç önbelleklenmiyordu, boş `/blog` ve `/communities` indekslenebilir ince sayfalardı ve sitemap eşiği bir seed test hesabını indekse sokuyordu. Denetimin kod dışı kalan üç kalemi ayrı iş olarak açıldı: BE-NF-31 (Tailwind CDN → build, görsel doğrulama gerektiriyor), BE-NF-32 (blog içeriği), BE-NF-33 (İspanyolca sayfalar). Prior: 2026-08-19 (**BE-FX-17 — the marketing site's structured data was invalid everywhere**: Laravel 12's `@context` Blade directive silently replaced the `@context` key in every inline `{!! json_encode([...]) !!}` JSON-LD block, so Organization / WebSite / Product / FAQPage markup emitted compiled PHP instead and Google could not interpret any of it. Found while inspecting the new public profile page on production, which was unaffected because it encodes from a `@php` block. Fixed in four templates the same way, with a guard test that parses every ld+json block on ten marketing URLs. Prior same day: 2026-08-19 (**BE-NF-29 — public profile pages, in the panel and on the open web** (branch `feat/public-profiles`): businesses and communities had a rich public profile in the API that **no web surface ever rendered** — the panel had no profile route at all, and a shared Kolabing link led nowhere. Two pages now: `app.kolabing.com/profiles/{id}` shows the full detail (reviews with their authors + the five-category rating breakdown, past events with photos, past collaborations, gallery with a lightbox, contact links), reachable from Explore cards, the Kolab overlay, application rows, collaboration partners, notification actors, the chat header, the sidebar card and the account page. And `kolabing.com/p/{slug}` is a **public, indexable teaser** — identity, rating, three photos and one anonymised quote, with everything else (all reviews + who wrote them, contact links, past-event detail, partners) behind a sign-up wall that is enforced by *absence from the HTML*, not CSS. Slug is `name-<uuid tail>` because `profiles.handle` is only required for attendees (5 of 94 production profiles have one), so keying on it would 404 almost everywhere; old links survive a rename and full UUIDs still resolve. Backend: one additive endpoint `GET /profiles/{id}/public-profile` serving the rich shape for **both** roles — `kolabs.past_events` was always role-agnostic, only the community-scoped route made past events look community-only — plus additive `public_url` on that resource, `actor_profile_id` on notifications and `id` on chat participants so every client can link onward. Profiles with a completed collaboration are listed in `sitemap.xml`; `Organization`/`LocalBusiness` + `AggregateRating` JSON-LD, and the rating block is omitted rather than faked when there are no reviews. 29 new tests, most of them asserting what the public page must NOT contain. **Open decision:** the public page is opt-out-less — see BE-NF-30.) — Prior 2026-08-18 (**Reverb is already deployed — BE-IF-18's "MISSING" list was stale**: `.env` carries live credentials for Laravel Cloud's **managed** Reverb, so the self-hosted-daemon + nginx/TLS steps in that ticket do not apply. Verified by handshaking the real endpoint (accepts `Origin: https://app.kolabing.com`, `activity_timeout: 30`; a no-Origin connection is correctly rejected `4009`) and by running the whole loop — handshake → `/broadcasting/auth` → `pusher:subscribe` → `message.sent` — against a local `reverb:start` + `queue:work` on an isolated SQLite DB. The web panel therefore needs **no code change** for real-time; it lights up on deploy. One thing left to confirm: a **queue worker in production**, since `NewChatMessage` is `ShouldBroadcast` on a `database` queue and without a worker every broadcast is enqueued and never delivered. Docs corrected accordingly (they claimed the daemon was missing).) — Prior same day (**BE-NF-28 — chat integrated into the web panel** (branch `feat/webapp-chat`): app.kolabing.com had no chat at all — no route, no nav entry, not one call to the chat API — while the backend carried the whole feature (thread types, inbox, unread counts, bans, tier-gated channels) and only mobile used it. Added `/chats` (+ `/es`, `/ca`): a two-pane inbox (thread list + conversation, one route, no page load between threads), a `Messages` nav row with its own unread badge on every page, day-separated bubbles, community **channel management** (create ≤5 / rename / delete + per-member block list, owned communities only), and deep links `?thread=` / `?application=` / `?collaboration=` so My Kolabs rows and new-message notifications open the right conversation. Real-time is a **hand-written 250-line Pusher-protocol client** (`public/webapp-assets/kb-realtime.js`) instead of laravel-echo + pusher-js: the panel has no bundler and the CSP forbids CDNs. It stays dormant until BE-IF-18 hands over `REVERB_APP_KEY` and a 4s ticker polls in the meantime, so chat works either way. CSP gained `wss:` on the web-app host only (`connect-src https:` does NOT cover WebSockets). Three real backend gaps were found and filed rather than papered over: BE-FX-13 (silent Kolab messages), BE-FX-14 (§2.8 lapse re-gate not enforced for chat), BE-FX-15 (`/me/communities` hides delegated communities). Verified by replaying every call the page makes against a running server on an isolated SQLite copy (the local `.env` points at the production DB, so nothing was written there) — which caught a real bug in the block list: the member roster pages on `limit`, not `per_page`, and nests rows under `data.members`, where `kb.rows()` cannot find them, so the list rendered empty. Fixed and pinned. `POST /broadcasting/auth` was confirmed to return a valid channel signature for a bearer token, so the auth half of real-time is proven; only the daemon is missing. 1506 tests pass (7088 assertions), pint clean. ROLES §4.1 + backend map §15.) — Prior 2026-08-18 (**BE-FX-12 — `GET /chats` 500 fixed**: the mobile chat inbox was dead in production (`ChatException(null: Server error)`); root cause was `latestOfMany()` aggregating `MAX(uuid)` on the `chat_messages` primary key, which Postgres has no function for, introduced with BE-FX-8's preview. Replaced with a plain `latest()` relation + `ChatService::attachLatestMessages()` (two grouped queries, no N+1). Reproduced AND verified read-only against the production DB; guard test added because the SQLite test suite structurally cannot catch Postgres-only SQL. Needs deploy — see Fixes BE-FX-12.) — Prior 2026-08-17 (**Web payment flow completed** (BE-NF-26, branch feat/kolabing-web-app): the web checkout existed but was not sellable — activation depended entirely on the Stripe webhook, so a business could pay, be shown "you're subscribed", and still be paywalled (and on prod, where the webhook is not yet registered, the sale would never activate at all). Added `POST /me/subscription/checkout/confirm` + a `/subscription/success` page that confirms the Checkout Session on return (ownership-checked; 409 `pending` retried with backoff; same idempotent activation the webhook uses), enabled **Stripe promotion codes** + email prefill + Stripe-customer reuse, replaced the buried quarterly text-link with two selectable plan cards (`Save 12%` derived from config), gave every paywall redirect a `?reason=` so the plan page names the blocked action, surfaced **`past_due`** as a shell alert → Billing Portal (additive `subscription_status` on `/auth/me`), and shipped a public **`kolabing.com/pricing` + `/es/pricing`** with Product/Offer/FAQPage JSON-LD. Also corrected a real doc/code drift: ROLES §2.1 claimed €39.99/mo, the code has always billed €49/€129 from `config/subscriptions.php`. 1486 tests pass, pint clean. ROLES §2.12 + backend map §14. Prior: **Marketing → web-app funnel wired** (BE-NF-27, branch feat/marketing-webapp-funnel, stacked on feat/kolabing-web-app): kolabing.com had no link to app.kolabing.com at all — every acquisition CTA was a dead `href="#"` or an in-page anchor, and mobile had no header CTA behind an inert hamburger. All surfaces now funnel into /login + /register via `config('webapp.url')`, with role-prefilled `?type=` links where the audience is already known. Prior same day: **Web App rebuilt on the Claude-Design handoff** (branch feat/kolabing-web-app): the whole app.kolabing.com UI replaced with the `kolabing-web-design/` bundle — Anton/Inter self-hosted, cream+#FFE28C palette, 264px sidebar shell, Explore card grid + detail/apply overlays, 4-tab My Kolabs (offers/requests/active/finished), multi-step Create wizard, Profile, Plan, and a new **/notifications** page. Alpine is now self-hosted (`public/webapp-assets/`) instead of a CDN. Two real wiring bugs fixed while wiring it: list endpoints return rows at `data.data` (the old views read `data` and silently rendered empty lists) — now via `kb.rows()`; and the business paywall on **accept** surfaces as **403**, not 402, so unsubscribed businesses dead-ended — now routed to /subscription (documented in ROLES-BACKEND-DB-MAP §3). Accept date picker bounded to the Kolab window + recurring days. **Third bug: the global CSP had no `'unsafe-eval'`, so Alpine (which compiles every x-* expression with `new Function`) never initialised and the whole web app was inert** — `AddSecurityHeaders` now serves a per-host policy: the web-app host gets `'unsafe-eval'` + `accounts.google.com` (script/style/frame, needed by Google Sign-In, which the old CSP also blocked); marketing + /admin keep the strict baseline. Covered by two new tests. es/ca kept at 100%. Prior: 2026-08-15 (**Kolabing Web App consolidated** (epic #128, branch feat/kolabing-web-app): the whole web app on app.kolabing.com — web Google login (BE-NF-18/GOOGLE_CLIENT_ID_WEB), Stripe web checkout (BE-NF-17, source=stripe) + Billing Portal (BE-NF-19), the Blade+Alpine app foundation+auth+subscription (BE-NF-20), feed + Kolab CRUD (BE-NF-21), profile edit + applications (BE-NF-22), image uploads (BE-NF-23), and es/ca i18n + SEO locale URLs (BE-NF-24/25). One PR. Prior same day: BE-NF-16 blog engine + SEO/GEO built, branch feat/blog-engine-seo-geo: public /blog + admin /admin/blog CRUD + Article/Organization/WebSite/FAQPage JSON-LD + homepage meta/OG + sitemap/llms now include posts. Prior 2026-08-02: BE-FX-11 F1 host_profile_id on EventResource + BE-FX-8 chat Prior: 2026-08-19 (**Community members web panel built** (BE-NF-34, branch `feat/community-members-hub`): the NF-6 members & tiers backend had no human-reachable surface at all. Shipped the seven-tab Community Hub on app.kolabing.com plus the backend it needed — a searchable/filterable/sortable roster enriched with real engagement data, a stats endpoint, pending **email invitations** that convert on signup, member detail + bulk actions, and the public **`/c/{slug}` join page that had never existed even though every invite link pointed at it**. Four other live defects fixed on the way (removed members leaking into the roster; the roster ungated despite carrying emails; every member rendering as an email prefix; `kb.rows()` silently returning [] for keyed envelopes). Never paywalled — guarded by a test. es/ca 100%. Prior: 2026-08-19 (**BE-FX-17 — the marketing site's structured data was invalid everywhere**: Laravel 12's `@context` Blade directive silently replaced the `@context` key in every inline `{!! json_encode([...]) !!}` JSON-LD block, so Organization / WebSite / Product / FAQPage markup emitted compiled PHP instead and Google could not interpret any of it. Found while inspecting the new public profile page on production, which was unaffected because it encodes from a `@php` block. Fixed in four templates the same way, with a guard test that parses every ld+json block on ten marketing URLs. Prior same day: 2026-08-19 (**BE-NF-29 — public profile pages, in the panel and on the open web** (branch `feat/public-profiles`): businesses and communities had a rich public profile in the API that **no web surface ever rendered** — the panel had no profile route at all, and a shared Kolabing link led nowhere. Two pages now: `app.kolabing.com/profiles/{id}` shows the full detail (reviews with their authors + the five-category rating breakdown, past events with photos, past collaborations, gallery with a lightbox, contact links), reachable from Explore cards, the Kolab overlay, application rows, collaboration partners, notification actors, the chat header, the sidebar card and the account page. And `kolabing.com/p/{slug}` is a **public, indexable teaser** — identity, rating, three photos and one anonymised quote, with everything else (all reviews + who wrote them, contact links, past-event detail, partners) behind a sign-up wall that is enforced by *absence from the HTML*, not CSS. Slug is `name-<uuid tail>` because `profiles.handle` is only required for attendees (5 of 94 production profiles have one), so keying on it would 404 almost everywhere; old links survive a rename and full UUIDs still resolve. Backend: one additive endpoint `GET /profiles/{id}/public-profile` serving the rich shape for **both** roles — `kolabs.past_events` was always role-agnostic, only the community-scoped route made past events look community-only — plus additive `public_url` on that resource, `actor_profile_id` on notifications and `id` on chat participants so every client can link onward. Profiles with a completed collaboration are listed in `sitemap.xml`; `Organization`/`LocalBusiness` + `AggregateRating` JSON-LD, and the rating block is omitted rather than faked when there are no reviews. 29 new tests, most of them asserting what the public page must NOT contain. **Open decision:** the public page is opt-out-less — see BE-NF-30.) — Prior 2026-08-18 (**Reverb is already deployed — BE-IF-18's "MISSING" list was stale**: `.env` carries live credentials for Laravel Cloud's **managed** Reverb, so the self-hosted-daemon + nginx/TLS steps in that ticket do not apply. Verified by handshaking the real endpoint (accepts `Origin: https://app.kolabing.com`, `activity_timeout: 30`; a no-Origin connection is correctly rejected `4009`) and by running the whole loop — handshake → `/broadcasting/auth` → `pusher:subscribe` → `message.sent` — against a local `reverb:start` + `queue:work` on an isolated SQLite DB. The web panel therefore needs **no code change** for real-time; it lights up on deploy. One thing left to confirm: a **queue worker in production**, since `NewChatMessage` is `ShouldBroadcast` on a `database` queue and without a worker every broadcast is enqueued and never delivered. Docs corrected accordingly (they claimed the daemon was missing).) — Prior same day (**BE-NF-28 — chat integrated into the web panel** (branch `feat/webapp-chat`): app.kolabing.com had no chat at all — no route, no nav entry, not one call to the chat API — while the backend carried the whole feature (thread types, inbox, unread counts, bans, tier-gated channels) and only mobile used it. Added `/chats` (+ `/es`, `/ca`): a two-pane inbox (thread list + conversation, one route, no page load between threads), a `Messages` nav row with its own unread badge on every page, day-separated bubbles, community **channel management** (create ≤5 / rename / delete + per-member block list, owned communities only), and deep links `?thread=` / `?application=` / `?collaboration=` so My Kolabs rows and new-message notifications open the right conversation. Real-time is a **hand-written 250-line Pusher-protocol client** (`public/webapp-assets/kb-realtime.js`) instead of laravel-echo + pusher-js: the panel has no bundler and the CSP forbids CDNs. It stays dormant until BE-IF-18 hands over `REVERB_APP_KEY` and a 4s ticker polls in the meantime, so chat works either way. CSP gained `wss:` on the web-app host only (`connect-src https:` does NOT cover WebSockets). Three real backend gaps were found and filed rather than papered over: BE-FX-13 (silent Kolab messages), BE-FX-14 (§2.8 lapse re-gate not enforced for chat), BE-FX-15 (`/me/communities` hides delegated communities). Verified by replaying every call the page makes against a running server on an isolated SQLite copy (the local `.env` points at the production DB, so nothing was written there) — which caught a real bug in the block list: the member roster pages on `limit`, not `per_page`, and nests rows under `data.members`, where `kb.rows()` cannot find them, so the list rendered empty. Fixed and pinned. `POST /broadcasting/auth` was confirmed to return a valid channel signature for a bearer token, so the auth half of real-time is proven; only the daemon is missing. 1506 tests pass (7088 assertions), pint clean. ROLES §4.1 + backend map §15.) — Prior 2026-08-18 (**BE-FX-12 — `GET /chats` 500 fixed**: the mobile chat inbox was dead in production (`ChatException(null: Server error)`); root cause was `latestOfMany()` aggregating `MAX(uuid)` on the `chat_messages` primary key, which Postgres has no function for, introduced with BE-FX-8's preview. Replaced with a plain `latest()` relation + `ChatService::attachLatestMessages()` (two grouped queries, no N+1). Reproduced AND verified read-only against the production DB; guard test added because the SQLite test suite structurally cannot catch Postgres-only SQL. Needs deploy — see Fixes BE-FX-12.) — Prior 2026-08-17 (**Web payment flow completed** (BE-NF-26, branch feat/kolabing-web-app): the web checkout existed but was not sellable — activation depended entirely on the Stripe webhook, so a business could pay, be shown "you're subscribed", and still be paywalled (and on prod, where the webhook is not yet registered, the sale would never activate at all). Added `POST /me/subscription/checkout/confirm` + a `/subscription/success` page that confirms the Checkout Session on return (ownership-checked; 409 `pending` retried with backoff; same idempotent activation the webhook uses), enabled **Stripe promotion codes** + email prefill + Stripe-customer reuse, replaced the buried quarterly text-link with two selectable plan cards (`Save 12%` derived from config), gave every paywall redirect a `?reason=` so the plan page names the blocked action, surfaced **`past_due`** as a shell alert → Billing Portal (additive `subscription_status` on `/auth/me`), and shipped a public **`kolabing.com/pricing` + `/es/pricing`** with Product/Offer/FAQPage JSON-LD. Also corrected a real doc/code drift: ROLES §2.1 claimed €39.99/mo, the code has always billed €49/€129 from `config/subscriptions.php`. 1486 tests pass, pint clean. ROLES §2.12 + backend map §14. Prior: **Marketing → web-app funnel wired** (BE-NF-27, branch feat/marketing-webapp-funnel, stacked on feat/kolabing-web-app): kolabing.com had no link to app.kolabing.com at all — every acquisition CTA was a dead `href="#"` or an in-page anchor, and mobile had no header CTA behind an inert hamburger. All surfaces now funnel into /login + /register via `config('webapp.url')`, with role-prefilled `?type=` links where the audience is already known. Prior same day: **Web App rebuilt on the Claude-Design handoff** (branch feat/kolabing-web-app): the whole app.kolabing.com UI replaced with the `kolabing-web-design/` bundle — Anton/Inter self-hosted, cream+#FFE28C palette, 264px sidebar shell, Explore card grid + detail/apply overlays, 4-tab My Kolabs (offers/requests/active/finished), multi-step Create wizard, Profile, Plan, and a new **/notifications** page. Alpine is now self-hosted (`public/webapp-assets/`) instead of a CDN. Two real wiring bugs fixed while wiring it: list endpoints return rows at `data.data` (the old views read `data` and silently rendered empty lists) — now via `kb.rows()`; and the business paywall on **accept** surfaces as **403**, not 402, so unsubscribed businesses dead-ended — now routed to /subscription (documented in ROLES-BACKEND-DB-MAP §3). Accept date picker bounded to the Kolab window + recurring days. **Third bug: the global CSP had no `'unsafe-eval'`, so Alpine (which compiles every x-* expression with `new Function`) never initialised and the whole web app was inert** — `AddSecurityHeaders` now serves a per-host policy: the web-app host gets `'unsafe-eval'` + `accounts.google.com` (script/style/frame, needed by Google Sign-In, which the old CSP also blocked); marketing + /admin keep the strict baseline. Covered by two new tests. es/ca kept at 100%. Prior: 2026-08-15 (**Kolabing Web App consolidated** (epic #128, branch feat/kolabing-web-app): the whole web app on app.kolabing.com — web Google login (BE-NF-18/GOOGLE_CLIENT_ID_WEB), Stripe web checkout (BE-NF-17, source=stripe) + Billing Portal (BE-NF-19), the Blade+Alpine app foundation+auth+subscription (BE-NF-20), feed + Kolab CRUD (BE-NF-21), profile edit + applications (BE-NF-22), image uploads (BE-NF-23), and es/ca i18n + SEO locale URLs (BE-NF-24/25). One PR. Prior same day: BE-NF-16 blog engine + SEO/GEO built, branch feat/blog-engine-seo-geo: public /blog + admin /admin/blog CRUD + Article/Organization/WebSite/FAQPage JSON-LD + homepage meta/OG + sitemap/llms now include posts. Prior 2026-08-02: BE-FX-11 F1 host_profile_id on EventResource + BE-FX-8 chat Prior: 2026-08-19 (**BE-NF-34 deployed** — Community Hub is live on prod: migration ran, `/community/*` + `/c/{slug}` verified serving, queue worker draining. Deploy surfaced **BE-FX-18**: prod has no `POSTMARK_API_KEY`, so all outbound email — including the new invitation mail — fails. Env-only fix. Prior: 2026-08-19 (**Community members web panel built** (BE-NF-34, branch `feat/community-members-hub`): the NF-6 members & tiers backend had no human-reachable surface at all. Shipped the seven-tab Community Hub on app.kolabing.com plus the backend it needed — a searchable/filterable/sortable roster enriched with real engagement data, a stats endpoint, pending **email invitations** that convert on signup, member detail + bulk actions, and the public **`/c/{slug}` join page that had never existed even though every invite link pointed at it**. Four other live defects fixed on the way (removed members leaking into the roster; the roster ungated despite carrying emails; every member rendering as an email prefix; `kb.rows()` silently returning [] for keyed envelopes). Never paywalled — guarded by a test. es/ca 100%. Prior: 2026-08-19 (**BE-FX-17 — the marketing site's structured data was invalid everywhere**: Laravel 12's `@context` Blade directive silently replaced the `@context` key in every inline `{!! json_encode([...]) !!}` JSON-LD block, so Organization / WebSite / Product / FAQPage markup emitted compiled PHP instead and Google could not interpret any of it. Found while inspecting the new public profile page on production, which was unaffected because it encodes from a `@php` block. Fixed in four templates the same way, with a guard test that parses every ld+json block on ten marketing URLs. Prior same day: 2026-08-19 (**BE-NF-29 — public profile pages, in the panel and on the open web** (branch `feat/public-profiles`): businesses and communities had a rich public profile in the API that **no web surface ever rendered** — the panel had no profile route at all, and a shared Kolabing link led nowhere. Two pages now: `app.kolabing.com/profiles/{id}` shows the full detail (reviews with their authors + the five-category rating breakdown, past events with photos, past collaborations, gallery with a lightbox, contact links), reachable from Explore cards, the Kolab overlay, application rows, collaboration partners, notification actors, the chat header, the sidebar card and the account page. And `kolabing.com/p/{slug}` is a **public, indexable teaser** — identity, rating, three photos and one anonymised quote, with everything else (all reviews + who wrote them, contact links, past-event detail, partners) behind a sign-up wall that is enforced by *absence from the HTML*, not CSS. Slug is `name-<uuid tail>` because `profiles.handle` is only required for attendees (5 of 94 production profiles have one), so keying on it would 404 almost everywhere; old links survive a rename and full UUIDs still resolve. Backend: one additive endpoint `GET /profiles/{id}/public-profile` serving the rich shape for **both** roles — `kolabs.past_events` was always role-agnostic, only the community-scoped route made past events look community-only — plus additive `public_url` on that resource, `actor_profile_id` on notifications and `id` on chat participants so every client can link onward. Profiles with a completed collaboration are listed in `sitemap.xml`; `Organization`/`LocalBusiness` + `AggregateRating` JSON-LD, and the rating block is omitted rather than faked when there are no reviews. 29 new tests, most of them asserting what the public page must NOT contain. **Open decision:** the public page is opt-out-less — see BE-NF-30.) — Prior 2026-08-18 (**Reverb is already deployed — BE-IF-18's "MISSING" list was stale**: `.env` carries live credentials for Laravel Cloud's **managed** Reverb, so the self-hosted-daemon + nginx/TLS steps in that ticket do not apply. Verified by handshaking the real endpoint (accepts `Origin: https://app.kolabing.com`, `activity_timeout: 30`; a no-Origin connection is correctly rejected `4009`) and by running the whole loop — handshake → `/broadcasting/auth` → `pusher:subscribe` → `message.sent` — against a local `reverb:start` + `queue:work` on an isolated SQLite DB. The web panel therefore needs **no code change** for real-time; it lights up on deploy. One thing left to confirm: a **queue worker in production**, since `NewChatMessage` is `ShouldBroadcast` on a `database` queue and without a worker every broadcast is enqueued and never delivered. Docs corrected accordingly (they claimed the daemon was missing).) — Prior same day (**BE-NF-28 — chat integrated into the web panel** (branch `feat/webapp-chat`): app.kolabing.com had no chat at all — no route, no nav entry, not one call to the chat API — while the backend carried the whole feature (thread types, inbox, unread counts, bans, tier-gated channels) and only mobile used it. Added `/chats` (+ `/es`, `/ca`): a two-pane inbox (thread list + conversation, one route, no page load between threads), a `Messages` nav row with its own unread badge on every page, day-separated bubbles, community **channel management** (create ≤5 / rename / delete + per-member block list, owned communities only), and deep links `?thread=` / `?application=` / `?collaboration=` so My Kolabs rows and new-message notifications open the right conversation. Real-time is a **hand-written 250-line Pusher-protocol client** (`public/webapp-assets/kb-realtime.js`) instead of laravel-echo + pusher-js: the panel has no bundler and the CSP forbids CDNs. It stays dormant until BE-IF-18 hands over `REVERB_APP_KEY` and a 4s ticker polls in the meantime, so chat works either way. CSP gained `wss:` on the web-app host only (`connect-src https:` does NOT cover WebSockets). Three real backend gaps were found and filed rather than papered over: BE-FX-13 (silent Kolab messages), BE-FX-14 (§2.8 lapse re-gate not enforced for chat), BE-FX-15 (`/me/communities` hides delegated communities). Verified by replaying every call the page makes against a running server on an isolated SQLite copy (the local `.env` points at the production DB, so nothing was written there) — which caught a real bug in the block list: the member roster pages on `limit`, not `per_page`, and nests rows under `data.members`, where `kb.rows()` cannot find them, so the list rendered empty. Fixed and pinned. `POST /broadcasting/auth` was confirmed to return a valid channel signature for a bearer token, so the auth half of real-time is proven; only the daemon is missing. 1506 tests pass (7088 assertions), pint clean. ROLES §4.1 + backend map §15.) — Prior 2026-08-18 (**BE-FX-12 — `GET /chats` 500 fixed**: the mobile chat inbox was dead in production (`ChatException(null: Server error)`); root cause was `latestOfMany()` aggregating `MAX(uuid)` on the `chat_messages` primary key, which Postgres has no function for, introduced with BE-FX-8's preview. Replaced with a plain `latest()` relation + `ChatService::attachLatestMessages()` (two grouped queries, no N+1). Reproduced AND verified read-only against the production DB; guard test added because the SQLite test suite structurally cannot catch Postgres-only SQL. Needs deploy — see Fixes BE-FX-12.) — Prior 2026-08-17 (**Web payment flow completed** (BE-NF-26, branch feat/kolabing-web-app): the web checkout existed but was not sellable — activation depended entirely on the Stripe webhook, so a business could pay, be shown "you're subscribed", and still be paywalled (and on prod, where the webhook is not yet registered, the sale would never activate at all). Added `POST /me/subscription/checkout/confirm` + a `/subscription/success` page that confirms the Checkout Session on return (ownership-checked; 409 `pending` retried with backoff; same idempotent activation the webhook uses), enabled **Stripe promotion codes** + email prefill + Stripe-customer reuse, replaced the buried quarterly text-link with two selectable plan cards (`Save 12%` derived from config), gave every paywall redirect a `?reason=` so the plan page names the blocked action, surfaced **`past_due`** as a shell alert → Billing Portal (additive `subscription_status` on `/auth/me`), and shipped a public **`kolabing.com/pricing` + `/es/pricing`** with Product/Offer/FAQPage JSON-LD. Also corrected a real doc/code drift: ROLES §2.1 claimed €39.99/mo, the code has always billed €49/€129 from `config/subscriptions.php`. 1486 tests pass, pint clean. ROLES §2.12 + backend map §14. Prior: **Marketing → web-app funnel wired** (BE-NF-27, branch feat/marketing-webapp-funnel, stacked on feat/kolabing-web-app): kolabing.com had no link to app.kolabing.com at all — every acquisition CTA was a dead `href="#"` or an in-page anchor, and mobile had no header CTA behind an inert hamburger. All surfaces now funnel into /login + /register via `config('webapp.url')`, with role-prefilled `?type=` links where the audience is already known. Prior same day: **Web App rebuilt on the Claude-Design handoff** (branch feat/kolabing-web-app): the whole app.kolabing.com UI replaced with the `kolabing-web-design/` bundle — Anton/Inter self-hosted, cream+#FFE28C palette, 264px sidebar shell, Explore card grid + detail/apply overlays, 4-tab My Kolabs (offers/requests/active/finished), multi-step Create wizard, Profile, Plan, and a new **/notifications** page. Alpine is now self-hosted (`public/webapp-assets/`) instead of a CDN. Two real wiring bugs fixed while wiring it: list endpoints return rows at `data.data` (the old views read `data` and silently rendered empty lists) — now via `kb.rows()`; and the business paywall on **accept** surfaces as **403**, not 402, so unsubscribed businesses dead-ended — now routed to /subscription (documented in ROLES-BACKEND-DB-MAP §3). Accept date picker bounded to the Kolab window + recurring days. **Third bug: the global CSP had no `'unsafe-eval'`, so Alpine (which compiles every x-* expression with `new Function`) never initialised and the whole web app was inert** — `AddSecurityHeaders` now serves a per-host policy: the web-app host gets `'unsafe-eval'` + `accounts.google.com` (script/style/frame, needed by Google Sign-In, which the old CSP also blocked); marketing + /admin keep the strict baseline. Covered by two new tests. es/ca kept at 100%. Prior: 2026-08-15 (**Kolabing Web App consolidated** (epic #128, branch feat/kolabing-web-app): the whole web app on app.kolabing.com — web Google login (BE-NF-18/GOOGLE_CLIENT_ID_WEB), Stripe web checkout (BE-NF-17, source=stripe) + Billing Portal (BE-NF-19), the Blade+Alpine app foundation+auth+subscription (BE-NF-20), feed + Kolab CRUD (BE-NF-21), profile edit + applications (BE-NF-22), image uploads (BE-NF-23), and es/ca i18n + SEO locale URLs (BE-NF-24/25). One PR. Prior same day: BE-NF-16 blog engine + SEO/GEO built, branch feat/blog-engine-seo-geo: public /blog + admin /admin/blog CRUD + Article/Organization/WebSite/FAQPage JSON-LD + homepage meta/OG + sitemap/llms now include posts. Prior 2026-08-02: BE-FX-11 F1 host_profile_id on EventResource + BE-FX-8 chat Prior: 2026-08-19 (**BE-FX-18 withdrawn — not a bug.** `POSTMARK_API_KEY` IS set on prod; the `failed_jobs` rows I read were historical (newest 07:27, none since — verified 13:15, spanning the BE-NF-34 deploy). BE-NF-34 is therefore **deployed and verified end-to-end**, invitation email included. Prior: 2026-08-19 (**BE-NF-34 deployed** — Community Hub is live on prod: migration ran, `/community/*` + `/c/{slug}` verified serving, queue worker draining. Deploy surfaced **BE-FX-18**: prod has no `POSTMARK_API_KEY`, so all outbound email — including the new invitation mail — fails. Env-only fix. Prior: 2026-08-19 (**Community members web panel built** (BE-NF-34, branch `feat/community-members-hub`): the NF-6 members & tiers backend had no human-reachable surface at all. Shipped the seven-tab Community Hub on app.kolabing.com plus the backend it needed — a searchable/filterable/sortable roster enriched with real engagement data, a stats endpoint, pending **email invitations** that convert on signup, member detail + bulk actions, and the public **`/c/{slug}` join page that had never existed even though every invite link pointed at it**. Four other live defects fixed on the way (removed members leaking into the roster; the roster ungated despite carrying emails; every member rendering as an email prefix; `kb.rows()` silently returning [] for keyed envelopes). Never paywalled — guarded by a test. es/ca 100%. Prior: 2026-08-19 (**BE-FX-17 — the marketing site's structured data was invalid everywhere**: Laravel 12's `@context` Blade directive silently replaced the `@context` key in every inline `{!! json_encode([...]) !!}` JSON-LD block, so Organization / WebSite / Product / FAQPage markup emitted compiled PHP instead and Google could not interpret any of it. Found while inspecting the new public profile page on production, which was unaffected because it encodes from a `@php` block. Fixed in four templates the same way, with a guard test that parses every ld+json block on ten marketing URLs. Prior same day: 2026-08-19 (**BE-NF-29 — public profile pages, in the panel and on the open web** (branch `feat/public-profiles`): businesses and communities had a rich public profile in the API that **no web surface ever rendered** — the panel had no profile route at all, and a shared Kolabing link led nowhere. Two pages now: `app.kolabing.com/profiles/{id}` shows the full detail (reviews with their authors + the five-category rating breakdown, past events with photos, past collaborations, gallery with a lightbox, contact links), reachable from Explore cards, the Kolab overlay, application rows, collaboration partners, notification actors, the chat header, the sidebar card and the account page. And `kolabing.com/p/{slug}` is a **public, indexable teaser** — identity, rating, three photos and one anonymised quote, with everything else (all reviews + who wrote them, contact links, past-event detail, partners) behind a sign-up wall that is enforced by *absence from the HTML*, not CSS. Slug is `name-<uuid tail>` because `profiles.handle` is only required for attendees (5 of 94 production profiles have one), so keying on it would 404 almost everywhere; old links survive a rename and full UUIDs still resolve. Backend: one additive endpoint `GET /profiles/{id}/public-profile` serving the rich shape for **both** roles — `kolabs.past_events` was always role-agnostic, only the community-scoped route made past events look community-only — plus additive `public_url` on that resource, `actor_profile_id` on notifications and `id` on chat participants so every client can link onward. Profiles with a completed collaboration are listed in `sitemap.xml`; `Organization`/`LocalBusiness` + `AggregateRating` JSON-LD, and the rating block is omitted rather than faked when there are no reviews. 29 new tests, most of them asserting what the public page must NOT contain. **Open decision:** the public page is opt-out-less — see BE-NF-30.) — Prior 2026-08-18 (**Reverb is already deployed — BE-IF-18's "MISSING" list was stale**: `.env` carries live credentials for Laravel Cloud's **managed** Reverb, so the self-hosted-daemon + nginx/TLS steps in that ticket do not apply. Verified by handshaking the real endpoint (accepts `Origin: https://app.kolabing.com`, `activity_timeout: 30`; a no-Origin connection is correctly rejected `4009`) and by running the whole loop — handshake → `/broadcasting/auth` → `pusher:subscribe` → `message.sent` — against a local `reverb:start` + `queue:work` on an isolated SQLite DB. The web panel therefore needs **no code change** for real-time; it lights up on deploy. One thing left to confirm: a **queue worker in production**, since `NewChatMessage` is `ShouldBroadcast` on a `database` queue and without a worker every broadcast is enqueued and never delivered. Docs corrected accordingly (they claimed the daemon was missing).) — Prior same day (**BE-NF-28 — chat integrated into the web panel** (branch `feat/webapp-chat`): app.kolabing.com had no chat at all — no route, no nav entry, not one call to the chat API — while the backend carried the whole feature (thread types, inbox, unread counts, bans, tier-gated channels) and only mobile used it. Added `/chats` (+ `/es`, `/ca`): a two-pane inbox (thread list + conversation, one route, no page load between threads), a `Messages` nav row with its own unread badge on every page, day-separated bubbles, community **channel management** (create ≤5 / rename / delete + per-member block list, owned communities only), and deep links `?thread=` / `?application=` / `?collaboration=` so My Kolabs rows and new-message notifications open the right conversation. Real-time is a **hand-written 250-line Pusher-protocol client** (`public/webapp-assets/kb-realtime.js`) instead of laravel-echo + pusher-js: the panel has no bundler and the CSP forbids CDNs. It stays dormant until BE-IF-18 hands over `REVERB_APP_KEY` and a 4s ticker polls in the meantime, so chat works either way. CSP gained `wss:` on the web-app host only (`connect-src https:` does NOT cover WebSockets). Three real backend gaps were found and filed rather than papered over: BE-FX-13 (silent Kolab messages), BE-FX-14 (§2.8 lapse re-gate not enforced for chat), BE-FX-15 (`/me/communities` hides delegated communities). Verified by replaying every call the page makes against a running server on an isolated SQLite copy (the local `.env` points at the production DB, so nothing was written there) — which caught a real bug in the block list: the member roster pages on `limit`, not `per_page`, and nests rows under `data.members`, where `kb.rows()` cannot find them, so the list rendered empty. Fixed and pinned. `POST /broadcasting/auth` was confirmed to return a valid channel signature for a bearer token, so the auth half of real-time is proven; only the daemon is missing. 1506 tests pass (7088 assertions), pint clean. ROLES §4.1 + backend map §15.) — Prior 2026-08-18 (**BE-FX-12 — `GET /chats` 500 fixed**: the mobile chat inbox was dead in production (`ChatException(null: Server error)`); root cause was `latestOfMany()` aggregating `MAX(uuid)` on the `chat_messages` primary key, which Postgres has no function for, introduced with BE-FX-8's preview. Replaced with a plain `latest()` relation + `ChatService::attachLatestMessages()` (two grouped queries, no N+1). Reproduced AND verified read-only against the production DB; guard test added because the SQLite test suite structurally cannot catch Postgres-only SQL. Needs deploy — see Fixes BE-FX-12.) — Prior 2026-08-17 (**Web payment flow completed** (BE-NF-26, branch feat/kolabing-web-app): the web checkout existed but was not sellable — activation depended entirely on the Stripe webhook, so a business could pay, be shown "you're subscribed", and still be paywalled (and on prod, where the webhook is not yet registered, the sale would never activate at all). Added `POST /me/subscription/checkout/confirm` + a `/subscription/success` page that confirms the Checkout Session on return (ownership-checked; 409 `pending` retried with backoff; same idempotent activation the webhook uses), enabled **Stripe promotion codes** + email prefill + Stripe-customer reuse, replaced the buried quarterly text-link with two selectable plan cards (`Save 12%` derived from config), gave every paywall redirect a `?reason=` so the plan page names the blocked action, surfaced **`past_due`** as a shell alert → Billing Portal (additive `subscription_status` on `/auth/me`), and shipped a public **`kolabing.com/pricing` + `/es/pricing`** with Product/Offer/FAQPage JSON-LD. Also corrected a real doc/code drift: ROLES §2.1 claimed €39.99/mo, the code has always billed €49/€129 from `config/subscriptions.php`. 1486 tests pass, pint clean. ROLES §2.12 + backend map §14. Prior: **Marketing → web-app funnel wired** (BE-NF-27, branch feat/marketing-webapp-funnel, stacked on feat/kolabing-web-app): kolabing.com had no link to app.kolabing.com at all — every acquisition CTA was a dead `href="#"` or an in-page anchor, and mobile had no header CTA behind an inert hamburger. All surfaces now funnel into /login + /register via `config('webapp.url')`, with role-prefilled `?type=` links where the audience is already known. Prior same day: **Web App rebuilt on the Claude-Design handoff** (branch feat/kolabing-web-app): the whole app.kolabing.com UI replaced with the `kolabing-web-design/` bundle — Anton/Inter self-hosted, cream+#FFE28C palette, 264px sidebar shell, Explore card grid + detail/apply overlays, 4-tab My Kolabs (offers/requests/active/finished), multi-step Create wizard, Profile, Plan, and a new **/notifications** page. Alpine is now self-hosted (`public/webapp-assets/`) instead of a CDN. Two real wiring bugs fixed while wiring it: list endpoints return rows at `data.data` (the old views read `data` and silently rendered empty lists) — now via `kb.rows()`; and the business paywall on **accept** surfaces as **403**, not 402, so unsubscribed businesses dead-ended — now routed to /subscription (documented in ROLES-BACKEND-DB-MAP §3). Accept date picker bounded to the Kolab window + recurring days. **Third bug: the global CSP had no `'unsafe-eval'`, so Alpine (which compiles every x-* expression with `new Function`) never initialised and the whole web app was inert** — `AddSecurityHeaders` now serves a per-host policy: the web-app host gets `'unsafe-eval'` + `accounts.google.com` (script/style/frame, needed by Google Sign-In, which the old CSP also blocked); marketing + /admin keep the strict baseline. Covered by two new tests. es/ca kept at 100%. Prior: 2026-08-22 (**BE-NF-37 — attendee'nin web yüzeyi**: attendee rolünün webde hiç girişi yoktu (kayıt ekranında rol olarak bile yok, tüm yüzey yayınlanmamış mobilde). `kolabing.com/events` + `/events/{slug}` herkese açık ve indekslenebilir oldu; yalnızca `EventVisibility::Public` — hem listelemede hem çözümlemede zorlanıyor, çünkü listelenmemek okunamamak değil. Detay açık, duvar eylemde: RSVP panele `?rsvp=1` ile devrediliyor (herkese açık origin token'ı okuyamaz). Kayıtta üçüncü rol kartı, panelde ev sahibi olmayan için RSVP/bekleme listesi. Yeni endpoint ve şema değişikliği yok — discovery ve RSVP uygunluk kuralları zaten doğruydu, yeniden yazılmadı. Prior: 2026-08-22 (**Check-in artık iki istemci için sözleşmeli** (BE-NF-35 güncellemesi): mobil de aynı yüzeyi açacağı için senkron kuralları yazıldı ve zorlandı. Bir gün canlı kalan tuzak düzeltildi — `generate-qr` her çağrıda yeni token üretiyordu, yani ev sahibi ikinci cihazda kapıyı açınca ilkindeki QR sessizce ölüyordu; açmak idempotent, retire etmek açık bir eylem oldu. Gelenler artık yayınlanıyor (`AttendeeCheckedIn` → `private-event.{id}.door`, varış + yürüyen toplam), polling yedek; kanal yetkisi üç yerde tekrarlanan sahiplik kontrolü yerine tek `Event::isHostedBy()` ile. Tek QR'ın hem uygulamayı hem tarayıcıyı açması için app host'una Universal Links / App Links dosyaları eklendi (mobil kimlikler gelene kadar 404). Yeni karar kalemi: BE-NF-36 çevrimdışı check-in ve `checked_in_at`'in kime ait olduğu. Prior: 2026-08-21 (**BE-NF-35 — etkinlik + check-in kapısı panele geldi, BE-FX-20 kırık QR URL'i düzeltildi**: check-in API'si 13 uç noktayla hazırdı ama hiçbir istemcisi yoktu (yalnızca mobil, o da yayında değil), ve QR URL'i kuran tek yer var olmayan bir route'a işaret ediyordu — okutan telefon 404 alıyordu. Panelde `/events`, `/events/{id}` (kapı: QR + yazılabilir kod + canlı sayaç + gelenler) ve `/checkin/{token}` (QR'ın açtığı katılımcı ekranı, `?next=` ile giriş sonrası geri dönüş). QR bağımlılık eklenmeden sunucuda üretiliyor ve bağımsız bir çözücüyle doğrulanmış dondurulmuş matrislerle test ediliyor. Token artık süreli ve yalnızca ev sahibine dönüyor. Beyan/gerçek katılım oranı hem listede hem kapı ekranında. Prior: 2026-08-20 (**BE-NF-38 — the invite page never worked, now fixed and given a join form**: its CTA sat in `<template x-if>` blocks on a layout with no Alpine, and the CSP grants 'unsafe-eval' only to the app host, so nothing was ever painted. Moved the page to the app host rather than weakening a security header; added Google sign-up plus a name/phone/photo form so an invitee joins in place. The regression test now asserts the page can RUN its Alpine, not just that the markup is there. Prior: 2026-08-20 (**BE-NF-37 phone preview built**: a read-only replica of the mobile app's profile screen in a device frame beside every Profile-section tab, refreshed after each edit — so reordering a gallery shows immediately which photo leads on a phone. Mirrors three named Dart files in kolabing-app; the pairing is recorded in backend map §17.5. No API, schema or role change. Prior: 2026-08-20 (**BE-NF-36 public portfolio panel built**: business and community accounts can finally manage the portfolio their public profile renders — gallery, past events, per-event photos — from a tabbed Profile section, with a Preview of their own public page. The find that shaped it: 'past events' lives in two unrelated stores and the profile read only the smaller one, so **60 past events and 173 photographs were invisible**; both are merged now. Also fixed: the gallery had `caption` and `sort_order` columns no endpoint ever wrote. No migration. Prior: 2026-08-19 (**SEO denetimi + düzeltmeleri**: kolabing.com canlı olarak denetlendi (16 sayfa) ve bulunan sekiz kod hatası aynı gün düzeltildi — BE-FX-19. En büyüğü ana sayfanın **24,58 MB görsel** yükü (→ 860 KB WebP, %96,5); ayrıca `www` ikinci bir kanonik site olarak yayındaydı, ana sayfa hiçbir ticari sayfaya link vermiyordu, pazarlama HTML'i hiç önbelleklenmiyordu, boş `/blog` ve `/communities` indekslenebilir ince sayfalardı ve sitemap eşiği bir seed test hesabını indekse sokuyordu. Denetimin kod dışı kalan üç kalemi ayrı iş olarak açıldı: BE-NF-31 (Tailwind CDN → build, görsel doğrulama gerektiriyor), BE-NF-32 (blog içeriği), BE-NF-33 (İspanyolca sayfalar). Prior: 2026-08-19 (**BE-FX-17 — the marketing site's structured data was invalid everywhere**: Laravel 12's `@context` Blade directive silently replaced the `@context` key in every inline `{!! json_encode([...]) !!}` JSON-LD block, so Organization / WebSite / Product / FAQPage markup emitted compiled PHP instead and Google could not interpret any of it. Found while inspecting the new public profile page on production, which was unaffected because it encodes from a `@php` block. Fixed in four templates the same way, with a guard test that parses every ld+json block on ten marketing URLs. Prior same day: 2026-08-19 (**BE-NF-29 — public profile pages, in the panel and on the open web** (branch `feat/public-profiles`): businesses and communities had a rich public profile in the API that **no web surface ever rendered** — the panel had no profile route at all, and a shared Kolabing link led nowhere. Two pages now: `app.kolabing.com/profiles/{id}` shows the full detail (reviews with their authors + the five-category rating breakdown, past events with photos, past collaborations, gallery with a lightbox, contact links), reachable from Explore cards, the Kolab overlay, application rows, collaboration partners, notification actors, the chat header, the sidebar card and the account page. And `kolabing.com/p/{slug}` is a **public, indexable teaser** — identity, rating, three photos and one anonymised quote, with everything else (all reviews + who wrote them, contact links, past-event detail, partners) behind a sign-up wall that is enforced by *absence from the HTML*, not CSS. Slug is `name-<uuid tail>` because `profiles.handle` is only required for attendees (5 of 94 production profiles have one), so keying on it would 404 almost everywhere; old links survive a rename and full UUIDs still resolve. Backend: one additive endpoint `GET /profiles/{id}/public-profile` serving the rich shape for **both** roles — `kolabs.past_events` was always role-agnostic, only the community-scoped route made past events look community-only — plus additive `public_url` on that resource, `actor_profile_id` on notifications and `id` on chat participants so every client can link onward. Profiles with a completed collaboration are listed in `sitemap.xml`; `Organization`/`LocalBusiness` + `AggregateRating` JSON-LD, and the rating block is omitted rather than faked when there are no reviews. 29 new tests, most of them asserting what the public page must NOT contain. **Open decision:** the public page is opt-out-less — see BE-NF-30.) — Prior 2026-08-18 (**Reverb is already deployed — BE-IF-18's "MISSING" list was stale**: `.env` carries live credentials for Laravel Cloud's **managed** Reverb, so the self-hosted-daemon + nginx/TLS steps in that ticket do not apply. Verified by handshaking the real endpoint (accepts `Origin: https://app.kolabing.com`, `activity_timeout: 30`; a no-Origin connection is correctly rejected `4009`) and by running the whole loop — handshake → `/broadcasting/auth` → `pusher:subscribe` → `message.sent` — against a local `reverb:start` + `queue:work` on an isolated SQLite DB. The web panel therefore needs **no code change** for real-time; it lights up on deploy. One thing left to confirm: a **queue worker in production**, since `NewChatMessage` is `ShouldBroadcast` on a `database` queue and without a worker every broadcast is enqueued and never delivered. Docs corrected accordingly (they claimed the daemon was missing).) — Prior same day (**BE-NF-28 — chat integrated into the web panel** (branch `feat/webapp-chat`): app.kolabing.com had no chat at all — no route, no nav entry, not one call to the chat API — while the backend carried the whole feature (thread types, inbox, unread counts, bans, tier-gated channels) and only mobile used it. Added `/chats` (+ `/es`, `/ca`): a two-pane inbox (thread list + conversation, one route, no page load between threads), a `Messages` nav row with its own unread badge on every page, day-separated bubbles, community **channel management** (create ≤5 / rename / delete + per-member block list, owned communities only), and deep links `?thread=` / `?application=` / `?collaboration=` so My Kolabs rows and new-message notifications open the right conversation. Real-time is a **hand-written 250-line Pusher-protocol client** (`public/webapp-assets/kb-realtime.js`) instead of laravel-echo + pusher-js: the panel has no bundler and the CSP forbids CDNs. It stays dormant until BE-IF-18 hands over `REVERB_APP_KEY` and a 4s ticker polls in the meantime, so chat works either way. CSP gained `wss:` on the web-app host only (`connect-src https:` does NOT cover WebSockets). Three real backend gaps were found and filed rather than papered over: BE-FX-13 (silent Kolab messages), BE-FX-14 (§2.8 lapse re-gate not enforced for chat), BE-FX-15 (`/me/communities` hides delegated communities). Verified by replaying every call the page makes against a running server on an isolated SQLite copy (the local `.env` points at the production DB, so nothing was written there) — which caught a real bug in the block list: the member roster pages on `limit`, not `per_page`, and nests rows under `data.members`, where `kb.rows()` cannot find them, so the list rendered empty. Fixed and pinned. `POST /broadcasting/auth` was confirmed to return a valid channel signature for a bearer token, so the auth half of real-time is proven; only the daemon is missing. 1506 tests pass (7088 assertions), pint clean. ROLES §4.1 + backend map §15.) — Prior 2026-08-18 (**BE-FX-12 — `GET /chats` 500 fixed**: the mobile chat inbox was dead in production (`ChatException(null: Server error)`); root cause was `latestOfMany()` aggregating `MAX(uuid)` on the `chat_messages` primary key, which Postgres has no function for, introduced with BE-FX-8's preview. Replaced with a plain `latest()` relation + `ChatService::attachLatestMessages()` (two grouped queries, no N+1). Reproduced AND verified read-only against the production DB; guard test added because the SQLite test suite structurally cannot catch Postgres-only SQL. Needs deploy — see Fixes BE-FX-12.) — Prior 2026-08-17 (**Web payment flow completed** (BE-NF-26, branch feat/kolabing-web-app): the web checkout existed but was not sellable — activation depended entirely on the Stripe webhook, so a business could pay, be shown "you're subscribed", and still be paywalled (and on prod, where the webhook is not yet registered, the sale would never activate at all). Added `POST /me/subscription/checkout/confirm` + a `/subscription/success` page that confirms the Checkout Session on return (ownership-checked; 409 `pending` retried with backoff; same idempotent activation the webhook uses), enabled **Stripe promotion codes** + email prefill + Stripe-customer reuse, replaced the buried quarterly text-link with two selectable plan cards (`Save 12%` derived from config), gave every paywall redirect a `?reason=` so the plan page names the blocked action, surfaced **`past_due`** as a shell alert → Billing Portal (additive `subscription_status` on `/auth/me`), and shipped a public **`kolabing.com/pricing` + `/es/pricing`** with Product/Offer/FAQPage JSON-LD. Also corrected a real doc/code drift: ROLES §2.1 claimed €39.99/mo, the code has always billed €49/€129 from `config/subscriptions.php`. 1486 tests pass, pint clean. ROLES §2.12 + backend map §14. Prior: **Marketing → web-app funnel wired** (BE-NF-27, branch feat/marketing-webapp-funnel, stacked on feat/kolabing-web-app): kolabing.com had no link to app.kolabing.com at all — every acquisition CTA was a dead `href="#"` or an in-page anchor, and mobile had no header CTA behind an inert hamburger. All surfaces now funnel into /login + /register via `config('webapp.url')`, with role-prefilled `?type=` links where the audience is already known. Prior same day: **Web App rebuilt on the Claude-Design handoff** (branch feat/kolabing-web-app): the whole app.kolabing.com UI replaced with the `kolabing-web-design/` bundle — Anton/Inter self-hosted, cream+#FFE28C palette, 264px sidebar shell, Explore card grid + detail/apply overlays, 4-tab My Kolabs (offers/requests/active/finished), multi-step Create wizard, Profile, Plan, and a new **/notifications** page. Alpine is now self-hosted (`public/webapp-assets/`) instead of a CDN. Two real wiring bugs fixed while wiring it: list endpoints return rows at `data.data` (the old views read `data` and silently rendered empty lists) — now via `kb.rows()`; and the business paywall on **accept** surfaces as **403**, not 402, so unsubscribed businesses dead-ended — now routed to /subscription (documented in ROLES-BACKEND-DB-MAP §3). Accept date picker bounded to the Kolab window + recurring days. **Third bug: the global CSP had no `'unsafe-eval'`, so Alpine (which compiles every x-* expression with `new Function`) never initialised and the whole web app was inert** — `AddSecurityHeaders` now serves a per-host policy: the web-app host gets `'unsafe-eval'` + `accounts.google.com` (script/style/frame, needed by Google Sign-In, which the old CSP also blocked); marketing + /admin keep the strict baseline. Covered by two new tests. es/ca kept at 100%. Prior: 2026-08-15 (**Kolabing Web App consolidated** (epic #128, branch feat/kolabing-web-app): the whole web app on app.kolabing.com — web Google login (BE-NF-18/GOOGLE_CLIENT_ID_WEB), Stripe web checkout (BE-NF-17, source=stripe) + Billing Portal (BE-NF-19), the Blade+Alpine app foundation+auth+subscription (BE-NF-20), feed + Kolab CRUD (BE-NF-21), profile edit + applications (BE-NF-22), image uploads (BE-NF-23), and es/ca i18n + SEO locale URLs (BE-NF-24/25). One PR. Prior same day: BE-NF-16 blog engine + SEO/GEO built, branch feat/blog-engine-seo-geo: public /blog + admin /admin/blog CRUD + Article/Organization/WebSite/FAQPage JSON-LD + homepage meta/OG + sitemap/llms now include posts. Prior 2026-08-02: BE-FX-11 F1 host_profile_id on EventResource + BE-FX-8 chat Prior: 2026-08-19 (**Community members web panel built** (BE-NF-34, branch `feat/community-members-hub`): the NF-6 members & tiers backend had no human-reachable surface at all. Shipped the seven-tab Community Hub on app.kolabing.com plus the backend it needed — a searchable/filterable/sortable roster enriched with real engagement data, a stats endpoint, pending **email invitations** that convert on signup, member detail + bulk actions, and the public **`/c/{slug}` join page that had never existed even though every invite link pointed at it**. Four other live defects fixed on the way (removed members leaking into the roster; the roster ungated despite carrying emails; every member rendering as an email prefix; `kb.rows()` silently returning [] for keyed envelopes). Never paywalled — guarded by a test. es/ca 100%. Prior: 2026-08-19 (**BE-FX-17 — the marketing site's structured data was invalid everywhere**: Laravel 12's `@context` Blade directive silently replaced the `@context` key in every inline `{!! json_encode([...]) !!}` JSON-LD block, so Organization / WebSite / Product / FAQPage markup emitted compiled PHP instead and Google could not interpret any of it. Found while inspecting the new public profile page on production, which was unaffected because it encodes from a `@php` block. Fixed in four templates the same way, with a guard test that parses every ld+json block on ten marketing URLs. Prior same day: 2026-08-19 (**BE-NF-29 — public profile pages, in the panel and on the open web** (branch `feat/public-profiles`): businesses and communities had a rich public profile in the API that **no web surface ever rendered** — the panel had no profile route at all, and a shared Kolabing link led nowhere. Two pages now: `app.kolabing.com/profiles/{id}` shows the full detail (reviews with their authors + the five-category rating breakdown, past events with photos, past collaborations, gallery with a lightbox, contact links), reachable from Explore cards, the Kolab overlay, application rows, collaboration partners, notification actors, the chat header, the sidebar card and the account page. And `kolabing.com/p/{slug}` is a **public, indexable teaser** — identity, rating, three photos and one anonymised quote, with everything else (all reviews + who wrote them, contact links, past-event detail, partners) behind a sign-up wall that is enforced by *absence from the HTML*, not CSS. Slug is `name-<uuid tail>` because `profiles.handle` is only required for attendees (5 of 94 production profiles have one), so keying on it would 404 almost everywhere; old links survive a rename and full UUIDs still resolve. Backend: one additive endpoint `GET /profiles/{id}/public-profile` serving the rich shape for **both** roles — `kolabs.past_events` was always role-agnostic, only the community-scoped route made past events look community-only — plus additive `public_url` on that resource, `actor_profile_id` on notifications and `id` on chat participants so every client can link onward. Profiles with a completed collaboration are listed in `sitemap.xml`; `Organization`/`LocalBusiness` + `AggregateRating` JSON-LD, and the rating block is omitted rather than faked when there are no reviews. 29 new tests, most of them asserting what the public page must NOT contain. **Open decision:** the public page is opt-out-less — see BE-NF-30.) — Prior 2026-08-18 (**Reverb is already deployed — BE-IF-18's "MISSING" list was stale**: `.env` carries live credentials for Laravel Cloud's **managed** Reverb, so the self-hosted-daemon + nginx/TLS steps in that ticket do not apply. Verified by handshaking the real endpoint (accepts `Origin: https://app.kolabing.com`, `activity_timeout: 30`; a no-Origin connection is correctly rejected `4009`) and by running the whole loop — handshake → `/broadcasting/auth` → `pusher:subscribe` → `message.sent` — against a local `reverb:start` + `queue:work` on an isolated SQLite DB. The web panel therefore needs **no code change** for real-time; it lights up on deploy. One thing left to confirm: a **queue worker in production**, since `NewChatMessage` is `ShouldBroadcast` on a `database` queue and without a worker every broadcast is enqueued and never delivered. Docs corrected accordingly (they claimed the daemon was missing).) — Prior same day (**BE-NF-28 — chat integrated into the web panel** (branch `feat/webapp-chat`): app.kolabing.com had no chat at all — no route, no nav entry, not one call to the chat API — while the backend carried the whole feature (thread types, inbox, unread counts, bans, tier-gated channels) and only mobile used it. Added `/chats` (+ `/es`, `/ca`): a two-pane inbox (thread list + conversation, one route, no page load between threads), a `Messages` nav row with its own unread badge on every page, day-separated bubbles, community **channel management** (create ≤5 / rename / delete + per-member block list, owned communities only), and deep links `?thread=` / `?application=` / `?collaboration=` so My Kolabs rows and new-message notifications open the right conversation. Real-time is a **hand-written 250-line Pusher-protocol client** (`public/webapp-assets/kb-realtime.js`) instead of laravel-echo + pusher-js: the panel has no bundler and the CSP forbids CDNs. It stays dormant until BE-IF-18 hands over `REVERB_APP_KEY` and a 4s ticker polls in the meantime, so chat works either way. CSP gained `wss:` on the web-app host only (`connect-src https:` does NOT cover WebSockets). Three real backend gaps were found and filed rather than papered over: BE-FX-13 (silent Kolab messages), BE-FX-14 (§2.8 lapse re-gate not enforced for chat), BE-FX-15 (`/me/communities` hides delegated communities). Verified by replaying every call the page makes against a running server on an isolated SQLite copy (the local `.env` points at the production DB, so nothing was written there) — which caught a real bug in the block list: the member roster pages on `limit`, not `per_page`, and nests rows under `data.members`, where `kb.rows()` cannot find them, so the list rendered empty. Fixed and pinned. `POST /broadcasting/auth` was confirmed to return a valid channel signature for a bearer token, so the auth half of real-time is proven; only the daemon is missing. 1506 tests pass (7088 assertions), pint clean. ROLES §4.1 + backend map §15.) — Prior 2026-08-18 (**BE-FX-12 — `GET /chats` 500 fixed**: the mobile chat inbox was dead in production (`ChatException(null: Server error)`); root cause was `latestOfMany()` aggregating `MAX(uuid)` on the `chat_messages` primary key, which Postgres has no function for, introduced with BE-FX-8's preview. Replaced with a plain `latest()` relation + `ChatService::attachLatestMessages()` (two grouped queries, no N+1). Reproduced AND verified read-only against the production DB; guard test added because the SQLite test suite structurally cannot catch Postgres-only SQL. Needs deploy — see Fixes BE-FX-12.) — Prior 2026-08-17 (**Web payment flow completed** (BE-NF-26, branch feat/kolabing-web-app): the web checkout existed but was not sellable — activation depended entirely on the Stripe webhook, so a business could pay, be shown "you're subscribed", and still be paywalled (and on prod, where the webhook is not yet registered, the sale would never activate at all). Added `POST /me/subscription/checkout/confirm` + a `/subscription/success` page that confirms the Checkout Session on return (ownership-checked; 409 `pending` retried with backoff; same idempotent activation the webhook uses), enabled **Stripe promotion codes** + email prefill + Stripe-customer reuse, replaced the buried quarterly text-link with two selectable plan cards (`Save 12%` derived from config), gave every paywall redirect a `?reason=` so the plan page names the blocked action, surfaced **`past_due`** as a shell alert → Billing Portal (additive `subscription_status` on `/auth/me`), and shipped a public **`kolabing.com/pricing` + `/es/pricing`** with Product/Offer/FAQPage JSON-LD. Also corrected a real doc/code drift: ROLES §2.1 claimed €39.99/mo, the code has always billed €49/€129 from `config/subscriptions.php`. 1486 tests pass, pint clean. ROLES §2.12 + backend map §14. Prior: **Marketing → web-app funnel wired** (BE-NF-27, branch feat/marketing-webapp-funnel, stacked on feat/kolabing-web-app): kolabing.com had no link to app.kolabing.com at all — every acquisition CTA was a dead `href="#"` or an in-page anchor, and mobile had no header CTA behind an inert hamburger. All surfaces now funnel into /login + /register via `config('webapp.url')`, with role-prefilled `?type=` links where the audience is already known. Prior same day: **Web App rebuilt on the Claude-Design handoff** (branch feat/kolabing-web-app): the whole app.kolabing.com UI replaced with the `kolabing-web-design/` bundle — Anton/Inter self-hosted, cream+#FFE28C palette, 264px sidebar shell, Explore card grid + detail/apply overlays, 4-tab My Kolabs (offers/requests/active/finished), multi-step Create wizard, Profile, Plan, and a new **/notifications** page. Alpine is now self-hosted (`public/webapp-assets/`) instead of a CDN. Two real wiring bugs fixed while wiring it: list endpoints return rows at `data.data` (the old views read `data` and silently rendered empty lists) — now via `kb.rows()`; and the business paywall on **accept** surfaces as **403**, not 402, so unsubscribed businesses dead-ended — now routed to /subscription (documented in ROLES-BACKEND-DB-MAP §3). Accept date picker bounded to the Kolab window + recurring days. **Third bug: the global CSP had no `'unsafe-eval'`, so Alpine (which compiles every x-* expression with `new Function`) never initialised and the whole web app was inert** — `AddSecurityHeaders` now serves a per-host policy: the web-app host gets `'unsafe-eval'` + `accounts.google.com` (script/style/frame, needed by Google Sign-In, which the old CSP also blocked); marketing + /admin keep the strict baseline. Covered by two new tests. es/ca kept at 100%. Prior: 2026-08-15 (**Kolabing Web App consolidated** (epic #128, branch feat/kolabing-web-app): the whole web app on app.kolabing.com — web Google login (BE-NF-18/GOOGLE_CLIENT_ID_WEB), Stripe web checkout (BE-NF-17, source=stripe) + Billing Portal (BE-NF-19), the Blade+Alpine app foundation+auth+subscription (BE-NF-20), feed + Kolab CRUD (BE-NF-21), profile edit + applications (BE-NF-22), image uploads (BE-NF-23), and es/ca i18n + SEO locale URLs (BE-NF-24/25). One PR. Prior same day: BE-NF-16 blog engine + SEO/GEO built, branch feat/blog-engine-seo-geo: public /blog + admin /admin/blog CRUD + Article/Organization/WebSite/FAQPage JSON-LD + homepage meta/OG + sitemap/llms now include posts. Prior 2026-08-02: BE-FX-11 F1 host_profile_id on EventResource + BE-FX-8 chat Prior: 2026-08-19 (**BE-NF-34 deployed** — Community Hub is live on prod: migration ran, `/community/*` + `/c/{slug}` verified serving, queue worker draining. Deploy surfaced **BE-FX-18**: prod has no `POSTMARK_API_KEY`, so all outbound email — including the new invitation mail — fails. Env-only fix. Prior: 2026-08-19 (**Community members web panel built** (BE-NF-34, branch `feat/community-members-hub`): the NF-6 members & tiers backend had no human-reachable surface at all. Shipped the seven-tab Community Hub on app.kolabing.com plus the backend it needed — a searchable/filterable/sortable roster enriched with real engagement data, a stats endpoint, pending **email invitations** that convert on signup, member detail + bulk actions, and the public **`/c/{slug}` join page that had never existed even though every invite link pointed at it**. Four other live defects fixed on the way (removed members leaking into the roster; the roster ungated despite carrying emails; every member rendering as an email prefix; `kb.rows()` silently returning [] for keyed envelopes). Never paywalled — guarded by a test. es/ca 100%. Prior: 2026-08-19 (**BE-FX-17 — the marketing site's structured data was invalid everywhere**: Laravel 12's `@context` Blade directive silently replaced the `@context` key in every inline `{!! json_encode([...]) !!}` JSON-LD block, so Organization / WebSite / Product / FAQPage markup emitted compiled PHP instead and Google could not interpret any of it. Found while inspecting the new public profile page on production, which was unaffected because it encodes from a `@php` block. Fixed in four templates the same way, with a guard test that parses every ld+json block on ten marketing URLs. Prior same day: 2026-08-19 (**BE-NF-29 — public profile pages, in the panel and on the open web** (branch `feat/public-profiles`): businesses and communities had a rich public profile in the API that **no web surface ever rendered** — the panel had no profile route at all, and a shared Kolabing link led nowhere. Two pages now: `app.kolabing.com/profiles/{id}` shows the full detail (reviews with their authors + the five-category rating breakdown, past events with photos, past collaborations, gallery with a lightbox, contact links), reachable from Explore cards, the Kolab overlay, application rows, collaboration partners, notification actors, the chat header, the sidebar card and the account page. And `kolabing.com/p/{slug}` is a **public, indexable teaser** — identity, rating, three photos and one anonymised quote, with everything else (all reviews + who wrote them, contact links, past-event detail, partners) behind a sign-up wall that is enforced by *absence from the HTML*, not CSS. Slug is `name-<uuid tail>` because `profiles.handle` is only required for attendees (5 of 94 production profiles have one), so keying on it would 404 almost everywhere; old links survive a rename and full UUIDs still resolve. Backend: one additive endpoint `GET /profiles/{id}/public-profile` serving the rich shape for **both** roles — `kolabs.past_events` was always role-agnostic, only the community-scoped route made past events look community-only — plus additive `public_url` on that resource, `actor_profile_id` on notifications and `id` on chat participants so every client can link onward. Profiles with a completed collaboration are listed in `sitemap.xml`; `Organization`/`LocalBusiness` + `AggregateRating` JSON-LD, and the rating block is omitted rather than faked when there are no reviews. 29 new tests, most of them asserting what the public page must NOT contain. **Open decision:** the public page is opt-out-less — see BE-NF-30.) — Prior 2026-08-18 (**Reverb is already deployed — BE-IF-18's "MISSING" list was stale**: `.env` carries live credentials for Laravel Cloud's **managed** Reverb, so the self-hosted-daemon + nginx/TLS steps in that ticket do not apply. Verified by handshaking the real endpoint (accepts `Origin: https://app.kolabing.com`, `activity_timeout: 30`; a no-Origin connection is correctly rejected `4009`) and by running the whole loop — handshake → `/broadcasting/auth` → `pusher:subscribe` → `message.sent` — against a local `reverb:start` + `queue:work` on an isolated SQLite DB. The web panel therefore needs **no code change** for real-time; it lights up on deploy. One thing left to confirm: a **queue worker in production**, since `NewChatMessage` is `ShouldBroadcast` on a `database` queue and without a worker every broadcast is enqueued and never delivered. Docs corrected accordingly (they claimed the daemon was missing).) — Prior same day (**BE-NF-28 — chat integrated into the web panel** (branch `feat/webapp-chat`): app.kolabing.com had no chat at all — no route, no nav entry, not one call to the chat API — while the backend carried the whole feature (thread types, inbox, unread counts, bans, tier-gated channels) and only mobile used it. Added `/chats` (+ `/es`, `/ca`): a two-pane inbox (thread list + conversation, one route, no page load between threads), a `Messages` nav row with its own unread badge on every page, day-separated bubbles, community **channel management** (create ≤5 / rename / delete + per-member block list, owned communities only), and deep links `?thread=` / `?application=` / `?collaboration=` so My Kolabs rows and new-message notifications open the right conversation. Real-time is a **hand-written 250-line Pusher-protocol client** (`public/webapp-assets/kb-realtime.js`) instead of laravel-echo + pusher-js: the panel has no bundler and the CSP forbids CDNs. It stays dormant until BE-IF-18 hands over `REVERB_APP_KEY` and a 4s ticker polls in the meantime, so chat works either way. CSP gained `wss:` on the web-app host only (`connect-src https:` does NOT cover WebSockets). Three real backend gaps were found and filed rather than papered over: BE-FX-13 (silent Kolab messages), BE-FX-14 (§2.8 lapse re-gate not enforced for chat), BE-FX-15 (`/me/communities` hides delegated communities). Verified by replaying every call the page makes against a running server on an isolated SQLite copy (the local `.env` points at the production DB, so nothing was written there) — which caught a real bug in the block list: the member roster pages on `limit`, not `per_page`, and nests rows under `data.members`, where `kb.rows()` cannot find them, so the list rendered empty. Fixed and pinned. `POST /broadcasting/auth` was confirmed to return a valid channel signature for a bearer token, so the auth half of real-time is proven; only the daemon is missing. 1506 tests pass (7088 assertions), pint clean. ROLES §4.1 + backend map §15.) — Prior 2026-08-18 (**BE-FX-12 — `GET /chats` 500 fixed**: the mobile chat inbox was dead in production (`ChatException(null: Server error)`); root cause was `latestOfMany()` aggregating `MAX(uuid)` on the `chat_messages` primary key, which Postgres has no function for, introduced with BE-FX-8's preview. Replaced with a plain `latest()` relation + `ChatService::attachLatestMessages()` (two grouped queries, no N+1). Reproduced AND verified read-only against the production DB; guard test added because the SQLite test suite structurally cannot catch Postgres-only SQL. Needs deploy — see Fixes BE-FX-12.) — Prior 2026-08-17 (**Web payment flow completed** (BE-NF-26, branch feat/kolabing-web-app): the web checkout existed but was not sellable — activation depended entirely on the Stripe webhook, so a business could pay, be shown "you're subscribed", and still be paywalled (and on prod, where the webhook is not yet registered, the sale would never activate at all). Added `POST /me/subscription/checkout/confirm` + a `/subscription/success` page that confirms the Checkout Session on return (ownership-checked; 409 `pending` retried with backoff; same idempotent activation the webhook uses), enabled **Stripe promotion codes** + email prefill + Stripe-customer reuse, replaced the buried quarterly text-link with two selectable plan cards (`Save 12%` derived from config), gave every paywall redirect a `?reason=` so the plan page names the blocked action, surfaced **`past_due`** as a shell alert → Billing Portal (additive `subscription_status` on `/auth/me`), and shipped a public **`kolabing.com/pricing` + `/es/pricing`** with Product/Offer/FAQPage JSON-LD. Also corrected a real doc/code drift: ROLES §2.1 claimed €39.99/mo, the code has always billed €49/€129 from `config/subscriptions.php`. 1486 tests pass, pint clean. ROLES §2.12 + backend map §14. Prior: **Marketing → web-app funnel wired** (BE-NF-27, branch feat/marketing-webapp-funnel, stacked on feat/kolabing-web-app): kolabing.com had no link to app.kolabing.com at all — every acquisition CTA was a dead `href="#"` or an in-page anchor, and mobile had no header CTA behind an inert hamburger. All surfaces now funnel into /login + /register via `config('webapp.url')`, with role-prefilled `?type=` links where the audience is already known. Prior same day: **Web App rebuilt on the Claude-Design handoff** (branch feat/kolabing-web-app): the whole app.kolabing.com UI replaced with the `kolabing-web-design/` bundle — Anton/Inter self-hosted, cream+#FFE28C palette, 264px sidebar shell, Explore card grid + detail/apply overlays, 4-tab My Kolabs (offers/requests/active/finished), multi-step Create wizard, Profile, Plan, and a new **/notifications** page. Alpine is now self-hosted (`public/webapp-assets/`) instead of a CDN. Two real wiring bugs fixed while wiring it: list endpoints return rows at `data.data` (the old views read `data` and silently rendered empty lists) — now via `kb.rows()`; and the business paywall on **accept** surfaces as **403**, not 402, so unsubscribed businesses dead-ended — now routed to /subscription (documented in ROLES-BACKEND-DB-MAP §3). Accept date picker bounded to the Kolab window + recurring days. **Third bug: the global CSP had no `'unsafe-eval'`, so Alpine (which compiles every x-* expression with `new Function`) never initialised and the whole web app was inert** — `AddSecurityHeaders` now serves a per-host policy: the web-app host gets `'unsafe-eval'` + `accounts.google.com` (script/style/frame, needed by Google Sign-In, which the old CSP also blocked); marketing + /admin keep the strict baseline. Covered by two new tests. es/ca kept at 100%. Prior: 2026-08-15 (**Kolabing Web App consolidated** (epic #128, branch feat/kolabing-web-app): the whole web app on app.kolabing.com — web Google login (BE-NF-18/GOOGLE_CLIENT_ID_WEB), Stripe web checkout (BE-NF-17, source=stripe) + Billing Portal (BE-NF-19), the Blade+Alpine app foundation+auth+subscription (BE-NF-20), feed + Kolab CRUD (BE-NF-21), profile edit + applications (BE-NF-22), image uploads (BE-NF-23), and es/ca i18n + SEO locale URLs (BE-NF-24/25). One PR. Prior same day: BE-NF-16 blog engine + SEO/GEO built, branch feat/blog-engine-seo-geo: public /blog + admin /admin/blog CRUD + Article/Organization/WebSite/FAQPage JSON-LD + homepage meta/OG + sitemap/llms now include posts. Prior 2026-08-02: BE-FX-11 F1 host_profile_id on EventResource + BE-FX-8 chat Prior: 2026-08-19 (**BE-FX-18 withdrawn — not a bug.** `POSTMARK_API_KEY` IS set on prod; the `failed_jobs` rows I read were historical (newest 07:27, none since — verified 13:15, spanning the BE-NF-34 deploy). BE-NF-34 is therefore **deployed and verified end-to-end**, invitation email included. Prior: 2026-08-19 (**BE-NF-34 deployed** — Community Hub is live on prod: migration ran, `/community/*` + `/c/{slug}` verified serving, queue worker draining. Deploy surfaced **BE-FX-18**: prod has no `POSTMARK_API_KEY`, so all outbound email — including the new invitation mail — fails. Env-only fix. Prior: 2026-08-19 (**Community members web panel built** (BE-NF-34, branch `feat/community-members-hub`): the NF-6 members & tiers backend had no human-reachable surface at all. Shipped the seven-tab Community Hub on app.kolabing.com plus the backend it needed — a searchable/filterable/sortable roster enriched with real engagement data, a stats endpoint, pending **email invitations** that convert on signup, member detail + bulk actions, and the public **`/c/{slug}` join page that had never existed even though every invite link pointed at it**. Four other live defects fixed on the way (removed members leaking into the roster; the roster ungated despite carrying emails; every member rendering as an email prefix; `kb.rows()` silently returning [] for keyed envelopes). Never paywalled — guarded by a test. es/ca 100%. Prior: 2026-08-19 (**BE-FX-17 — the marketing site's structured data was invalid everywhere**: Laravel 12's `@context` Blade directive silently replaced the `@context` key in every inline `{!! json_encode([...]) !!}` JSON-LD block, so Organization / WebSite / Product / FAQPage markup emitted compiled PHP instead and Google could not interpret any of it. Found while inspecting the new public profile page on production, which was unaffected because it encodes from a `@php` block. Fixed in four templates the same way, with a guard test that parses every ld+json block on ten marketing URLs. Prior same day: 2026-08-19 (**BE-NF-29 — public profile pages, in the panel and on the open web** (branch `feat/public-profiles`): businesses and communities had a rich public profile in the API that **no web surface ever rendered** — the panel had no profile route at all, and a shared Kolabing link led nowhere. Two pages now: `app.kolabing.com/profiles/{id}` shows the full detail (reviews with their authors + the five-category rating breakdown, past events with photos, past collaborations, gallery with a lightbox, contact links), reachable from Explore cards, the Kolab overlay, application rows, collaboration partners, notification actors, the chat header, the sidebar card and the account page. And `kolabing.com/p/{slug}` is a **public, indexable teaser** — identity, rating, three photos and one anonymised quote, with everything else (all reviews + who wrote them, contact links, past-event detail, partners) behind a sign-up wall that is enforced by *absence from the HTML*, not CSS. Slug is `name-<uuid tail>` because `profiles.handle` is only required for attendees (5 of 94 production profiles have one), so keying on it would 404 almost everywhere; old links survive a rename and full UUIDs still resolve. Backend: one additive endpoint `GET /profiles/{id}/public-profile` serving the rich shape for **both** roles — `kolabs.past_events` was always role-agnostic, only the community-scoped route made past events look community-only — plus additive `public_url` on that resource, `actor_profile_id` on notifications and `id` on chat participants so every client can link onward. Profiles with a completed collaboration are listed in `sitemap.xml`; `Organization`/`LocalBusiness` + `AggregateRating` JSON-LD, and the rating block is omitted rather than faked when there are no reviews. 29 new tests, most of them asserting what the public page must NOT contain. **Open decision:** the public page is opt-out-less — see BE-NF-30.) — Prior 2026-08-18 (**Reverb is already deployed — BE-IF-18's "MISSING" list was stale**: `.env` carries live credentials for Laravel Cloud's **managed** Reverb, so the self-hosted-daemon + nginx/TLS steps in that ticket do not apply. Verified by handshaking the real endpoint (accepts `Origin: https://app.kolabing.com`, `activity_timeout: 30`; a no-Origin connection is correctly rejected `4009`) and by running the whole loop — handshake → `/broadcasting/auth` → `pusher:subscribe` → `message.sent` — against a local `reverb:start` + `queue:work` on an isolated SQLite DB. The web panel therefore needs **no code change** for real-time; it lights up on deploy. One thing left to confirm: a **queue worker in production**, since `NewChatMessage` is `ShouldBroadcast` on a `database` queue and without a worker every broadcast is enqueued and never delivered. Docs corrected accordingly (they claimed the daemon was missing).) — Prior same day (**BE-NF-28 — chat integrated into the web panel** (branch `feat/webapp-chat`): app.kolabing.com had no chat at all — no route, no nav entry, not one call to the chat API — while the backend carried the whole feature (thread types, inbox, unread counts, bans, tier-gated channels) and only mobile used it. Added `/chats` (+ `/es`, `/ca`): a two-pane inbox (thread list + conversation, one route, no page load between threads), a `Messages` nav row with its own unread badge on every page, day-separated bubbles, community **channel management** (create ≤5 / rename / delete + per-member block list, owned communities only), and deep links `?thread=` / `?application=` / `?collaboration=` so My Kolabs rows and new-message notifications open the right conversation. Real-time is a **hand-written 250-line Pusher-protocol client** (`public/webapp-assets/kb-realtime.js`) instead of laravel-echo + pusher-js: the panel has no bundler and the CSP forbids CDNs. It stays dormant until BE-IF-18 hands over `REVERB_APP_KEY` and a 4s ticker polls in the meantime, so chat works either way. CSP gained `wss:` on the web-app host only (`connect-src https:` does NOT cover WebSockets). Three real backend gaps were found and filed rather than papered over: BE-FX-13 (silent Kolab messages), BE-FX-14 (§2.8 lapse re-gate not enforced for chat), BE-FX-15 (`/me/communities` hides delegated communities). Verified by replaying every call the page makes against a running server on an isolated SQLite copy (the local `.env` points at the production DB, so nothing was written there) — which caught a real bug in the block list: the member roster pages on `limit`, not `per_page`, and nests rows under `data.members`, where `kb.rows()` cannot find them, so the list rendered empty. Fixed and pinned. `POST /broadcasting/auth` was confirmed to return a valid channel signature for a bearer token, so the auth half of real-time is proven; only the daemon is missing. 1506 tests pass (7088 assertions), pint clean. ROLES §4.1 + backend map §15.) — Prior 2026-08-18 (**BE-FX-12 — `GET /chats` 500 fixed**: the mobile chat inbox was dead in production (`ChatException(null: Server error)`); root cause was `latestOfMany()` aggregating `MAX(uuid)` on the `chat_messages` primary key, which Postgres has no function for, introduced with BE-FX-8's preview. Replaced with a plain `latest()` relation + `ChatService::attachLatestMessages()` (two grouped queries, no N+1). Reproduced AND verified read-only against the production DB; guard test added because the SQLite test suite structurally cannot catch Postgres-only SQL. Needs deploy — see Fixes BE-FX-12.) — Prior 2026-08-17 (**Web payment flow completed** (BE-NF-26, branch feat/kolabing-web-app): the web checkout existed but was not sellable — activation depended entirely on the Stripe webhook, so a business could pay, be shown "you're subscribed", and still be paywalled (and on prod, where the webhook is not yet registered, the sale would never activate at all). Added `POST /me/subscription/checkout/confirm` + a `/subscription/success` page that confirms the Checkout Session on return (ownership-checked; 409 `pending` retried with backoff; same idempotent activation the webhook uses), enabled **Stripe promotion codes** + email prefill + Stripe-customer reuse, replaced the buried quarterly text-link with two selectable plan cards (`Save 12%` derived from config), gave every paywall redirect a `?reason=` so the plan page names the blocked action, surfaced **`past_due`** as a shell alert → Billing Portal (additive `subscription_status` on `/auth/me`), and shipped a public **`kolabing.com/pricing` + `/es/pricing`** with Product/Offer/FAQPage JSON-LD. Also corrected a real doc/code drift: ROLES §2.1 claimed €39.99/mo, the code has always billed €49/€129 from `config/subscriptions.php`. 1486 tests pass, pint clean. ROLES §2.12 + backend map §14. Prior: **Marketing → web-app funnel wired** (BE-NF-27, branch feat/marketing-webapp-funnel, stacked on feat/kolabing-web-app): kolabing.com had no link to app.kolabing.com at all — every acquisition CTA was a dead `href="#"` or an in-page anchor, and mobile had no header CTA behind an inert hamburger. All surfaces now funnel into /login + /register via `config('webapp.url')`, with role-prefilled `?type=` links where the audience is already known. Prior same day: **Web App rebuilt on the Claude-Design handoff** (branch feat/kolabing-web-app): the whole app.kolabing.com UI replaced with the `kolabing-web-design/` bundle — Anton/Inter self-hosted, cream+#FFE28C palette, 264px sidebar shell, Explore card grid + detail/apply overlays, 4-tab My Kolabs (offers/requests/active/finished), multi-step Create wizard, Profile, Plan, and a new **/notifications** page. Alpine is now self-hosted (`public/webapp-assets/`) instead of a CDN. Two real wiring bugs fixed while wiring it: list endpoints return rows at `data.data` (the old views read `data` and silently rendered empty lists) — now via `kb.rows()`; and the business paywall on **accept** surfaces as **403**, not 402, so unsubscribed businesses dead-ended — now routed to /subscription (documented in ROLES-BACKEND-DB-MAP §3). Accept date picker bounded to the Kolab window + recurring days. **Third bug: the global CSP had no `'unsafe-eval'`, so Alpine (which compiles every x-* expression with `new Function`) never initialised and the whole web app was inert** — `AddSecurityHeaders` now serves a per-host policy: the web-app host gets `'unsafe-eval'` + `accounts.google.com` (script/style/frame, needed by Google Sign-In, which the old CSP also blocked); marketing + /admin keep the strict baseline. Covered by two new tests. es/ca kept at 100%. Prior: 2026-08-15 (**Kolabing Web App consolidated** (epic #128, branch feat/kolabing-web-app): the whole web app on app.kolabing.com — web Google login (BE-NF-18/GOOGLE_CLIENT_ID_WEB), Stripe web checkout (BE-NF-17, source=stripe) + Billing Portal (BE-NF-19), the Blade+Alpine app foundation+auth+subscription (BE-NF-20), feed + Kolab CRUD (BE-NF-21), profile edit + applications (BE-NF-22), image uploads (BE-NF-23), and es/ca i18n + SEO locale URLs (BE-NF-24/25). One PR. Prior same day: BE-NF-16 blog engine + SEO/GEO built, branch feat/blog-engine-seo-geo: public /blog + admin /admin/blog CRUD + Article/Organization/WebSite/FAQPage JSON-LD + homepage meta/OG + sitemap/llms now include posts. Prior 2026-08-02: BE-FX-11 F1 host_profile_id on EventResource + BE-FX-8 chat — Also 2026-08-22 (**three chat / discovery defects fixed** on branch `fix/chat-notify-and-managed-communities`: **BE-FX-13** — a Kolab message posted to the generic thread endpoint notified nobody, because `threadRecipientIds()` returns `[]` for collaboration threads and only the application path called `notifyNewMessage()`; `sendThreadMessage()` now delegates to that one path, so both routes notify identically and exactly once (proven with global counts, not `>= 1`), and an `InvalidArgumentException` that would have surfaced as a 500 is now a 403. **BE-FX-15** — `GET /me/communities` hid communities a profile co-runs; new `Community::scopeManageableBy()` (the query form of `CommunityPolicy::manage()`) plus an additive `my_can_manage` discriminator, no N+1. **BE-FX-23** (filed and fixed together; it was numbered BE-FX-20 at merge time, colliding with the collaboration-QR entry already on master) — `EventDiscoveryService` ran different distance maths on SQLite and Postgres, so CI never executed the production path; behind that sat a live Postgres 500 for a zero-distance event, pagination applied before filtering, and a float radius bound as a string. One portable SQL expression now runs on both. 1955 tests pass, pint clean.) — Prior 2026-08-20 (**BE-NF-38 — the invite page never worked, now fixed and given a join form**: its CTA sat in `<template x-if>` blocks on a layout with no Alpine, and the CSP grants 'unsafe-eval' only to the app host, so nothing was ever painted. Moved the page to the app host rather than weakening a security header; added Google sign-up plus a name/phone/photo form so an invitee joins in place. The regression test now asserts the page can RUN its Alpine, not just that the markup is there. Prior: 2026-08-20 (**BE-NF-37 phone preview built**: a read-only replica of the mobile app's profile screen in a device frame beside every Profile-section tab, refreshed after each edit — so reordering a gallery shows immediately which photo leads on a phone. Mirrors three named Dart files in kolabing-app; the pairing is recorded in backend map §17.5. No API, schema or role change. Prior: 2026-08-20 (**BE-NF-36 public portfolio panel built**: business and community accounts can finally manage the portfolio their public profile renders — gallery, past events, per-event photos — from a tabbed Profile section, with a Preview of their own public page. The find that shaped it: 'past events' lives in two unrelated stores and the profile read only the smaller one, so **60 past events and 173 photographs were invisible**; both are merged now. Also fixed: the gallery had `caption` and `sort_order` columns no endpoint ever wrote. No migration. Prior: 2026-08-19 (**SEO denetimi + düzeltmeleri**: kolabing.com canlı olarak denetlendi (16 sayfa) ve bulunan sekiz kod hatası aynı gün düzeltildi — BE-FX-19. En büyüğü ana sayfanın **24,58 MB görsel** yükü (→ 860 KB WebP, %96,5); ayrıca `www` ikinci bir kanonik site olarak yayındaydı, ana sayfa hiçbir ticari sayfaya link vermiyordu, pazarlama HTML'i hiç önbelleklenmiyordu, boş `/blog` ve `/communities` indekslenebilir ince sayfalardı ve sitemap eşiği bir seed test hesabını indekse sokuyordu. Denetimin kod dışı kalan üç kalemi ayrı iş olarak açıldı: BE-NF-31 (Tailwind CDN → build, görsel doğrulama gerektiriyor), BE-NF-32 (blog içeriği), BE-NF-33 (İspanyolca sayfalar). Prior: 2026-08-19 (**BE-FX-17 — the marketing site's structured data was invalid everywhere**: Laravel 12's `@context` Blade directive silently replaced the `@context` key in every inline `{!! json_encode([...]) !!}` JSON-LD block, so Organization / WebSite / Product / FAQPage markup emitted compiled PHP instead and Google could not interpret any of it. Found while inspecting the new public profile page on production, which was unaffected because it encodes from a `@php` block. Fixed in four templates the same way, with a guard test that parses every ld+json block on ten marketing URLs. Prior same day: 2026-08-19 (**BE-NF-29 — public profile pages, in the panel and on the open web** (branch `feat/public-profiles`): businesses and communities had a rich public profile in the API that **no web surface ever rendered** — the panel had no profile route at all, and a shared Kolabing link led nowhere. Two pages now: `app.kolabing.com/profiles/{id}` shows the full detail (reviews with their authors + the five-category rating breakdown, past events with photos, past collaborations, gallery with a lightbox, contact links), reachable from Explore cards, the Kolab overlay, application rows, collaboration partners, notification actors, the chat header, the sidebar card and the account page. And `kolabing.com/p/{slug}` is a **public, indexable teaser** — identity, rating, three photos and one anonymised quote, with everything else (all reviews + who wrote them, contact links, past-event detail, partners) behind a sign-up wall that is enforced by *absence from the HTML*, not CSS. Slug is `name-<uuid tail>` because `profiles.handle` is only required for attendees (5 of 94 production profiles have one), so keying on it would 404 almost everywhere; old links survive a rename and full UUIDs still resolve. Backend: one additive endpoint `GET /profiles/{id}/public-profile` serving the rich shape for **both** roles — `kolabs.past_events` was always role-agnostic, only the community-scoped route made past events look community-only — plus additive `public_url` on that resource, `actor_profile_id` on notifications and `id` on chat participants so every client can link onward. Profiles with a completed collaboration are listed in `sitemap.xml`; `Organization`/`LocalBusiness` + `AggregateRating` JSON-LD, and the rating block is omitted rather than faked when there are no reviews. 29 new tests, most of them asserting what the public page must NOT contain. **Open decision:** the public page is opt-out-less — see BE-NF-30.) — Prior 2026-08-18 (**Reverb is already deployed — BE-IF-18's "MISSING" list was stale**: `.env` carries live credentials for Laravel Cloud's **managed** Reverb, so the self-hosted-daemon + nginx/TLS steps in that ticket do not apply. Verified by handshaking the real endpoint (accepts `Origin: https://app.kolabing.com`, `activity_timeout: 30`; a no-Origin connection is correctly rejected `4009`) and by running the whole loop — handshake → `/broadcasting/auth` → `pusher:subscribe` → `message.sent` — against a local `reverb:start` + `queue:work` on an isolated SQLite DB. The web panel therefore needs **no code change** for real-time; it lights up on deploy. One thing left to confirm: a **queue worker in production**, since `NewChatMessage` is `ShouldBroadcast` on a `database` queue and without a worker every broadcast is enqueued and never delivered. Docs corrected accordingly (they claimed the daemon was missing).) — Prior same day (**BE-NF-28 — chat integrated into the web panel** (branch `feat/webapp-chat`): app.kolabing.com had no chat at all — no route, no nav entry, not one call to the chat API — while the backend carried the whole feature (thread types, inbox, unread counts, bans, tier-gated channels) and only mobile used it. Added `/chats` (+ `/es`, `/ca`): a two-pane inbox (thread list + conversation, one route, no page load between threads), a `Messages` nav row with its own unread badge on every page, day-separated bubbles, community **channel management** (create ≤5 / rename / delete + per-member block list, owned communities only), and deep links `?thread=` / `?application=` / `?collaboration=` so My Kolabs rows and new-message notifications open the right conversation. Real-time is a **hand-written 250-line Pusher-protocol client** (`public/webapp-assets/kb-realtime.js`) instead of laravel-echo + pusher-js: the panel has no bundler and the CSP forbids CDNs. It stays dormant until BE-IF-18 hands over `REVERB_APP_KEY` and a 4s ticker polls in the meantime, so chat works either way. CSP gained `wss:` on the web-app host only (`connect-src https:` does NOT cover WebSockets). Three real backend gaps were found and filed rather than papered over: BE-FX-13 (silent Kolab messages), BE-FX-14 (§2.8 lapse re-gate not enforced for chat), BE-FX-15 (`/me/communities` hides delegated communities). Verified by replaying every call the page makes against a running server on an isolated SQLite copy (the local `.env` points at the production DB, so nothing was written there) — which caught a real bug in the block list: the member roster pages on `limit`, not `per_page`, and nests rows under `data.members`, where `kb.rows()` cannot find them, so the list rendered empty. Fixed and pinned. `POST /broadcasting/auth` was confirmed to return a valid channel signature for a bearer token, so the auth half of real-time is proven; only the daemon is missing. 1506 tests pass (7088 assertions), pint clean. ROLES §4.1 + backend map §15.) — Prior 2026-08-18 (**BE-FX-12 — `GET /chats` 500 fixed**: the mobile chat inbox was dead in production (`ChatException(null: Server error)`); root cause was `latestOfMany()` aggregating `MAX(uuid)` on the `chat_messages` primary key, which Postgres has no function for, introduced with BE-FX-8's preview. Replaced with a plain `latest()` relation + `ChatService::attachLatestMessages()` (two grouped queries, no N+1). Reproduced AND verified read-only against the production DB; guard test added because the SQLite test suite structurally cannot catch Postgres-only SQL. Needs deploy — see Fixes BE-FX-12.) — Prior 2026-08-17 (**Web payment flow completed** (BE-NF-26, branch feat/kolabing-web-app): the web checkout existed but was not sellable — activation depended entirely on the Stripe webhook, so a business could pay, be shown "you're subscribed", and still be paywalled (and on prod, where the webhook is not yet registered, the sale would never activate at all). Added `POST /me/subscription/checkout/confirm` + a `/subscription/success` page that confirms the Checkout Session on return (ownership-checked; 409 `pending` retried with backoff; same idempotent activation the webhook uses), enabled **Stripe promotion codes** + email prefill + Stripe-customer reuse, replaced the buried quarterly text-link with two selectable plan cards (`Save 12%` derived from config), gave every paywall redirect a `?reason=` so the plan page names the blocked action, surfaced **`past_due`** as a shell alert → Billing Portal (additive `subscription_status` on `/auth/me`), and shipped a public **`kolabing.com/pricing` + `/es/pricing`** with Product/Offer/FAQPage JSON-LD. Also corrected a real doc/code drift: ROLES §2.1 claimed €39.99/mo, the code has always billed €49/€129 from `config/subscriptions.php`. 1486 tests pass, pint clean. ROLES §2.12 + backend map §14. Prior: **Marketing → web-app funnel wired** (BE-NF-27, branch feat/marketing-webapp-funnel, stacked on feat/kolabing-web-app): kolabing.com had no link to app.kolabing.com at all — every acquisition CTA was a dead `href="#"` or an in-page anchor, and mobile had no header CTA behind an inert hamburger. All surfaces now funnel into /login + /register via `config('webapp.url')`, with role-prefilled `?type=` links where the audience is already known. Prior same day: **Web App rebuilt on the Claude-Design handoff** (branch feat/kolabing-web-app): the whole app.kolabing.com UI replaced with the `kolabing-web-design/` bundle — Anton/Inter self-hosted, cream+#FFE28C palette, 264px sidebar shell, Explore card grid + detail/apply overlays, 4-tab My Kolabs (offers/requests/active/finished), multi-step Create wizard, Profile, Plan, and a new **/notifications** page. Alpine is now self-hosted (`public/webapp-assets/`) instead of a CDN. Two real wiring bugs fixed while wiring it: list endpoints return rows at `data.data` (the old views read `data` and silently rendered empty lists) — now via `kb.rows()`; and the business paywall on **accept** surfaces as **403**, not 402, so unsubscribed businesses dead-ended — now routed to /subscription (documented in ROLES-BACKEND-DB-MAP §3). Accept date picker bounded to the Kolab window + recurring days. **Third bug: the global CSP had no `'unsafe-eval'`, so Alpine (which compiles every x-* expression with `new Function`) never initialised and the whole web app was inert** — `AddSecurityHeaders` now serves a per-host policy: the web-app host gets `'unsafe-eval'` + `accounts.google.com` (script/style/frame, needed by Google Sign-In, which the old CSP also blocked); marketing + /admin keep the strict baseline. Covered by two new tests. es/ca kept at 100%. Prior: 2026-08-15 (**Kolabing Web App consolidated** (epic #128, branch feat/kolabing-web-app): the whole web app on app.kolabing.com — web Google login (BE-NF-18/GOOGLE_CLIENT_ID_WEB), Stripe web checkout (BE-NF-17, source=stripe) + Billing Portal (BE-NF-19), the Blade+Alpine app foundation+auth+subscription (BE-NF-20), feed + Kolab CRUD (BE-NF-21), profile edit + applications (BE-NF-22), image uploads (BE-NF-23), and es/ca i18n + SEO locale URLs (BE-NF-24/25). One PR. Prior same day: BE-NF-16 blog engine + SEO/GEO built, branch feat/blog-engine-seo-geo: public /blog + admin /admin/blog CRUD + Article/Organization/WebSite/FAQPage JSON-LD + homepage meta/OG + sitemap/llms now include posts. Prior 2026-08-02: BE-FX-11 F1 host_profile_id on EventResource + BE-FX-8 chat Prior: 2026-08-19 (**Community members web panel built** (BE-NF-34, branch `feat/community-members-hub`): the NF-6 members & tiers backend had no human-reachable surface at all. Shipped the seven-tab Community Hub on app.kolabing.com plus the backend it needed — a searchable/filterable/sortable roster enriched with real engagement data, a stats endpoint, pending **email invitations** that convert on signup, member detail + bulk actions, and the public **`/c/{slug}` join page that had never existed even though every invite link pointed at it**. Four other live defects fixed on the way (removed members leaking into the roster; the roster ungated despite carrying emails; every member rendering as an email prefix; `kb.rows()` silently returning [] for keyed envelopes). Never paywalled — guarded by a test. es/ca 100%. Prior: 2026-08-19 (**BE-FX-17 — the marketing site's structured data was invalid everywhere**: Laravel 12's `@context` Blade directive silently replaced the `@context` key in every inline `{!! json_encode([...]) !!}` JSON-LD block, so Organization / WebSite / Product / FAQPage markup emitted compiled PHP instead and Google could not interpret any of it. Found while inspecting the new public profile page on production, which was unaffected because it encodes from a `@php` block. Fixed in four templates the same way, with a guard test that parses every ld+json block on ten marketing URLs. Prior same day: 2026-08-19 (**BE-NF-29 — public profile pages, in the panel and on the open web** (branch `feat/public-profiles`): businesses and communities had a rich public profile in the API that **no web surface ever rendered** — the panel had no profile route at all, and a shared Kolabing link led nowhere. Two pages now: `app.kolabing.com/profiles/{id}` shows the full detail (reviews with their authors + the five-category rating breakdown, past events with photos, past collaborations, gallery with a lightbox, contact links), reachable from Explore cards, the Kolab overlay, application rows, collaboration partners, notification actors, the chat header, the sidebar card and the account page. And `kolabing.com/p/{slug}` is a **public, indexable teaser** — identity, rating, three photos and one anonymised quote, with everything else (all reviews + who wrote them, contact links, past-event detail, partners) behind a sign-up wall that is enforced by *absence from the HTML*, not CSS. Slug is `name-<uuid tail>` because `profiles.handle` is only required for attendees (5 of 94 production profiles have one), so keying on it would 404 almost everywhere; old links survive a rename and full UUIDs still resolve. Backend: one additive endpoint `GET /profiles/{id}/public-profile` serving the rich shape for **both** roles — `kolabs.past_events` was always role-agnostic, only the community-scoped route made past events look community-only — plus additive `public_url` on that resource, `actor_profile_id` on notifications and `id` on chat participants so every client can link onward. Profiles with a completed collaboration are listed in `sitemap.xml`; `Organization`/`LocalBusiness` + `AggregateRating` JSON-LD, and the rating block is omitted rather than faked when there are no reviews. 29 new tests, most of them asserting what the public page must NOT contain. **Open decision:** the public page is opt-out-less — see BE-NF-30.) — Prior 2026-08-18 (**Reverb is already deployed — BE-IF-18's "MISSING" list was stale**: `.env` carries live credentials for Laravel Cloud's **managed** Reverb, so the self-hosted-daemon + nginx/TLS steps in that ticket do not apply. Verified by handshaking the real endpoint (accepts `Origin: https://app.kolabing.com`, `activity_timeout: 30`; a no-Origin connection is correctly rejected `4009`) and by running the whole loop — handshake → `/broadcasting/auth` → `pusher:subscribe` → `message.sent` — against a local `reverb:start` + `queue:work` on an isolated SQLite DB. The web panel therefore needs **no code change** for real-time; it lights up on deploy. One thing left to confirm: a **queue worker in production**, since `NewChatMessage` is `ShouldBroadcast` on a `database` queue and without a worker every broadcast is enqueued and never delivered. Docs corrected accordingly (they claimed the daemon was missing).) — Prior same day (**BE-NF-28 — chat integrated into the web panel** (branch `feat/webapp-chat`): app.kolabing.com had no chat at all — no route, no nav entry, not one call to the chat API — while the backend carried the whole feature (thread types, inbox, unread counts, bans, tier-gated channels) and only mobile used it. Added `/chats` (+ `/es`, `/ca`): a two-pane inbox (thread list + conversation, one route, no page load between threads), a `Messages` nav row with its own unread badge on every page, day-separated bubbles, community **channel management** (create ≤5 / rename / delete + per-member block list, owned communities only), and deep links `?thread=` / `?application=` / `?collaboration=` so My Kolabs rows and new-message notifications open the right conversation. Real-time is a **hand-written 250-line Pusher-protocol client** (`public/webapp-assets/kb-realtime.js`) instead of laravel-echo + pusher-js: the panel has no bundler and the CSP forbids CDNs. It stays dormant until BE-IF-18 hands over `REVERB_APP_KEY` and a 4s ticker polls in the meantime, so chat works either way. CSP gained `wss:` on the web-app host only (`connect-src https:` does NOT cover WebSockets). Three real backend gaps were found and filed rather than papered over: BE-FX-13 (silent Kolab messages), BE-FX-14 (§2.8 lapse re-gate not enforced for chat), BE-FX-15 (`/me/communities` hides delegated communities). Verified by replaying every call the page makes against a running server on an isolated SQLite copy (the local `.env` points at the production DB, so nothing was written there) — which caught a real bug in the block list: the member roster pages on `limit`, not `per_page`, and nests rows under `data.members`, where `kb.rows()` cannot find them, so the list rendered empty. Fixed and pinned. `POST /broadcasting/auth` was confirmed to return a valid channel signature for a bearer token, so the auth half of real-time is proven; only the daemon is missing. 1506 tests pass (7088 assertions), pint clean. ROLES §4.1 + backend map §15.) — Prior 2026-08-18 (**BE-FX-12 — `GET /chats` 500 fixed**: the mobile chat inbox was dead in production (`ChatException(null: Server error)`); root cause was `latestOfMany()` aggregating `MAX(uuid)` on the `chat_messages` primary key, which Postgres has no function for, introduced with BE-FX-8's preview. Replaced with a plain `latest()` relation + `ChatService::attachLatestMessages()` (two grouped queries, no N+1). Reproduced AND verified read-only against the production DB; guard test added because the SQLite test suite structurally cannot catch Postgres-only SQL. Needs deploy — see Fixes BE-FX-12.) — Prior 2026-08-17 (**Web payment flow completed** (BE-NF-26, branch feat/kolabing-web-app): the web checkout existed but was not sellable — activation depended entirely on the Stripe webhook, so a business could pay, be shown "you're subscribed", and still be paywalled (and on prod, where the webhook is not yet registered, the sale would never activate at all). Added `POST /me/subscription/checkout/confirm` + a `/subscription/success` page that confirms the Checkout Session on return (ownership-checked; 409 `pending` retried with backoff; same idempotent activation the webhook uses), enabled **Stripe promotion codes** + email prefill + Stripe-customer reuse, replaced the buried quarterly text-link with two selectable plan cards (`Save 12%` derived from config), gave every paywall redirect a `?reason=` so the plan page names the blocked action, surfaced **`past_due`** as a shell alert → Billing Portal (additive `subscription_status` on `/auth/me`), and shipped a public **`kolabing.com/pricing` + `/es/pricing`** with Product/Offer/FAQPage JSON-LD. Also corrected a real doc/code drift: ROLES §2.1 claimed €39.99/mo, the code has always billed €49/€129 from `config/subscriptions.php`. 1486 tests pass, pint clean. ROLES §2.12 + backend map §14. Prior: **Marketing → web-app funnel wired** (BE-NF-27, branch feat/marketing-webapp-funnel, stacked on feat/kolabing-web-app): kolabing.com had no link to app.kolabing.com at all — every acquisition CTA was a dead `href="#"` or an in-page anchor, and mobile had no header CTA behind an inert hamburger. All surfaces now funnel into /login + /register via `config('webapp.url')`, with role-prefilled `?type=` links where the audience is already known. Prior same day: **Web App rebuilt on the Claude-Design handoff** (branch feat/kolabing-web-app): the whole app.kolabing.com UI replaced with the `kolabing-web-design/` bundle — Anton/Inter self-hosted, cream+#FFE28C palette, 264px sidebar shell, Explore card grid + detail/apply overlays, 4-tab My Kolabs (offers/requests/active/finished), multi-step Create wizard, Profile, Plan, and a new **/notifications** page. Alpine is now self-hosted (`public/webapp-assets/`) instead of a CDN. Two real wiring bugs fixed while wiring it: list endpoints return rows at `data.data` (the old views read `data` and silently rendered empty lists) — now via `kb.rows()`; and the business paywall on **accept** surfaces as **403**, not 402, so unsubscribed businesses dead-ended — now routed to /subscription (documented in ROLES-BACKEND-DB-MAP §3). Accept date picker bounded to the Kolab window + recurring days. **Third bug: the global CSP had no `'unsafe-eval'`, so Alpine (which compiles every x-* expression with `new Function`) never initialised and the whole web app was inert** — `AddSecurityHeaders` now serves a per-host policy: the web-app host gets `'unsafe-eval'` + `accounts.google.com` (script/style/frame, needed by Google Sign-In, which the old CSP also blocked); marketing + /admin keep the strict baseline. Covered by two new tests. es/ca kept at 100%. Prior: 2026-08-15 (**Kolabing Web App consolidated** (epic #128, branch feat/kolabing-web-app): the whole web app on app.kolabing.com — web Google login (BE-NF-18/GOOGLE_CLIENT_ID_WEB), Stripe web checkout (BE-NF-17, source=stripe) + Billing Portal (BE-NF-19), the Blade+Alpine app foundation+auth+subscription (BE-NF-20), feed + Kolab CRUD (BE-NF-21), profile edit + applications (BE-NF-22), image uploads (BE-NF-23), and es/ca i18n + SEO locale URLs (BE-NF-24/25). One PR. Prior same day: BE-NF-16 blog engine + SEO/GEO built, branch feat/blog-engine-seo-geo: public /blog + admin /admin/blog CRUD + Article/Organization/WebSite/FAQPage JSON-LD + homepage meta/OG + sitemap/llms now include posts. Prior 2026-08-02: BE-FX-11 F1 host_profile_id on EventResource + BE-FX-8 chat Prior: 2026-08-19 (**BE-NF-34 deployed** — Community Hub is live on prod: migration ran, `/community/*` + `/c/{slug}` verified serving, queue worker draining. Deploy surfaced **BE-FX-18**: prod has no `POSTMARK_API_KEY`, so all outbound email — including the new invitation mail — fails. Env-only fix. Prior: 2026-08-19 (**Community members web panel built** (BE-NF-34, branch `feat/community-members-hub`): the NF-6 members & tiers backend had no human-reachable surface at all. Shipped the seven-tab Community Hub on app.kolabing.com plus the backend it needed — a searchable/filterable/sortable roster enriched with real engagement data, a stats endpoint, pending **email invitations** that convert on signup, member detail + bulk actions, and the public **`/c/{slug}` join page that had never existed even though every invite link pointed at it**. Four other live defects fixed on the way (removed members leaking into the roster; the roster ungated despite carrying emails; every member rendering as an email prefix; `kb.rows()` silently returning [] for keyed envelopes). Never paywalled — guarded by a test. es/ca 100%. Prior: 2026-08-19 (**BE-FX-17 — the marketing site's structured data was invalid everywhere**: Laravel 12's `@context` Blade directive silently replaced the `@context` key in every inline `{!! json_encode([...]) !!}` JSON-LD block, so Organization / WebSite / Product / FAQPage markup emitted compiled PHP instead and Google could not interpret any of it. Found while inspecting the new public profile page on production, which was unaffected because it encodes from a `@php` block. Fixed in four templates the same way, with a guard test that parses every ld+json block on ten marketing URLs. Prior same day: 2026-08-19 (**BE-NF-29 — public profile pages, in the panel and on the open web** (branch `feat/public-profiles`): businesses and communities had a rich public profile in the API that **no web surface ever rendered** — the panel had no profile route at all, and a shared Kolabing link led nowhere. Two pages now: `app.kolabing.com/profiles/{id}` shows the full detail (reviews with their authors + the five-category rating breakdown, past events with photos, past collaborations, gallery with a lightbox, contact links), reachable from Explore cards, the Kolab overlay, application rows, collaboration partners, notification actors, the chat header, the sidebar card and the account page. And `kolabing.com/p/{slug}` is a **public, indexable teaser** — identity, rating, three photos and one anonymised quote, with everything else (all reviews + who wrote them, contact links, past-event detail, partners) behind a sign-up wall that is enforced by *absence from the HTML*, not CSS. Slug is `name-<uuid tail>` because `profiles.handle` is only required for attendees (5 of 94 production profiles have one), so keying on it would 404 almost everywhere; old links survive a rename and full UUIDs still resolve. Backend: one additive endpoint `GET /profiles/{id}/public-profile` serving the rich shape for **both** roles — `kolabs.past_events` was always role-agnostic, only the community-scoped route made past events look community-only — plus additive `public_url` on that resource, `actor_profile_id` on notifications and `id` on chat participants so every client can link onward. Profiles with a completed collaboration are listed in `sitemap.xml`; `Organization`/`LocalBusiness` + `AggregateRating` JSON-LD, and the rating block is omitted rather than faked when there are no reviews. 29 new tests, most of them asserting what the public page must NOT contain. **Open decision:** the public page is opt-out-less — see BE-NF-30.) — Prior 2026-08-18 (**Reverb is already deployed — BE-IF-18's "MISSING" list was stale**: `.env` carries live credentials for Laravel Cloud's **managed** Reverb, so the self-hosted-daemon + nginx/TLS steps in that ticket do not apply. Verified by handshaking the real endpoint (accepts `Origin: https://app.kolabing.com`, `activity_timeout: 30`; a no-Origin connection is correctly rejected `4009`) and by running the whole loop — handshake → `/broadcasting/auth` → `pusher:subscribe` → `message.sent` — against a local `reverb:start` + `queue:work` on an isolated SQLite DB. The web panel therefore needs **no code change** for real-time; it lights up on deploy. One thing left to confirm: a **queue worker in production**, since `NewChatMessage` is `ShouldBroadcast` on a `database` queue and without a worker every broadcast is enqueued and never delivered. Docs corrected accordingly (they claimed the daemon was missing).) — Prior same day (**BE-NF-28 — chat integrated into the web panel** (branch `feat/webapp-chat`): app.kolabing.com had no chat at all — no route, no nav entry, not one call to the chat API — while the backend carried the whole feature (thread types, inbox, unread counts, bans, tier-gated channels) and only mobile used it. Added `/chats` (+ `/es`, `/ca`): a two-pane inbox (thread list + conversation, one route, no page load between threads), a `Messages` nav row with its own unread badge on every page, day-separated bubbles, community **channel management** (create ≤5 / rename / delete + per-member block list, owned communities only), and deep links `?thread=` / `?application=` / `?collaboration=` so My Kolabs rows and new-message notifications open the right conversation. Real-time is a **hand-written 250-line Pusher-protocol client** (`public/webapp-assets/kb-realtime.js`) instead of laravel-echo + pusher-js: the panel has no bundler and the CSP forbids CDNs. It stays dormant until BE-IF-18 hands over `REVERB_APP_KEY` and a 4s ticker polls in the meantime, so chat works either way. CSP gained `wss:` on the web-app host only (`connect-src https:` does NOT cover WebSockets). Three real backend gaps were found and filed rather than papered over: BE-FX-13 (silent Kolab messages), BE-FX-14 (§2.8 lapse re-gate not enforced for chat), BE-FX-15 (`/me/communities` hides delegated communities). Verified by replaying every call the page makes against a running server on an isolated SQLite copy (the local `.env` points at the production DB, so nothing was written there) — which caught a real bug in the block list: the member roster pages on `limit`, not `per_page`, and nests rows under `data.members`, where `kb.rows()` cannot find them, so the list rendered empty. Fixed and pinned. `POST /broadcasting/auth` was confirmed to return a valid channel signature for a bearer token, so the auth half of real-time is proven; only the daemon is missing. 1506 tests pass (7088 assertions), pint clean. ROLES §4.1 + backend map §15.) — Prior 2026-08-18 (**BE-FX-12 — `GET /chats` 500 fixed**: the mobile chat inbox was dead in production (`ChatException(null: Server error)`); root cause was `latestOfMany()` aggregating `MAX(uuid)` on the `chat_messages` primary key, which Postgres has no function for, introduced with BE-FX-8's preview. Replaced with a plain `latest()` relation + `ChatService::attachLatestMessages()` (two grouped queries, no N+1). Reproduced AND verified read-only against the production DB; guard test added because the SQLite test suite structurally cannot catch Postgres-only SQL. Needs deploy — see Fixes BE-FX-12.) — Prior 2026-08-17 (**Web payment flow completed** (BE-NF-26, branch feat/kolabing-web-app): the web checkout existed but was not sellable — activation depended entirely on the Stripe webhook, so a business could pay, be shown "you're subscribed", and still be paywalled (and on prod, where the webhook is not yet registered, the sale would never activate at all). Added `POST /me/subscription/checkout/confirm` + a `/subscription/success` page that confirms the Checkout Session on return (ownership-checked; 409 `pending` retried with backoff; same idempotent activation the webhook uses), enabled **Stripe promotion codes** + email prefill + Stripe-customer reuse, replaced the buried quarterly text-link with two selectable plan cards (`Save 12%` derived from config), gave every paywall redirect a `?reason=` so the plan page names the blocked action, surfaced **`past_due`** as a shell alert → Billing Portal (additive `subscription_status` on `/auth/me`), and shipped a public **`kolabing.com/pricing` + `/es/pricing`** with Product/Offer/FAQPage JSON-LD. Also corrected a real doc/code drift: ROLES §2.1 claimed €39.99/mo, the code has always billed €49/€129 from `config/subscriptions.php`. 1486 tests pass, pint clean. ROLES §2.12 + backend map §14. Prior: **Marketing → web-app funnel wired** (BE-NF-27, branch feat/marketing-webapp-funnel, stacked on feat/kolabing-web-app): kolabing.com had no link to app.kolabing.com at all — every acquisition CTA was a dead `href="#"` or an in-page anchor, and mobile had no header CTA behind an inert hamburger. All surfaces now funnel into /login + /register via `config('webapp.url')`, with role-prefilled `?type=` links where the audience is already known. Prior same day: **Web App rebuilt on the Claude-Design handoff** (branch feat/kolabing-web-app): the whole app.kolabing.com UI replaced with the `kolabing-web-design/` bundle — Anton/Inter self-hosted, cream+#FFE28C palette, 264px sidebar shell, Explore card grid + detail/apply overlays, 4-tab My Kolabs (offers/requests/active/finished), multi-step Create wizard, Profile, Plan, and a new **/notifications** page. Alpine is now self-hosted (`public/webapp-assets/`) instead of a CDN. Two real wiring bugs fixed while wiring it: list endpoints return rows at `data.data` (the old views read `data` and silently rendered empty lists) — now via `kb.rows()`; and the business paywall on **accept** surfaces as **403**, not 402, so unsubscribed businesses dead-ended — now routed to /subscription (documented in ROLES-BACKEND-DB-MAP §3). Accept date picker bounded to the Kolab window + recurring days. **Third bug: the global CSP had no `'unsafe-eval'`, so Alpine (which compiles every x-* expression with `new Function`) never initialised and the whole web app was inert** — `AddSecurityHeaders` now serves a per-host policy: the web-app host gets `'unsafe-eval'` + `accounts.google.com` (script/style/frame, needed by Google Sign-In, which the old CSP also blocked); marketing + /admin keep the strict baseline. Covered by two new tests. es/ca kept at 100%. Prior: 2026-08-15 (**Kolabing Web App consolidated** (epic #128, branch feat/kolabing-web-app): the whole web app on app.kolabing.com — web Google login (BE-NF-18/GOOGLE_CLIENT_ID_WEB), Stripe web checkout (BE-NF-17, source=stripe) + Billing Portal (BE-NF-19), the Blade+Alpine app foundation+auth+subscription (BE-NF-20), feed + Kolab CRUD (BE-NF-21), profile edit + applications (BE-NF-22), image uploads (BE-NF-23), and es/ca i18n + SEO locale URLs (BE-NF-24/25). One PR. Prior same day: BE-NF-16 blog engine + SEO/GEO built, branch feat/blog-engine-seo-geo: public /blog + admin /admin/blog CRUD + Article/Organization/WebSite/FAQPage JSON-LD + homepage meta/OG + sitemap/llms now include posts. Prior 2026-08-02: BE-FX-11 F1 host_profile_id on EventResource + BE-FX-8 chat Prior: 2026-08-19 (**BE-FX-18 withdrawn — not a bug.** `POSTMARK_API_KEY` IS set on prod; the `failed_jobs` rows I read were historical (newest 07:27, none since — verified 13:15, spanning the BE-NF-34 deploy). BE-NF-34 is therefore **deployed and verified end-to-end**, invitation email included. Prior: 2026-08-19 (**BE-NF-34 deployed** — Community Hub is live on prod: migration ran, `/community/*` + `/c/{slug}` verified serving, queue worker draining. Deploy surfaced **BE-FX-18**: prod has no `POSTMARK_API_KEY`, so all outbound email — including the new invitation mail — fails. Env-only fix. Prior: 2026-08-19 (**Community members web panel built** (BE-NF-34, branch `feat/community-members-hub`): the NF-6 members & tiers backend had no human-reachable surface at all. Shipped the seven-tab Community Hub on app.kolabing.com plus the backend it needed — a searchable/filterable/sortable roster enriched with real engagement data, a stats endpoint, pending **email invitations** that convert on signup, member detail + bulk actions, and the public **`/c/{slug}` join page that had never existed even though every invite link pointed at it**. Four other live defects fixed on the way (removed members leaking into the roster; the roster ungated despite carrying emails; every member rendering as an email prefix; `kb.rows()` silently returning [] for keyed envelopes). Never paywalled — guarded by a test. es/ca 100%. Prior: 2026-08-19 (**BE-FX-17 — the marketing site's structured data was invalid everywhere**: Laravel 12's `@context` Blade directive silently replaced the `@context` key in every inline `{!! json_encode([...]) !!}` JSON-LD block, so Organization / WebSite / Product / FAQPage markup emitted compiled PHP instead and Google could not interpret any of it. Found while inspecting the new public profile page on production, which was unaffected because it encodes from a `@php` block. Fixed in four templates the same way, with a guard test that parses every ld+json block on ten marketing URLs. Prior same day: 2026-08-19 (**BE-NF-29 — public profile pages, in the panel and on the open web** (branch `feat/public-profiles`): businesses and communities had a rich public profile in the API that **no web surface ever rendered** — the panel had no profile route at all, and a shared Kolabing link led nowhere. Two pages now: `app.kolabing.com/profiles/{id}` shows the full detail (reviews with their authors + the five-category rating breakdown, past events with photos, past collaborations, gallery with a lightbox, contact links), reachable from Explore cards, the Kolab overlay, application rows, collaboration partners, notification actors, the chat header, the sidebar card and the account page. And `kolabing.com/p/{slug}` is a **public, indexable teaser** — identity, rating, three photos and one anonymised quote, with everything else (all reviews + who wrote them, contact links, past-event detail, partners) behind a sign-up wall that is enforced by *absence from the HTML*, not CSS. Slug is `name-<uuid tail>` because `profiles.handle` is only required for attendees (5 of 94 production profiles have one), so keying on it would 404 almost everywhere; old links survive a rename and full UUIDs still resolve. Backend: one additive endpoint `GET /profiles/{id}/public-profile` serving the rich shape for **both** roles — `kolabs.past_events` was always role-agnostic, only the community-scoped route made past events look community-only — plus additive `public_url` on that resource, `actor_profile_id` on notifications and `id` on chat participants so every client can link onward. Profiles with a completed collaboration are listed in `sitemap.xml`; `Organization`/`LocalBusiness` + `AggregateRating` JSON-LD, and the rating block is omitted rather than faked when there are no reviews. 29 new tests, most of them asserting what the public page must NOT contain. **Open decision:** the public page is opt-out-less — see BE-NF-30.) — Prior 2026-08-18 (**Reverb is already deployed — BE-IF-18's "MISSING" list was stale**: `.env` carries live credentials for Laravel Cloud's **managed** Reverb, so the self-hosted-daemon + nginx/TLS steps in that ticket do not apply. Verified by handshaking the real endpoint (accepts `Origin: https://app.kolabing.com`, `activity_timeout: 30`; a no-Origin connection is correctly rejected `4009`) and by running the whole loop — handshake → `/broadcasting/auth` → `pusher:subscribe` → `message.sent` — against a local `reverb:start` + `queue:work` on an isolated SQLite DB. The web panel therefore needs **no code change** for real-time; it lights up on deploy. One thing left to confirm: a **queue worker in production**, since `NewChatMessage` is `ShouldBroadcast` on a `database` queue and without a worker every broadcast is enqueued and never delivered. Docs corrected accordingly (they claimed the daemon was missing).) — Prior same day (**BE-NF-28 — chat integrated into the web panel** (branch `feat/webapp-chat`): app.kolabing.com had no chat at all — no route, no nav entry, not one call to the chat API — while the backend carried the whole feature (thread types, inbox, unread counts, bans, tier-gated channels) and only mobile used it. Added `/chats` (+ `/es`, `/ca`): a two-pane inbox (thread list + conversation, one route, no page load between threads), a `Messages` nav row with its own unread badge on every page, day-separated bubbles, community **channel management** (create ≤5 / rename / delete + per-member block list, owned communities only), and deep links `?thread=` / `?application=` / `?collaboration=` so My Kolabs rows and new-message notifications open the right conversation. Real-time is a **hand-written 250-line Pusher-protocol client** (`public/webapp-assets/kb-realtime.js`) instead of laravel-echo + pusher-js: the panel has no bundler and the CSP forbids CDNs. It stays dormant until BE-IF-18 hands over `REVERB_APP_KEY` and a 4s ticker polls in the meantime, so chat works either way. CSP gained `wss:` on the web-app host only (`connect-src https:` does NOT cover WebSockets). Three real backend gaps were found and filed rather than papered over: BE-FX-13 (silent Kolab messages), BE-FX-14 (§2.8 lapse re-gate not enforced for chat), BE-FX-15 (`/me/communities` hides delegated communities). Verified by replaying every call the page makes against a running server on an isolated SQLite copy (the local `.env` points at the production DB, so nothing was written there) — which caught a real bug in the block list: the member roster pages on `limit`, not `per_page`, and nests rows under `data.members`, where `kb.rows()` cannot find them, so the list rendered empty. Fixed and pinned. `POST /broadcasting/auth` was confirmed to return a valid channel signature for a bearer token, so the auth half of real-time is proven; only the daemon is missing. 1506 tests pass (7088 assertions), pint clean. ROLES §4.1 + backend map §15.) — Prior 2026-08-18 (**BE-FX-12 — `GET /chats` 500 fixed**: the mobile chat inbox was dead in production (`ChatException(null: Server error)`); root cause was `latestOfMany()` aggregating `MAX(uuid)` on the `chat_messages` primary key, which Postgres has no function for, introduced with BE-FX-8's preview. Replaced with a plain `latest()` relation + `ChatService::attachLatestMessages()` (two grouped queries, no N+1). Reproduced AND verified read-only against the production DB; guard test added because the SQLite test suite structurally cannot catch Postgres-only SQL. Needs deploy — see Fixes BE-FX-12.) — Prior 2026-08-17 (**Web payment flow completed** (BE-NF-26, branch feat/kolabing-web-app): the web checkout existed but was not sellable — activation depended entirely on the Stripe webhook, so a business could pay, be shown "you're subscribed", and still be paywalled (and on prod, where the webhook is not yet registered, the sale would never activate at all). Added `POST /me/subscription/checkout/confirm` + a `/subscription/success` page that confirms the Checkout Session on return (ownership-checked; 409 `pending` retried with backoff; same idempotent activation the webhook uses), enabled **Stripe promotion codes** + email prefill + Stripe-customer reuse, replaced the buried quarterly text-link with two selectable plan cards (`Save 12%` derived from config), gave every paywall redirect a `?reason=` so the plan page names the blocked action, surfaced **`past_due`** as a shell alert → Billing Portal (additive `subscription_status` on `/auth/me`), and shipped a public **`kolabing.com/pricing` + `/es/pricing`** with Product/Offer/FAQPage JSON-LD. Also corrected a real doc/code drift: ROLES §2.1 claimed €39.99/mo, the code has always billed €49/€129 from `config/subscriptions.php`. 1486 tests pass, pint clean. ROLES §2.12 + backend map §14. Prior: **Marketing → web-app funnel wired** (BE-NF-27, branch feat/marketing-webapp-funnel, stacked on feat/kolabing-web-app): kolabing.com had no link to app.kolabing.com at all — every acquisition CTA was a dead `href="#"` or an in-page anchor, and mobile had no header CTA behind an inert hamburger. All surfaces now funnel into /login + /register via `config('webapp.url')`, with role-prefilled `?type=` links where the audience is already known. Prior same day: **Web App rebuilt on the Claude-Design handoff** (branch feat/kolabing-web-app): the whole app.kolabing.com UI replaced with the `kolabing-web-design/` bundle — Anton/Inter self-hosted, cream+#FFE28C palette, 264px sidebar shell, Explore card grid + detail/apply overlays, 4-tab My Kolabs (offers/requests/active/finished), multi-step Create wizard, Profile, Plan, and a new **/notifications** page. Alpine is now self-hosted (`public/webapp-assets/`) instead of a CDN. Two real wiring bugs fixed while wiring it: list endpoints return rows at `data.data` (the old views read `data` and silently rendered empty lists) — now via `kb.rows()`; and the business paywall on **accept** surfaces as **403**, not 402, so unsubscribed businesses dead-ended — now routed to /subscription (documented in ROLES-BACKEND-DB-MAP §3). Accept date picker bounded to the Kolab window + recurring days. **Third bug: the global CSP had no `'unsafe-eval'`, so Alpine (which compiles every x-* expression with `new Function`) never initialised and the whole web app was inert** — `AddSecurityHeaders` now serves a per-host policy: the web-app host gets `'unsafe-eval'` + `accounts.google.com` (script/style/frame, needed by Google Sign-In, which the old CSP also blocked); marketing + /admin keep the strict baseline. Covered by two new tests. es/ca kept at 100%. Prior: 2026-08-15 (**Kolabing Web App consolidated** (epic #128, branch feat/kolabing-web-app): the whole web app on app.kolabing.com — web Google login (BE-NF-18/GOOGLE_CLIENT_ID_WEB), Stripe web checkout (BE-NF-17, source=stripe) + Billing Portal (BE-NF-19), the Blade+Alpine app foundation+auth+subscription (BE-NF-20), feed + Kolab CRUD (BE-NF-21), profile edit + applications (BE-NF-22), image uploads (BE-NF-23), and es/ca i18n + SEO locale URLs (BE-NF-24/25). One PR. Prior same day: BE-NF-16 blog engine + SEO/GEO built, branch feat/blog-engine-seo-geo: public /blog + admin /admin/blog CRUD + Article/Organization/WebSite/FAQPage JSON-LD + homepage meta/OG + sitemap/llms now include posts. Prior 2026-08-02: BE-FX-11 F1 host_profile_id on EventResource + BE-FX-8 chat) — Also 2026-08-22 (**BE-NF-39 — two-sided event suggestion system built, 13 of 18 tasks**, branch `feat/event-suggestion-system-impl`, ships dark behind `SUGGESTIONS_ENABLED=false`: a nightly batch scores candidate business-community pairs across six weighted signals and proposes a concrete event, three API endpoints serve them with an IDOR policy and **server-side** identity blur for non-subscribed businesses, and the web surface renders cards whose primary action opens a pre-filled Kolab form. Missing: the weekly digest, the ROLES §2.13 / backend-map §15 sections, and `paywall_hit`/`subscription_started` telemetry. Three defects in existing code found on the way and filed as **BE-FX-20/21/22** — note the ids: this work was numbered BE-NF-28 and BE-FX-13/14/15 against a three-day-old BACKLOG and every one of those had been claimed by a parallel session, so the commit messages on the branch still carry the old numbers.) — Prior 2026-08-20 (**BE-NF-38 — the invite page never worked, now fixed and given a join form**: its CTA sat in `<template x-if>` blocks on a layout with no Alpine, and the CSP grants 'unsafe-eval' only to the app host, so nothing was ever painted. Moved the page to the app host rather than weakening a security header; added Google sign-up plus a name/phone/photo form so an invitee joins in place. The regression test now asserts the page can RUN its Alpine, not just that the markup is there. Prior: 2026-08-20 (**BE-NF-37 phone preview built**: a read-only replica of the mobile app's profile screen in a device frame beside every Profile-section tab, refreshed after each edit — so reordering a gallery shows immediately which photo leads on a phone. Mirrors three named Dart files in kolabing-app; the pairing is recorded in backend map §17.5. No API, schema or role change. Prior: 2026-08-20 (**BE-NF-36 public portfolio panel built**: business and community accounts can finally manage the portfolio their public profile renders — gallery, past events, per-event photos — from a tabbed Profile section, with a Preview of their own public page. The find that shaped it: 'past events' lives in two unrelated stores and the profile read only the smaller one, so **60 past events and 173 photographs were invisible**; both are merged now. Also fixed: the gallery had `caption` and `sort_order` columns no endpoint ever wrote. No migration. Prior: 2026-08-19 (**SEO denetimi + düzeltmeleri**: kolabing.com canlı olarak denetlendi (16 sayfa) ve bulunan sekiz kod hatası aynı gün düzeltildi — BE-FX-19. En büyüğü ana sayfanın **24,58 MB görsel** yükü (→ 860 KB WebP, %96,5); ayrıca `www` ikinci bir kanonik site olarak yayındaydı, ana sayfa hiçbir ticari sayfaya link vermiyordu, pazarlama HTML'i hiç önbelleklenmiyordu, boş `/blog` ve `/communities` indekslenebilir ince sayfalardı ve sitemap eşiği bir seed test hesabını indekse sokuyordu. Denetimin kod dışı kalan üç kalemi ayrı iş olarak açıldı: BE-NF-31 (Tailwind CDN → build, görsel doğrulama gerektiriyor), BE-NF-32 (blog içeriği), BE-NF-33 (İspanyolca sayfalar). Prior: 2026-08-19 (**BE-FX-17 — the marketing site's structured data was invalid everywhere**: Laravel 12's `@context` Blade directive silently replaced the `@context` key in every inline `{!! json_encode([...]) !!}` JSON-LD block, so Organization / WebSite / Product / FAQPage markup emitted compiled PHP instead and Google could not interpret any of it. Found while inspecting the new public profile page on production, which was unaffected because it encodes from a `@php` block. Fixed in four templates the same way, with a guard test that parses every ld+json block on ten marketing URLs. Prior same day: 2026-08-19 (**BE-NF-29 — public profile pages, in the panel and on the open web** (branch `feat/public-profiles`): businesses and communities had a rich public profile in the API that **no web surface ever rendered** — the panel had no profile route at all, and a shared Kolabing link led nowhere. Two pages now: `app.kolabing.com/profiles/{id}` shows the full detail (reviews with their authors + the five-category rating breakdown, past events with photos, past collaborations, gallery with a lightbox, contact links), reachable from Explore cards, the Kolab overlay, application rows, collaboration partners, notification actors, the chat header, the sidebar card and the account page. And `kolabing.com/p/{slug}` is a **public, indexable teaser** — identity, rating, three photos and one anonymised quote, with everything else (all reviews + who wrote them, contact links, past-event detail, partners) behind a sign-up wall that is enforced by *absence from the HTML*, not CSS. Slug is `name-<uuid tail>` because `profiles.handle` is only required for attendees (5 of 94 production profiles have one), so keying on it would 404 almost everywhere; old links survive a rename and full UUIDs still resolve. Backend: one additive endpoint `GET /profiles/{id}/public-profile` serving the rich shape for **both** roles — `kolabs.past_events` was always role-agnostic, only the community-scoped route made past events look community-only — plus additive `public_url` on that resource, `actor_profile_id` on notifications and `id` on chat participants so every client can link onward. Profiles with a completed collaboration are listed in `sitemap.xml`; `Organization`/`LocalBusiness` + `AggregateRating` JSON-LD, and the rating block is omitted rather than faked when there are no reviews. 29 new tests, most of them asserting what the public page must NOT contain. **Open decision:** the public page is opt-out-less — see BE-NF-30.) — Prior 2026-08-18 (**Reverb is already deployed — BE-IF-18's "MISSING" list was stale**: `.env` carries live credentials for Laravel Cloud's **managed** Reverb, so the self-hosted-daemon + nginx/TLS steps in that ticket do not apply. Verified by handshaking the real endpoint (accepts `Origin: https://app.kolabing.com`, `activity_timeout: 30`; a no-Origin connection is correctly rejected `4009`) and by running the whole loop — handshake → `/broadcasting/auth` → `pusher:subscribe` → `message.sent` — against a local `reverb:start` + `queue:work` on an isolated SQLite DB. The web panel therefore needs **no code change** for real-time; it lights up on deploy. One thing left to confirm: a **queue worker in production**, since `NewChatMessage` is `ShouldBroadcast` on a `database` queue and without a worker every broadcast is enqueued and never delivered. Docs corrected accordingly (they claimed the daemon was missing).) — Prior same day (**BE-NF-28 — chat integrated into the web panel** (branch `feat/webapp-chat`): app.kolabing.com had no chat at all — no route, no nav entry, not one call to the chat API — while the backend carried the whole feature (thread types, inbox, unread counts, bans, tier-gated channels) and only mobile used it. Added `/chats` (+ `/es`, `/ca`): a two-pane inbox (thread list + conversation, one route, no page load between threads), a `Messages` nav row with its own unread badge on every page, day-separated bubbles, community **channel management** (create ≤5 / rename / delete + per-member block list, owned communities only), and deep links `?thread=` / `?application=` / `?collaboration=` so My Kolabs rows and new-message notifications open the right conversation. Real-time is a **hand-written 250-line Pusher-protocol client** (`public/webapp-assets/kb-realtime.js`) instead of laravel-echo + pusher-js: the panel has no bundler and the CSP forbids CDNs. It stays dormant until BE-IF-18 hands over `REVERB_APP_KEY` and a 4s ticker polls in the meantime, so chat works either way. CSP gained `wss:` on the web-app host only (`connect-src https:` does NOT cover WebSockets). Three real backend gaps were found and filed rather than papered over: BE-FX-13 (silent Kolab messages), BE-FX-14 (§2.8 lapse re-gate not enforced for chat), BE-FX-15 (`/me/communities` hides delegated communities). Verified by replaying every call the page makes against a running server on an isolated SQLite copy (the local `.env` points at the production DB, so nothing was written there) — which caught a real bug in the block list: the member roster pages on `limit`, not `per_page`, and nests rows under `data.members`, where `kb.rows()` cannot find them, so the list rendered empty. Fixed and pinned. `POST /broadcasting/auth` was confirmed to return a valid channel signature for a bearer token, so the auth half of real-time is proven; only the daemon is missing. 1506 tests pass (7088 assertions), pint clean. ROLES §4.1 + backend map §15.) — Prior 2026-08-18 (**BE-FX-12 — `GET /chats` 500 fixed**: the mobile chat inbox was dead in production (`ChatException(null: Server error)`); root cause was `latestOfMany()` aggregating `MAX(uuid)` on the `chat_messages` primary key, which Postgres has no function for, introduced with BE-FX-8's preview. Replaced with a plain `latest()` relation + `ChatService::attachLatestMessages()` (two grouped queries, no N+1). Reproduced AND verified read-only against the production DB; guard test added because the SQLite test suite structurally cannot catch Postgres-only SQL. Needs deploy — see Fixes BE-FX-12.) — Prior 2026-08-17 (**Web payment flow completed** (BE-NF-26, branch feat/kolabing-web-app): the web checkout existed but was not sellable — activation depended entirely on the Stripe webhook, so a business could pay, be shown "you're subscribed", and still be paywalled (and on prod, where the webhook is not yet registered, the sale would never activate at all). Added `POST /me/subscription/checkout/confirm` + a `/subscription/success` page that confirms the Checkout Session on return (ownership-checked; 409 `pending` retried with backoff; same idempotent activation the webhook uses), enabled **Stripe promotion codes** + email prefill + Stripe-customer reuse, replaced the buried quarterly text-link with two selectable plan cards (`Save 12%` derived from config), gave every paywall redirect a `?reason=` so the plan page names the blocked action, surfaced **`past_due`** as a shell alert → Billing Portal (additive `subscription_status` on `/auth/me`), and shipped a public **`kolabing.com/pricing` + `/es/pricing`** with Product/Offer/FAQPage JSON-LD. Also corrected a real doc/code drift: ROLES §2.1 claimed €39.99/mo, the code has always billed €49/€129 from `config/subscriptions.php`. 1486 tests pass, pint clean. ROLES §2.12 + backend map §14. Prior: **Marketing → web-app funnel wired** (BE-NF-27, branch feat/marketing-webapp-funnel, stacked on feat/kolabing-web-app): kolabing.com had no link to app.kolabing.com at all — every acquisition CTA was a dead `href="#"` or an in-page anchor, and mobile had no header CTA behind an inert hamburger. All surfaces now funnel into /login + /register via `config('webapp.url')`, with role-prefilled `?type=` links where the audience is already known. Prior same day: **Web App rebuilt on the Claude-Design handoff** (branch feat/kolabing-web-app): the whole app.kolabing.com UI replaced with the `kolabing-web-design/` bundle — Anton/Inter self-hosted, cream+#FFE28C palette, 264px sidebar shell, Explore card grid + detail/apply overlays, 4-tab My Kolabs (offers/requests/active/finished), multi-step Create wizard, Profile, Plan, and a new **/notifications** page. Alpine is now self-hosted (`public/webapp-assets/`) instead of a CDN. Two real wiring bugs fixed while wiring it: list endpoints return rows at `data.data` (the old views read `data` and silently rendered empty lists) — now via `kb.rows()`; and the business paywall on **accept** surfaces as **403**, not 402, so unsubscribed businesses dead-ended — now routed to /subscription (documented in ROLES-BACKEND-DB-MAP §3). Accept date picker bounded to the Kolab window + recurring days. **Third bug: the global CSP had no `'unsafe-eval'`, so Alpine (which compiles every x-* expression with `new Function`) never initialised and the whole web app was inert** — `AddSecurityHeaders` now serves a per-host policy: the web-app host gets `'unsafe-eval'` + `accounts.google.com` (script/style/frame, needed by Google Sign-In, which the old CSP also blocked); marketing + /admin keep the strict baseline. Covered by two new tests. es/ca kept at 100%. Prior: 2026-08-15 (**Kolabing Web App consolidated** (epic #128, branch feat/kolabing-web-app): the whole web app on app.kolabing.com — web Google login (BE-NF-18/GOOGLE_CLIENT_ID_WEB), Stripe web checkout (BE-NF-17, source=stripe) + Billing Portal (BE-NF-19), the Blade+Alpine app foundation+auth+subscription (BE-NF-20), feed + Kolab CRUD (BE-NF-21), profile edit + applications (BE-NF-22), image uploads (BE-NF-23), and es/ca i18n + SEO locale URLs (BE-NF-24/25). One PR. Prior same day: BE-NF-16 blog engine + SEO/GEO built, branch feat/blog-engine-seo-geo: public /blog + admin /admin/blog CRUD + Article/Organization/WebSite/FAQPage JSON-LD + homepage meta/OG + sitemap/llms now include posts. Prior 2026-08-02: BE-FX-11 F1 host_profile_id on EventResource + BE-FX-8 chat Prior: 2026-08-19 (**Community members web panel built** (BE-NF-34, branch `feat/community-members-hub`): the NF-6 members & tiers backend had no human-reachable surface at all. Shipped the seven-tab Community Hub on app.kolabing.com plus the backend it needed — a searchable/filterable/sortable roster enriched with real engagement data, a stats endpoint, pending **email invitations** that convert on signup, member detail + bulk actions, and the public **`/c/{slug}` join page that had never existed even though every invite link pointed at it**. Four other live defects fixed on the way (removed members leaking into the roster; the roster ungated despite carrying emails; every member rendering as an email prefix; `kb.rows()` silently returning [] for keyed envelopes). Never paywalled — guarded by a test. es/ca 100%. Prior: 2026-08-19 (**BE-FX-17 — the marketing site's structured data was invalid everywhere**: Laravel 12's `@context` Blade directive silently replaced the `@context` key in every inline `{!! json_encode([...]) !!}` JSON-LD block, so Organization / WebSite / Product / FAQPage markup emitted compiled PHP instead and Google could not interpret any of it. Found while inspecting the new public profile page on production, which was unaffected because it encodes from a `@php` block. Fixed in four templates the same way, with a guard test that parses every ld+json block on ten marketing URLs. Prior same day: 2026-08-19 (**BE-NF-29 — public profile pages, in the panel and on the open web** (branch `feat/public-profiles`): businesses and communities had a rich public profile in the API that **no web surface ever rendered** — the panel had no profile route at all, and a shared Kolabing link led nowhere. Two pages now: `app.kolabing.com/profiles/{id}` shows the full detail (reviews with their authors + the five-category rating breakdown, past events with photos, past collaborations, gallery with a lightbox, contact links), reachable from Explore cards, the Kolab overlay, application rows, collaboration partners, notification actors, the chat header, the sidebar card and the account page. And `kolabing.com/p/{slug}` is a **public, indexable teaser** — identity, rating, three photos and one anonymised quote, with everything else (all reviews + who wrote them, contact links, past-event detail, partners) behind a sign-up wall that is enforced by *absence from the HTML*, not CSS. Slug is `name-<uuid tail>` because `profiles.handle` is only required for attendees (5 of 94 production profiles have one), so keying on it would 404 almost everywhere; old links survive a rename and full UUIDs still resolve. Backend: one additive endpoint `GET /profiles/{id}/public-profile` serving the rich shape for **both** roles — `kolabs.past_events` was always role-agnostic, only the community-scoped route made past events look community-only — plus additive `public_url` on that resource, `actor_profile_id` on notifications and `id` on chat participants so every client can link onward. Profiles with a completed collaboration are listed in `sitemap.xml`; `Organization`/`LocalBusiness` + `AggregateRating` JSON-LD, and the rating block is omitted rather than faked when there are no reviews. 29 new tests, most of them asserting what the public page must NOT contain. **Open decision:** the public page is opt-out-less — see BE-NF-30.) — Prior 2026-08-18 (**Reverb is already deployed — BE-IF-18's "MISSING" list was stale**: `.env` carries live credentials for Laravel Cloud's **managed** Reverb, so the self-hosted-daemon + nginx/TLS steps in that ticket do not apply. Verified by handshaking the real endpoint (accepts `Origin: https://app.kolabing.com`, `activity_timeout: 30`; a no-Origin connection is correctly rejected `4009`) and by running the whole loop — handshake → `/broadcasting/auth` → `pusher:subscribe` → `message.sent` — against a local `reverb:start` + `queue:work` on an isolated SQLite DB. The web panel therefore needs **no code change** for real-time; it lights up on deploy. One thing left to confirm: a **queue worker in production**, since `NewChatMessage` is `ShouldBroadcast` on a `database` queue and without a worker every broadcast is enqueued and never delivered. Docs corrected accordingly (they claimed the daemon was missing).) — Prior same day (**BE-NF-28 — chat integrated into the web panel** (branch `feat/webapp-chat`): app.kolabing.com had no chat at all — no route, no nav entry, not one call to the chat API — while the backend carried the whole feature (thread types, inbox, unread counts, bans, tier-gated channels) and only mobile used it. Added `/chats` (+ `/es`, `/ca`): a two-pane inbox (thread list + conversation, one route, no page load between threads), a `Messages` nav row with its own unread badge on every page, day-separated bubbles, community **channel management** (create ≤5 / rename / delete + per-member block list, owned communities only), and deep links `?thread=` / `?application=` / `?collaboration=` so My Kolabs rows and new-message notifications open the right conversation. Real-time is a **hand-written 250-line Pusher-protocol client** (`public/webapp-assets/kb-realtime.js`) instead of laravel-echo + pusher-js: the panel has no bundler and the CSP forbids CDNs. It stays dormant until BE-IF-18 hands over `REVERB_APP_KEY` and a 4s ticker polls in the meantime, so chat works either way. CSP gained `wss:` on the web-app host only (`connect-src https:` does NOT cover WebSockets). Three real backend gaps were found and filed rather than papered over: BE-FX-13 (silent Kolab messages), BE-FX-14 (§2.8 lapse re-gate not enforced for chat), BE-FX-15 (`/me/communities` hides delegated communities). Verified by replaying every call the page makes against a running server on an isolated SQLite copy (the local `.env` points at the production DB, so nothing was written there) — which caught a real bug in the block list: the member roster pages on `limit`, not `per_page`, and nests rows under `data.members`, where `kb.rows()` cannot find them, so the list rendered empty. Fixed and pinned. `POST /broadcasting/auth` was confirmed to return a valid channel signature for a bearer token, so the auth half of real-time is proven; only the daemon is missing. 1506 tests pass (7088 assertions), pint clean. ROLES §4.1 + backend map §15.) — Prior 2026-08-18 (**BE-FX-12 — `GET /chats` 500 fixed**: the mobile chat inbox was dead in production (`ChatException(null: Server error)`); root cause was `latestOfMany()` aggregating `MAX(uuid)` on the `chat_messages` primary key, which Postgres has no function for, introduced with BE-FX-8's preview. Replaced with a plain `latest()` relation + `ChatService::attachLatestMessages()` (two grouped queries, no N+1). Reproduced AND verified read-only against the production DB; guard test added because the SQLite test suite structurally cannot catch Postgres-only SQL. Needs deploy — see Fixes BE-FX-12.) — Prior 2026-08-17 (**Web payment flow completed** (BE-NF-26, branch feat/kolabing-web-app): the web checkout existed but was not sellable — activation depended entirely on the Stripe webhook, so a business could pay, be shown "you're subscribed", and still be paywalled (and on prod, where the webhook is not yet registered, the sale would never activate at all). Added `POST /me/subscription/checkout/confirm` + a `/subscription/success` page that confirms the Checkout Session on return (ownership-checked; 409 `pending` retried with backoff; same idempotent activation the webhook uses), enabled **Stripe promotion codes** + email prefill + Stripe-customer reuse, replaced the buried quarterly text-link with two selectable plan cards (`Save 12%` derived from config), gave every paywall redirect a `?reason=` so the plan page names the blocked action, surfaced **`past_due`** as a shell alert → Billing Portal (additive `subscription_status` on `/auth/me`), and shipped a public **`kolabing.com/pricing` + `/es/pricing`** with Product/Offer/FAQPage JSON-LD. Also corrected a real doc/code drift: ROLES §2.1 claimed €39.99/mo, the code has always billed €49/€129 from `config/subscriptions.php`. 1486 tests pass, pint clean. ROLES §2.12 + backend map §14. Prior: **Marketing → web-app funnel wired** (BE-NF-27, branch feat/marketing-webapp-funnel, stacked on feat/kolabing-web-app): kolabing.com had no link to app.kolabing.com at all — every acquisition CTA was a dead `href="#"` or an in-page anchor, and mobile had no header CTA behind an inert hamburger. All surfaces now funnel into /login + /register via `config('webapp.url')`, with role-prefilled `?type=` links where the audience is already known. Prior same day: **Web App rebuilt on the Claude-Design handoff** (branch feat/kolabing-web-app): the whole app.kolabing.com UI replaced with the `kolabing-web-design/` bundle — Anton/Inter self-hosted, cream+#FFE28C palette, 264px sidebar shell, Explore card grid + detail/apply overlays, 4-tab My Kolabs (offers/requests/active/finished), multi-step Create wizard, Profile, Plan, and a new **/notifications** page. Alpine is now self-hosted (`public/webapp-assets/`) instead of a CDN. Two real wiring bugs fixed while wiring it: list endpoints return rows at `data.data` (the old views read `data` and silently rendered empty lists) — now via `kb.rows()`; and the business paywall on **accept** surfaces as **403**, not 402, so unsubscribed businesses dead-ended — now routed to /subscription (documented in ROLES-BACKEND-DB-MAP §3). Accept date picker bounded to the Kolab window + recurring days. **Third bug: the global CSP had no `'unsafe-eval'`, so Alpine (which compiles every x-* expression with `new Function`) never initialised and the whole web app was inert** — `AddSecurityHeaders` now serves a per-host policy: the web-app host gets `'unsafe-eval'` + `accounts.google.com` (script/style/frame, needed by Google Sign-In, which the old CSP also blocked); marketing + /admin keep the strict baseline. Covered by two new tests. es/ca kept at 100%. Prior: 2026-08-15 (**Kolabing Web App consolidated** (epic #128, branch feat/kolabing-web-app): the whole web app on app.kolabing.com — web Google login (BE-NF-18/GOOGLE_CLIENT_ID_WEB), Stripe web checkout (BE-NF-17, source=stripe) + Billing Portal (BE-NF-19), the Blade+Alpine app foundation+auth+subscription (BE-NF-20), feed + Kolab CRUD (BE-NF-21), profile edit + applications (BE-NF-22), image uploads (BE-NF-23), and es/ca i18n + SEO locale URLs (BE-NF-24/25). One PR. Prior same day: BE-NF-16 blog engine + SEO/GEO built, branch feat/blog-engine-seo-geo: public /blog + admin /admin/blog CRUD + Article/Organization/WebSite/FAQPage JSON-LD + homepage meta/OG + sitemap/llms now include posts. Prior 2026-08-02: BE-FX-11 F1 host_profile_id on EventResource + BE-FX-8 chat Prior: 2026-08-19 (**BE-NF-34 deployed** — Community Hub is live on prod: migration ran, `/community/*` + `/c/{slug}` verified serving, queue worker draining. Deploy surfaced **BE-FX-18**: prod has no `POSTMARK_API_KEY`, so all outbound email — including the new invitation mail — fails. Env-only fix. Prior: 2026-08-19 (**Community members web panel built** (BE-NF-34, branch `feat/community-members-hub`): the NF-6 members & tiers backend had no human-reachable surface at all. Shipped the seven-tab Community Hub on app.kolabing.com plus the backend it needed — a searchable/filterable/sortable roster enriched with real engagement data, a stats endpoint, pending **email invitations** that convert on signup, member detail + bulk actions, and the public **`/c/{slug}` join page that had never existed even though every invite link pointed at it**. Four other live defects fixed on the way (removed members leaking into the roster; the roster ungated despite carrying emails; every member rendering as an email prefix; `kb.rows()` silently returning [] for keyed envelopes). Never paywalled — guarded by a test. es/ca 100%. Prior: 2026-08-19 (**BE-FX-17 — the marketing site's structured data was invalid everywhere**: Laravel 12's `@context` Blade directive silently replaced the `@context` key in every inline `{!! json_encode([...]) !!}` JSON-LD block, so Organization / WebSite / Product / FAQPage markup emitted compiled PHP instead and Google could not interpret any of it. Found while inspecting the new public profile page on production, which was unaffected because it encodes from a `@php` block. Fixed in four templates the same way, with a guard test that parses every ld+json block on ten marketing URLs. Prior same day: 2026-08-19 (**BE-NF-29 — public profile pages, in the panel and on the open web** (branch `feat/public-profiles`): businesses and communities had a rich public profile in the API that **no web surface ever rendered** — the panel had no profile route at all, and a shared Kolabing link led nowhere. Two pages now: `app.kolabing.com/profiles/{id}` shows the full detail (reviews with their authors + the five-category rating breakdown, past events with photos, past collaborations, gallery with a lightbox, contact links), reachable from Explore cards, the Kolab overlay, application rows, collaboration partners, notification actors, the chat header, the sidebar card and the account page. And `kolabing.com/p/{slug}` is a **public, indexable teaser** — identity, rating, three photos and one anonymised quote, with everything else (all reviews + who wrote them, contact links, past-event detail, partners) behind a sign-up wall that is enforced by *absence from the HTML*, not CSS. Slug is `name-<uuid tail>` because `profiles.handle` is only required for attendees (5 of 94 production profiles have one), so keying on it would 404 almost everywhere; old links survive a rename and full UUIDs still resolve. Backend: one additive endpoint `GET /profiles/{id}/public-profile` serving the rich shape for **both** roles — `kolabs.past_events` was always role-agnostic, only the community-scoped route made past events look community-only — plus additive `public_url` on that resource, `actor_profile_id` on notifications and `id` on chat participants so every client can link onward. Profiles with a completed collaboration are listed in `sitemap.xml`; `Organization`/`LocalBusiness` + `AggregateRating` JSON-LD, and the rating block is omitted rather than faked when there are no reviews. 29 new tests, most of them asserting what the public page must NOT contain. **Open decision:** the public page is opt-out-less — see BE-NF-30.) — Prior 2026-08-18 (**Reverb is already deployed — BE-IF-18's "MISSING" list was stale**: `.env` carries live credentials for Laravel Cloud's **managed** Reverb, so the self-hosted-daemon + nginx/TLS steps in that ticket do not apply. Verified by handshaking the real endpoint (accepts `Origin: https://app.kolabing.com`, `activity_timeout: 30`; a no-Origin connection is correctly rejected `4009`) and by running the whole loop — handshake → `/broadcasting/auth` → `pusher:subscribe` → `message.sent` — against a local `reverb:start` + `queue:work` on an isolated SQLite DB. The web panel therefore needs **no code change** for real-time; it lights up on deploy. One thing left to confirm: a **queue worker in production**, since `NewChatMessage` is `ShouldBroadcast` on a `database` queue and without a worker every broadcast is enqueued and never delivered. Docs corrected accordingly (they claimed the daemon was missing).) — Prior same day (**BE-NF-28 — chat integrated into the web panel** (branch `feat/webapp-chat`): app.kolabing.com had no chat at all — no route, no nav entry, not one call to the chat API — while the backend carried the whole feature (thread types, inbox, unread counts, bans, tier-gated channels) and only mobile used it. Added `/chats` (+ `/es`, `/ca`): a two-pane inbox (thread list + conversation, one route, no page load between threads), a `Messages` nav row with its own unread badge on every page, day-separated bubbles, community **channel management** (create ≤5 / rename / delete + per-member block list, owned communities only), and deep links `?thread=` / `?application=` / `?collaboration=` so My Kolabs rows and new-message notifications open the right conversation. Real-time is a **hand-written 250-line Pusher-protocol client** (`public/webapp-assets/kb-realtime.js`) instead of laravel-echo + pusher-js: the panel has no bundler and the CSP forbids CDNs. It stays dormant until BE-IF-18 hands over `REVERB_APP_KEY` and a 4s ticker polls in the meantime, so chat works either way. CSP gained `wss:` on the web-app host only (`connect-src https:` does NOT cover WebSockets). Three real backend gaps were found and filed rather than papered over: BE-FX-13 (silent Kolab messages), BE-FX-14 (§2.8 lapse re-gate not enforced for chat), BE-FX-15 (`/me/communities` hides delegated communities). Verified by replaying every call the page makes against a running server on an isolated SQLite copy (the local `.env` points at the production DB, so nothing was written there) — which caught a real bug in the block list: the member roster pages on `limit`, not `per_page`, and nests rows under `data.members`, where `kb.rows()` cannot find them, so the list rendered empty. Fixed and pinned. `POST /broadcasting/auth` was confirmed to return a valid channel signature for a bearer token, so the auth half of real-time is proven; only the daemon is missing. 1506 tests pass (7088 assertions), pint clean. ROLES §4.1 + backend map §15.) — Prior 2026-08-18 (**BE-FX-12 — `GET /chats` 500 fixed**: the mobile chat inbox was dead in production (`ChatException(null: Server error)`); root cause was `latestOfMany()` aggregating `MAX(uuid)` on the `chat_messages` primary key, which Postgres has no function for, introduced with BE-FX-8's preview. Replaced with a plain `latest()` relation + `ChatService::attachLatestMessages()` (two grouped queries, no N+1). Reproduced AND verified read-only against the production DB; guard test added because the SQLite test suite structurally cannot catch Postgres-only SQL. Needs deploy — see Fixes BE-FX-12.) — Prior 2026-08-17 (**Web payment flow completed** (BE-NF-26, branch feat/kolabing-web-app): the web checkout existed but was not sellable — activation depended entirely on the Stripe webhook, so a business could pay, be shown "you're subscribed", and still be paywalled (and on prod, where the webhook is not yet registered, the sale would never activate at all). Added `POST /me/subscription/checkout/confirm` + a `/subscription/success` page that confirms the Checkout Session on return (ownership-checked; 409 `pending` retried with backoff; same idempotent activation the webhook uses), enabled **Stripe promotion codes** + email prefill + Stripe-customer reuse, replaced the buried quarterly text-link with two selectable plan cards (`Save 12%` derived from config), gave every paywall redirect a `?reason=` so the plan page names the blocked action, surfaced **`past_due`** as a shell alert → Billing Portal (additive `subscription_status` on `/auth/me`), and shipped a public **`kolabing.com/pricing` + `/es/pricing`** with Product/Offer/FAQPage JSON-LD. Also corrected a real doc/code drift: ROLES §2.1 claimed €39.99/mo, the code has always billed €49/€129 from `config/subscriptions.php`. 1486 tests pass, pint clean. ROLES §2.12 + backend map §14. Prior: **Marketing → web-app funnel wired** (BE-NF-27, branch feat/marketing-webapp-funnel, stacked on feat/kolabing-web-app): kolabing.com had no link to app.kolabing.com at all — every acquisition CTA was a dead `href="#"` or an in-page anchor, and mobile had no header CTA behind an inert hamburger. All surfaces now funnel into /login + /register via `config('webapp.url')`, with role-prefilled `?type=` links where the audience is already known. Prior same day: **Web App rebuilt on the Claude-Design handoff** (branch feat/kolabing-web-app): the whole app.kolabing.com UI replaced with the `kolabing-web-design/` bundle — Anton/Inter self-hosted, cream+#FFE28C palette, 264px sidebar shell, Explore card grid + detail/apply overlays, 4-tab My Kolabs (offers/requests/active/finished), multi-step Create wizard, Profile, Plan, and a new **/notifications** page. Alpine is now self-hosted (`public/webapp-assets/`) instead of a CDN. Two real wiring bugs fixed while wiring it: list endpoints return rows at `data.data` (the old views read `data` and silently rendered empty lists) — now via `kb.rows()`; and the business paywall on **accept** surfaces as **403**, not 402, so unsubscribed businesses dead-ended — now routed to /subscription (documented in ROLES-BACKEND-DB-MAP §3). Accept date picker bounded to the Kolab window + recurring days. **Third bug: the global CSP had no `'unsafe-eval'`, so Alpine (which compiles every x-* expression with `new Function`) never initialised and the whole web app was inert** — `AddSecurityHeaders` now serves a per-host policy: the web-app host gets `'unsafe-eval'` + `accounts.google.com` (script/style/frame, needed by Google Sign-In, which the old CSP also blocked); marketing + /admin keep the strict baseline. Covered by two new tests. es/ca kept at 100%. Prior: 2026-08-15 (**Kolabing Web App consolidated** (epic #128, branch feat/kolabing-web-app): the whole web app on app.kolabing.com — web Google login (BE-NF-18/GOOGLE_CLIENT_ID_WEB), Stripe web checkout (BE-NF-17, source=stripe) + Billing Portal (BE-NF-19), the Blade+Alpine app foundation+auth+subscription (BE-NF-20), feed + Kolab CRUD (BE-NF-21), profile edit + applications (BE-NF-22), image uploads (BE-NF-23), and es/ca i18n + SEO locale URLs (BE-NF-24/25). One PR. Prior same day: BE-NF-16 blog engine + SEO/GEO built, branch feat/blog-engine-seo-geo: public /blog + admin /admin/blog CRUD + Article/Organization/WebSite/FAQPage JSON-LD + homepage meta/OG + sitemap/llms now include posts. Prior 2026-08-02: BE-FX-11 F1 host_profile_id on EventResource + BE-FX-8 chat Prior: 2026-08-19 (**BE-FX-18 withdrawn — not a bug.** `POSTMARK_API_KEY` IS set on prod; the `failed_jobs` rows I read were historical (newest 07:27, none since — verified 13:15, spanning the BE-NF-34 deploy). BE-NF-34 is therefore **deployed and verified end-to-end**, invitation email included. Prior: 2026-08-19 (**BE-NF-34 deployed** — Community Hub is live on prod: migration ran, `/community/*` + `/c/{slug}` verified serving, queue worker draining. Deploy surfaced **BE-FX-18**: prod has no `POSTMARK_API_KEY`, so all outbound email — including the new invitation mail — fails. Env-only fix. Prior: 2026-08-19 (**Community members web panel built** (BE-NF-34, branch `feat/community-members-hub`): the NF-6 members & tiers backend had no human-reachable surface at all. Shipped the seven-tab Community Hub on app.kolabing.com plus the backend it needed — a searchable/filterable/sortable roster enriched with real engagement data, a stats endpoint, pending **email invitations** that convert on signup, member detail + bulk actions, and the public **`/c/{slug}` join page that had never existed even though every invite link pointed at it**. Four other live defects fixed on the way (removed members leaking into the roster; the roster ungated despite carrying emails; every member rendering as an email prefix; `kb.rows()` silently returning [] for keyed envelopes). Never paywalled — guarded by a test. es/ca 100%. Prior: 2026-08-19 (**BE-FX-17 — the marketing site's structured data was invalid everywhere**: Laravel 12's `@context` Blade directive silently replaced the `@context` key in every inline `{!! json_encode([...]) !!}` JSON-LD block, so Organization / WebSite / Product / FAQPage markup emitted compiled PHP instead and Google could not interpret any of it. Found while inspecting the new public profile page on production, which was unaffected because it encodes from a `@php` block. Fixed in four templates the same way, with a guard test that parses every ld+json block on ten marketing URLs. Prior same day: 2026-08-19 (**BE-NF-29 — public profile pages, in the panel and on the open web** (branch `feat/public-profiles`): businesses and communities had a rich public profile in the API that **no web surface ever rendered** — the panel had no profile route at all, and a shared Kolabing link led nowhere. Two pages now: `app.kolabing.com/profiles/{id}` shows the full detail (reviews with their authors + the five-category rating breakdown, past events with photos, past collaborations, gallery with a lightbox, contact links), reachable from Explore cards, the Kolab overlay, application rows, collaboration partners, notification actors, the chat header, the sidebar card and the account page. And `kolabing.com/p/{slug}` is a **public, indexable teaser** — identity, rating, three photos and one anonymised quote, with everything else (all reviews + who wrote them, contact links, past-event detail, partners) behind a sign-up wall that is enforced by *absence from the HTML*, not CSS. Slug is `name-<uuid tail>` because `profiles.handle` is only required for attendees (5 of 94 production profiles have one), so keying on it would 404 almost everywhere; old links survive a rename and full UUIDs still resolve. Backend: one additive endpoint `GET /profiles/{id}/public-profile` serving the rich shape for **both** roles — `kolabs.past_events` was always role-agnostic, only the community-scoped route made past events look community-only — plus additive `public_url` on that resource, `actor_profile_id` on notifications and `id` on chat participants so every client can link onward. Profiles with a completed collaboration are listed in `sitemap.xml`; `Organization`/`LocalBusiness` + `AggregateRating` JSON-LD, and the rating block is omitted rather than faked when there are no reviews. 29 new tests, most of them asserting what the public page must NOT contain. **Open decision:** the public page is opt-out-less — see BE-NF-30.) — Prior 2026-08-18 (**Reverb is already deployed — BE-IF-18's "MISSING" list was stale**: `.env` carries live credentials for Laravel Cloud's **managed** Reverb, so the self-hosted-daemon + nginx/TLS steps in that ticket do not apply. Verified by handshaking the real endpoint (accepts `Origin: https://app.kolabing.com`, `activity_timeout: 30`; a no-Origin connection is correctly rejected `4009`) and by running the whole loop — handshake → `/broadcasting/auth` → `pusher:subscribe` → `message.sent` — against a local `reverb:start` + `queue:work` on an isolated SQLite DB. The web panel therefore needs **no code change** for real-time; it lights up on deploy. One thing left to confirm: a **queue worker in production**, since `NewChatMessage` is `ShouldBroadcast` on a `database` queue and without a worker every broadcast is enqueued and never delivered. Docs corrected accordingly (they claimed the daemon was missing).) — Prior same day (**BE-NF-28 — chat integrated into the web panel** (branch `feat/webapp-chat`): app.kolabing.com had no chat at all — no route, no nav entry, not one call to the chat API — while the backend carried the whole feature (thread types, inbox, unread counts, bans, tier-gated channels) and only mobile used it. Added `/chats` (+ `/es`, `/ca`): a two-pane inbox (thread list + conversation, one route, no page load between threads), a `Messages` nav row with its own unread badge on every page, day-separated bubbles, community **channel management** (create ≤5 / rename / delete + per-member block list, owned communities only), and deep links `?thread=` / `?application=` / `?collaboration=` so My Kolabs rows and new-message notifications open the right conversation. Real-time is a **hand-written 250-line Pusher-protocol client** (`public/webapp-assets/kb-realtime.js`) instead of laravel-echo + pusher-js: the panel has no bundler and the CSP forbids CDNs. It stays dormant until BE-IF-18 hands over `REVERB_APP_KEY` and a 4s ticker polls in the meantime, so chat works either way. CSP gained `wss:` on the web-app host only (`connect-src https:` does NOT cover WebSockets). Three real backend gaps were found and filed rather than papered over: BE-FX-13 (silent Kolab messages), BE-FX-14 (§2.8 lapse re-gate not enforced for chat), BE-FX-15 (`/me/communities` hides delegated communities). Verified by replaying every call the page makes against a running server on an isolated SQLite copy (the local `.env` points at the production DB, so nothing was written there) — which caught a real bug in the block list: the member roster pages on `limit`, not `per_page`, and nests rows under `data.members`, where `kb.rows()` cannot find them, so the list rendered empty. Fixed and pinned. `POST /broadcasting/auth` was confirmed to return a valid channel signature for a bearer token, so the auth half of real-time is proven; only the daemon is missing. 1506 tests pass (7088 assertions), pint clean. ROLES §4.1 + backend map §15.) — Prior 2026-08-18 (**BE-FX-12 — `GET /chats` 500 fixed**: the mobile chat inbox was dead in production (`ChatException(null: Server error)`); root cause was `latestOfMany()` aggregating `MAX(uuid)` on the `chat_messages` primary key, which Postgres has no function for, introduced with BE-FX-8's preview. Replaced with a plain `latest()` relation + `ChatService::attachLatestMessages()` (two grouped queries, no N+1). Reproduced AND verified read-only against the production DB; guard test added because the SQLite test suite structurally cannot catch Postgres-only SQL. Needs deploy — see Fixes BE-FX-12.) — Prior 2026-08-17 (**Web payment flow completed** (BE-NF-26, branch feat/kolabing-web-app): the web checkout existed but was not sellable — activation depended entirely on the Stripe webhook, so a business could pay, be shown "you're subscribed", and still be paywalled (and on prod, where the webhook is not yet registered, the sale would never activate at all). Added `POST /me/subscription/checkout/confirm` + a `/subscription/success` page that confirms the Checkout Session on return (ownership-checked; 409 `pending` retried with backoff; same idempotent activation the webhook uses), enabled **Stripe promotion codes** + email prefill + Stripe-customer reuse, replaced the buried quarterly text-link with two selectable plan cards (`Save 12%` derived from config), gave every paywall redirect a `?reason=` so the plan page names the blocked action, surfaced **`past_due`** as a shell alert → Billing Portal (additive `subscription_status` on `/auth/me`), and shipped a public **`kolabing.com/pricing` + `/es/pricing`** with Product/Offer/FAQPage JSON-LD. Also corrected a real doc/code drift: ROLES §2.1 claimed €39.99/mo, the code has always billed €49/€129 from `config/subscriptions.php`. 1486 tests pass, pint clean. ROLES §2.12 + backend map §14. Prior: **Marketing → web-app funnel wired** (BE-NF-27, branch feat/marketing-webapp-funnel, stacked on feat/kolabing-web-app): kolabing.com had no link to app.kolabing.com at all — every acquisition CTA was a dead `href="#"` or an in-page anchor, and mobile had no header CTA behind an inert hamburger. All surfaces now funnel into /login + /register via `config('webapp.url')`, with role-prefilled `?type=` links where the audience is already known. Prior same day: **Web App rebuilt on the Claude-Design handoff** (branch feat/kolabing-web-app): the whole app.kolabing.com UI replaced with the `kolabing-web-design/` bundle — Anton/Inter self-hosted, cream+#FFE28C palette, 264px sidebar shell, Explore card grid + detail/apply overlays, 4-tab My Kolabs (offers/requests/active/finished), multi-step Create wizard, Profile, Plan, and a new **/notifications** page. Alpine is now self-hosted (`public/webapp-assets/`) instead of a CDN. Two real wiring bugs fixed while wiring it: list endpoints return rows at `data.data` (the old views read `data` and silently rendered empty lists) — now via `kb.rows()`; and the business paywall on **accept** surfaces as **403**, not 402, so unsubscribed businesses dead-ended — now routed to /subscription (documented in ROLES-BACKEND-DB-MAP §3). Accept date picker bounded to the Kolab window + recurring days. **Third bug: the global CSP had no `'unsafe-eval'`, so Alpine (which compiles every x-* expression with `new Function`) never initialised and the whole web app was inert** — `AddSecurityHeaders` now serves a per-host policy: the web-app host gets `'unsafe-eval'` + `accounts.google.com` (script/style/frame, needed by Google Sign-In, which the old CSP also blocked); marketing + /admin keep the strict baseline. Covered by two new tests. es/ca kept at 100%. Prior: 2026-08-15 (**Kolabing Web App consolidated** (epic #128, branch feat/kolabing-web-app): the whole web app on app.kolabing.com — web Google login (BE-NF-18/GOOGLE_CLIENT_ID_WEB), Stripe web checkout (BE-NF-17, source=stripe) + Billing Portal (BE-NF-19), the Blade+Alpine app foundation+auth+subscription (BE-NF-20), feed + Kolab CRUD (BE-NF-21), profile edit + applications (BE-NF-22), image uploads (BE-NF-23), and es/ca i18n + SEO locale URLs (BE-NF-24/25). One PR. Prior same day: BE-NF-16 blog engine + SEO/GEO built, branch feat/blog-engine-seo-geo: public /blog + admin /admin/blog CRUD + Article/Organization/WebSite/FAQPage JSON-LD + homepage meta/OG + sitemap/llms now include posts. Prior 2026-08-02: BE-FX-11 F1 host_profile_id on EventResource + BE-FX-8 chat)))))
> last_message preview + BE-FX-9 factory-avatar distinctness — fixed in code on branch
> fix/qa-batch-host-profile-chat-preview-avatars; app half = kolabing-app PR 108. Prior:
> added BE-FX-10 — Explore browse feed now hides date-exhausted
> Kolabs, fixed + tested. Prior: bootstrapped — the file `CLAUDE.md` mandates did not exist;
> seeded with the known backend-outstanding items from the mobile-audit analysis and
> referenced tickets. Future sessions must keep it in sync.)

---

## 🆕 New Features
_Planned backend work that does not exist yet._

| # | Feature | Notes | Status |
|---|---------|-------|--------|
| BE-NF-40 | **Attendee funnel must run over Kolabs, not a parallel events system** (backend BUILT 2026-08-22 — see BE-NF-43; the panel's *Events* nav entry is removed, and `/events` public pages still need retargeting at confirmed Kolabs) | Decided by Volkan 2026-08-22: *"ayrı bir events sistemi olmamalı, kolablar üstünden yürümeli — kolab onaylandı, insanlar kayıt oldu, QR'larını okutacaklar, giriş yapacaklar."* The schema already works this way — accepted application → `collaborations` → lazily-created `events` row → check-in token → QR (`CollaborationQrCodeController`) — so `events` is the **internal door mechanism**, not a user-facing surface. BE-NF-37 wrongly surfaced it as one (`kolabing.com/events` listing `Event` rows, event-level RSVP). Rework: the public "what's on" lists **confirmed Kolabs** (scheduled/active collaborations off a published Kolab), registering signs an attendee up to that collaboration's happening, and the QR stays the door. Production evidence for the urgency: 64 events, **all in the past**, 62 members-only, so the merged feed can only ever render empty; meanwhile 16 collaborations exist. | 🚧 IN PROGRESS |
| BE-NF-30 | **Admin CRM: verified community leads** | Challenge A replaced the 2026-08-18 first-pass 140 community leads with a VERIFIED set (145 across 7 cities): identity/status/plausibility gates + blind independent recount; global-brand/dead/wrong-city/handle-mismatch rows removed (reject ledger in-repo). `KolabingVerifiedLeadsSeeder` + prod-guarded migration. | PR open |
| BE-NF-32 | **Admin CRM: verified data displays/edits/scores + Barcelona** | Fix the "not seeded properly" defect: verification columns (city/type/audience/confidence) as community defaults, legacy demoted; edit no longer wipes verification metadata (validated() merges); numeric audience_count + locality flags for scoring; evidence renders as a link. Adds Barcelona (8th city) → 165 verified. | PR open (crm-data-fix) |
| BE-NF-33 | **Admin CRM: community fit-score + Work-now** | CrmScoreService fit model for verified communities (audience log-scaled, has-collab, recency, confidence scaler, locality/global-brand cap) + score_breakdown; recalc migration; "Work now" filter (high/med confidence, local, fit>=40, not contacted). | PR open (crm-fit-score) |
| BE-NF-5 | **Admin-managed gamification economy** | Server-owned reward economy so the app stops hardcoding XP/badges. `GET /gamification/config` + `xp_earn_rules` (drives `point_ledger` + display), badge requirements, referral/withdrawal economics. Prompts: `docs/tickets/2026-06-01-admin-challenges-prompt.md`, `docs/tickets/2026-06-01-admin-xp-economy-prompt.md`. | Not started |
| BE-NF-29 | **Admin CRM: city filter + map** | `/admin/crm` gained a city filter (from `metrics.city`/`source_city`) + an inline-SVG map of communities-by-city (bubbles link to the filter), for the 7-city supply-side lead set. Read-only, no role/paywall/API change. | PR open (feat/crm-city-filter-map) |
| BE-NF-36 | **Çevrimdışı check-in — `checked_in_at` kimin saati?** | Mekânın interneti gittiğinde kapı çalışmaya devam etmeli: istemci taramaları yerelde kuyruklar, bağlantı gelince gönderir. Ama o zaman `checked_in_at` **istemciden** gelmeli (sunucu kapı penceresine kırpar, gelecekteki saatleri reddeder) — yoksa damga, kişinin içeri girdiği anı değil telefonun yeniden bağlandığı anı kaydeder. Bu, bu yüzeyin var olma sebebi olan sayıyı bozar. Bugün `checked_in_at` yalnızca sunucu tarafında `now()`. **Bunu kazara karara bağlamayın:** alanı sonradan eklemek bir migration; toplanmış satırların anlamını değiştirmek migration'ın çözemeyeceği bir şey. Karar mobil çevrimdışı desteği yazılmadan önce verilmeli. | Ürün + mühendislik kararı bekliyor |
| BE-NF-31 | **Tailwind CDN'i build'e taşı** | Layout kullanan her pazarlama sayfası `cdn.tailwindcss.com` yüklüyor — bu bir stil dosyası değil, tarayıcıda CSS **üreten** JIT derleyici: indirilip çalışana kadar sayfa stilsiz (FOUC), sonra CLS ve gecikmiş LCP. Tailwind'in kendi dokümanı üretimde kullanmayın diyor. Altyapı hazır: `vite.config.js` + `@tailwindcss/vite` + `resources/css/app.css` (v4, `@theme`) zaten kurulu, ama **hiçbir view `@vite` kullanmıyor**. İş: layout'un satır içi JS `tailwind.config` temasını `@theme`'e taşı, `@vite` ekle, CDN'i kaldır. **Neden bu turda yapılmadı:** CDN v3 sözdizimi, build v4 — varsayılanlar farklı (border rengi, ring, preflight, `rounded-*` ölçüleri) ve bu oturumdan erişilebilen tarayıcı yerel sunucuya bağlanamıyor, yani görsel regresyonu göremem. Tarayıcı erişimi olan biri yapmalı; tek başına en büyük ikinci performans kazancı. | Analiz tamam — görsel doğrulama gerekiyor |
| BE-NF-32 | **Blog içeriği üretimi (motor hazır, yazı yok)** | Blog motoru BE-NF-16'da yazıldı, admin CRUD çalışıyor, tek yazı yayınlanmadı. `/blog` artık boşken `noindex` (BE-FX-19) ama bu bir çözüm değil, sadece zarar durdurma. Önerilen ilk beş konu: bir kafenin sessiz salı gecesini koşu kulübüyle doldurması (vaka, gerçek rakamlarla), topluluk liderleri için sponsorluk teklifi şablonu, Barcelona'da işletmeyle çalışmaya açık topluluk türleri, bir kolab'ın gerçek maliyeti, etkinlik sonrası ne ölçülür. İçerik üretimi işi — mühendislik değil. | Beklemede |
| BE-NF-33 | **İspanyolca pazarlama sayfaları** | Ürün Barcelona merkezli ama İspanyolca yalnızca üç sayfada: `/es/pricing`, `/es/privacy`, `/es/terms`. `/es` ana sayfası, `/es/for-businesses`, `/es/for-communities` ve Katalanca'nın tamamı 404 — aramanın yapıldığı dilde hedef sayfa yok. hreflang altyapısı mevcut üç sayfada doğru çalışıyor, yenileri ona takılır. Sıra: `/es` ana sayfa → iki hedef sayfası → Katalanca. Çeviri işi. | Beklemede |
| BE-NF-30 | **Opt-out (or opt-in) for the public profile page** | `kolabing.com/p/{slug}` (BE-NF-29) publishes every business/community profile with a completed collaboration to the open web — name, city, rating, three photos and one anonymised review quote — and lists them in `sitemap.xml`. Today there is **no way for a profile to decline**. That was a deliberate acquisition choice, but it is a privacy decision that deserves a switch: a `public_page_enabled` column (default on, editable from /account) plus `noindex` + a 404 when it is off, and removal from the sitemap. Also worth deciding whether a profile with no reviews and no photos should be `noindex` regardless, since a thin page dilutes the rest of the site. | Needs a product decision |
| BE-NF-10 | **SMS notification channel** | Transactional SMS (Twilio/Vonage) alongside push: application accepted, kolab scheduled/reminder, check-in. Needs provider integration + per-user phone capture/verify + channel preference. **[VERIFY with Daniel]:** provider, trigger events, opt-in, cost ceiling. | Not started |
| BE-NF-15 | **Scale audit & query optimization** | List endpoints issue O(N) queries/page (`EventResource` ~3 counts/event; `ChatService::visibleThreads` per-thread unread count; unpaginated `GET /chats`; non-index-friendly `COALESCE` time filter). Instrument → seed at scale → k6 load-test → fix via `withCount`/eager/grouped counts, cursor pagination + caps, covering indexes, chunked fan-out. Ticket: `docs/tickets/2026-06-05-backend-scale-audit-optimization.md`. | Spec ready — not started |
| BE-NF-18 | **Kolabing Web App** (epic #128, plan `docs/plans/2026-08-15-kolabing-web-app-plan.md`) | Web client on the existing `/api/v1` (same flow as mobile): community/business login+register, buy+manage subscription, Kolab CRUD, feed for paid users; acquire+sell on web then nudge to the app (slight nudge after purchase). Arch = Blade + Alpine + axios in kolabing-v2, same-origin `kolabing.com/app` (token auth, no CORS/session rewrite needed). Backend enablers: (1) **web Google login** — `GOOGLE_CLIENT_ID_WEB` audience in `GoogleAuthService`, branch `feat/web-google-login` ✅ built; (2) buy = PR #127; (3) `POST /me/subscription/portal` Billing Portal (after #127); (4) paid-feed blur (closes ROLES §4). | In progress — enabler 1 (PR), rest queued |
| BE-NF-25 | **Web App — i18n deep pages (100% es/ca)** (epic #128, stacked on BE-NF-24) | ✅ BUILT (branch `feat/web-app-i18n-deep-pages`): completes the localization — every remaining authed page (feed, my-Kolabs, Kolab detail, create/edit form, applications, account, **subscription**) now renders es/ca copy via `__()`/`window.t()` (added a `subscription.*` block to the 3 dictionaries; status/intent labels + counts localised with `:param` interpolation; Stripe return URLs kept locale-aware). i18n is now 100% across the web app. Test asserts `/es/feed` + `/ca/account` render localised. **PENDING:** merge (stacked chain #132→…→#136→this). | Built (PR, stacked) — pending merge |
| BE-NF-24 | **Web App — i18n (es/ca) + SEO locale URLs + responsive polish** (epic #128, stacked on BE-NF-23) | ✅ BUILT (branch `feat/web-app-i18n-seo`): Spanish + Catalan localization with **SEO-friendly URL prefixes** (`/es/…`, `/ca/…`; en at root) — `config/webapp.locales`, `SetWebappLocale` middleware, dual route registration (root + `{locale}` where es\|ca), **hreflang + canonical + `<html lang>` + per-locale meta** (public login/register indexable, app screens noindex), a **language switcher** + **responsive nav** (mobile menu). Full `lang/{en,es,ca}/webapp.php` dictionaries; a webapp View composer shares `$base`/locale URLs; a JS `window.t()` + `KB_BASE`-aware navigation (locale-safe path parsing on detail/form). **Public pages (index/login/register/welcome) + dashboard + shell fully localized.** Feature test covers /es//ca routing + hreflang. **REMAINING (BE-NF-25 follow-up):** localize the deep authed-page copy (feed/kolabs/detail/form/applications/account) — their URLs + navigation already work under /es//ca (locale persists), strings render en until converted. | Built (PR, stacked) — pending merge; deep-page copy = follow-up |
| BE-NF-23 | **Web App — image uploads** (epic #128, stacked on BE-NF-22) | ✅ BUILT (branch `feat/web-app-media-uploads`): a `kb.uploadFile(file, folder)` client helper (`POST /uploads`), a **Kolab cover photo** picker on create/edit (folder=`kolabs` → `media[{url,type:image,sort_order:0}]`, prefilled from existing media), and a **logo/photo uploader** on /account (multipart `PUT /me/profile` with `_method` spoof, folder=`profiles`). Removes the "photos are app-only" gap for the common web flows (venue-promotion create with a full gallery still routes to the app). View-only. **PENDING:** merge (stacked chain #132→#133→#134→this). | Built (PR, stacked) — pending merge |
| BE-NF-22 | **Web App — profile edit + applications** (epic #128, stacked on BE-NF-21) | ✅ BUILT (branch `feat/web-app-profile-apply`): **/account** (edit profile via `PUT /me/profile` — business: name/about/business_type/categories/city/instagram/website; community: +type/size/tiktok; photo stays in-app), **/applications** (role-aware: community sees sent apps + withdraw via `GET /me/applications`; business sees received + accept(`scheduled_date`)/decline via `GET /me/received-applications`), and the **apply flow** on the Kolab detail page (community → `POST /kolabs/{id}/applications` {message, availability≥20}; owner → View applications; business viewer → open-in-app). Nav gains Applications + Account. `WebAppRoutesTest` extended. **PENDING:** merge (stacked chain #132→#133→this). | Built (PR, stacked) — pending merge |
| BE-NF-21 | **Web App — Phase 1b (feed + Kolab CRUD)** (epic #128, stacked on BE-NF-20) | ✅ BUILT (branch `feat/web-app-feed-kolabs`): the `app.kolabing.com` app gains real pages (replacing the coming-soon shells): **feed** (`GET /kolabs` with search/city/intent filters + save/unsave), **my Kolabs** (`GET /kolabs/me` + status tabs + publish/close/delete; publish 402 → routes to /subscription), **detail** (`GET /kolabs/{id}` + owner actions + save + apply-in-app), **create/edit** (`POST`/`PUT /kolabs`, driven by the real `CreateKolabRequest`: community users → `community_seeking` (needs/typical_attendance/offers_in_return via `/lookup/*`); business → `product_promotion` (product_name/type/offering); venue promotions need photos + a venue profile → gated to the app). Blade + Alpine, all against the existing API. `WebAppRoutesTest` extended. **PENDING:** merge (stacked on #132); can't e2e-verify the create→publish round-trip headless (needs the live app.kolabing.com). | Built (PR, stacked on #132) — pending merge |
| BE-NF-20 | **Web App — Phase 1 frontend (foundation + auth + sales)** (epic #128) | ✅ BUILT (branch `feat/web-app-foundation-auth-subscription`): host-scoped web app at `app.kolabing.com` (`config/webapp.php` + `Route::domain` group in `routes/web.php`, registered before marketing so the app host wins at `/`; other hosts get the marketing site). Blade shells + CDN Alpine (matches the marketing site's CDN convention — no npm/Vite change) + an inline same-origin API client (bearer token in localStorage, one transparent refresh on 401 — same flow as mobile). Pages: index (auth-route), login (email/password + Google w/ business/community), register (Google sign-up), dashboard, **subscription (buy €49/€129 via checkout + manage via portal — the sales surface)**, welcome (post-purchase app-handoff nudge), kolabs/feed coming-soon shells. `WebAppRoutesTest`. Degrades gracefully before `GOOGLE_CLIENT_ID_WEB` is set (Google hidden, email/password works). **PENDING:** merge; Phase 1b = Kolab CRUD + live feed pages (need the Kolab/Discovery resource shapes); depends on the #127/#129/#130/#103 endpoints being live to function. | Built (PR) — pending merge + Phase 1b |

---

## 🚧 Incomplete Features
_Started or partially shipped; not yet fully working end-to-end._

| # | Feature | What's done / what's missing | Status |
|---|---------|------------------------------|--------|
| BE-NF-46 | **Community dashboard brought up to the business one** | ✅ BUILT (branch `feat/community-dashboard-parity`, issue #226). `getCommunityDashboard()` returned 3 keys to `getBusinessDashboard()`'s 7. Now also `opportunities`, `applications_received` (see BE-FX-29) and a community-shaped `next_action` whose first step is **apply**, not publish — browsing and applying are free (ROLES §3.5), so leading with "create a Kolab" would push the rarer path at the paywall-free role. Panel: Next-up card (suppressed while the profile-strength meter is up, since the two measure the same advice differently), a `received` tile that only appears once it has something in it, a community summary card built from stats the shell already fetched for the nav badge, and `?sub=sent|received` on `/kolabs`. Bonus: `next_action` is now translated by `key` in the panel with the server string as fallback, which also fixes the **business** card being English-only in ES/CA. Additive contract only; no schema change. 26 new tests. **Missing:** not seen in a browser. | PR open |
| BE-NF-45 | **Run a collaboration from the web panel** | ✅ BUILT (branch `feat/collaboration-lifecycle-web`, issue #224). Found by an API-vs-panel audit: the panel made **no `/collaborations/{id}` call of any kind** — it fetched the list once and stopped, leaving six existing endpoints unreachable from a browser (`activate`, `complete`, `cancel`, `completion`, `review`, `feedback`). New page at `/collaborations/{id}`: stage rail, start/cancel, the required yes/not_yet/no completion confirmation with **both** answers visible (so the three `/complete` refusals read as states, not as errors), the 5-star public review, and the private role-shaped impact form. Linked from My Kolabs rows and the dashboard's Upcoming cards, which were dead ends. No new endpoints, no schema change, no permission moved; §2.8 re-gate driven by the server's `viewer_must_resubscribe`. 29 new tests (20 page + 9 payload contract). **Missing:** not yet seen in a browser, and challenges + QR generation are deliberately out of scope. | PR open |
| BE-NF-44 | **Public marketplace + "what's on" redesign (Luma-referenced)** | Volkan 2026-08-22, with reference shots: photo-led cards, real descriptions, and a UI that pushes a visitor toward becoming an **attendee** rather than a marketplace browser. Blocked on nothing technical; `/kolabs` is switched off in the meantime (BE-FX-24) so the redesign should be aimed at the attendee surface first. | 📋 TODO |
| BE-NF-43 | **Attendee tickets + the attendee panel** | ✅ BUILT (branch `feat/public-kolab-feed`). The chain BE-NF-40 asked for now exists. `ApplicationService::accept()` creates the happening via the new `CollaborationHappeningService` (one `events` row per collaboration, **public** when a date is agreed); taking a seat mints a 10-character `ticket_code` on the `event_signups` row and queues `EventTicketMail`; the ticket's QR encodes `/admit/{code}` and `POST /tickets/{code}/admit` lets the holder in, writing the same `event_checkins` row the old door writes. No tickets table — a ticket *is* the sign-up plus a code, so it can never disagree about whether someone has a seat. Authorisation inverts at the door: only the **host** may admit, so a leaked code is not entry. **The panel is done too (2026-08-22):** register is account-only and hands off to `/onboarding/attendee`, which runs the **same four steps as the mobile app** (You → City → Interests → Join; only step 1 required; live handle check with suggestions on a 400 ms debounce; empty optionals omitted so a re-run never clobbers; photo as a data-URI) — the web flow used to collect name + handle only, so a web attendee had no city, interests, communities or photo while a mobile one had all four. Plus an attendee dashboard (next ticket, then what's on in their city), the `/tickets` wallet and the host-side `/admit/{code}` door. Nav is role-shaped: no Explore/Kolabs/Suggestions/plan/Create for an attendee; Chats stays because community channels are member-based (§8.4). 54 tests (15 + 9 backend, 30 panel). Docs: ROLES §7.5, map §22. **Missing:** the public "what's on" redesign (BE-NF-44), and a members-side Communities page (mobile has one; web does not). | ✅ BUILT — awaiting merge |
| BE-NF-42 | **Explore as an agenda + the Kolab drawer** | ✅ BUILT (branch `feat/public-kolab-feed`). Luma-inspired, on Volkan's reference: Explore groups Kolabs by **the soonest day each can actually be booked** (no fixed window → "Open now", first), the card leads with the offer and names the poster underneath, and opening one slides a drawer in from the right — the list keeps its place and two arrows walk it. The drawer carries what the old modal did not: a date tile, the trade split into *on offer* vs *looking for*, an action card that says where you stand before it offers a button, copy-link (public URL when there is one), and the poster. **No gate, query or endpoint changed.** Side effect worth knowing: the bookable-date rule had been duplicated between the apply picker and nothing else; it is now one helper (`window.kbNextDates`) that the picker, the rail and the drawer all call. Docs: ROLES §3.3, map §21. | ✅ BUILT — awaiting merge |
| BE-NF-41 | **Kolabs on the open web (`kolabing.com/kolabs`)** | ✅ BUILT (branch `feat/public-kolab-feed`, issue #204). The marketplace is the product, and the marketing site never showed a single real Kolab. Now: `/kolabs` (city + intent filters), `/kolabs/{slug}` in full, and a live strip on the homepage — one gate (`PublicKolabFeedService::publishable()`) shared by list, detail and `sitemap.xml`, so a URL the sitemap advertises can never 404. The identity rule is the delicate part and lives in one place (`PublicKolabPoster`): a **business** that posted is named (§3.3 — never blurred), a **community** is only *described* ("A run club in Barcelona"), because printing the name would let a free business defeat the §2.5 Explore blur by logging out. Applying still needs an account — the wall moved from reading to acting. Also fixed here: `kb.requireAuth()` threw the destination away, so every `?apply=1` / `?rsvp=1` hand-off died at sign-in. **Missing:** `indexable` is deliberately `false` until BE-FX-24 clears the production test rows. | ✅ BUILT — awaiting merge |
| BE-NF-39 | **Two-sided event suggestion system** (spec `docs/superpowers/specs/2026-08-19-event-suggestion-system-design.md`, plan `docs/plans/2026-08-19-event-suggestion-system-plan.md`) | Proposes a partner **and** a concrete event to both sides, from data the platform already holds: community event history and cadence, real collaboration outcomes (`posts_reels` / `stories_posted` / `would_collaborate_again`), business offering and venue capacity, and the existing category-fit matrix. **BUILT (13 of 18 planned tasks), branch `feat/event-suggestion-system-impl`:** `kolab_suggestions` table + model/factory + `config/suggestions.php`; `CategoryFitMatrix` and `OfferTypeAliases` extracted from `DiscoveryOpportunityService` so Explore and the engine share one table (Explore's 47 discovery tests unchanged throughout); `SignalScorer` (six weighted signals, weights renormalised over the signals that actually have data, with an honest `confidence` band); `FormatSuggester` (weekday/time from real cadence, attendance capped by venue capacity, no invented numbers); `PairCandidateFinder` (the only class that queries — 8–9 queries per profile, provably candidate-independent); the nightly `app:generate-suggestions` command + per-profile job at registration and profile completion, with retention pruning; three API endpoints with an IDOR policy and **server-side identity blur** for non-subscribed businesses; `suggestion_id` on `POST /kolabs` closing the conversion funnel; PostHog telemetry tagged per audience; and the `/suggestions` web page, pre-filled create form, `?reason=suggestion` banner and dashboard entry point, all at 100% en/es/ca. **MISSING:** the weekly digest (command + `NotificationType::SuggestionsReady` + two Postmark templates), the ROLES §2.13 / backend-map §15 doc sections, and `paywall_hit` / `subscription_started` telemetry — without those last two events the spec's sales chain stops at `suggestion_converted`. Ships dark behind `SUGGESTIONS_ENABLED=false`. | Built (13/18) — PR pending; digest + docs are a follow-up |
| BE-NF-38 | **Invite page: make it work + join form** | ✅ BUILT (branch `fix/invite-page-join-form`, spec `docs/superpowers/specs/2026-08-20-invite-join-form-design.md`). **The `/c/{slug}` page shipped in BE-NF-34 never worked** — its CTA lived in `<template x-if>` blocks on the marketing layout, which loads **no Alpine**, so a browser painted an empty call-to-action. Second, independent cause: `AddSecurityHeaders` grants `'unsafe-eval'` (which Alpine needs for every `x-*`) and `accounts.google.com` **only** to the app host, so it could not have run there anyway. The BE-NF-34 test asserted the markup was present — it is, inside inert `<template>` tags. Fix: **the page moved to the app host** (`invite_base_url` → `https://app.kolabing.com/c`, 301 from the old path preserving `?i=`), chosen over relaxing the marketing CSP. Also fixed a latent binding bug: the route is registered at `/c/{slug}` and `/{locale}/c/{slug}`, so a positional `$slug` argument picked up the locale — now read by name. **New:** a signed-out invitee joins in place — Google sign-up (`user_type: attendee`, §8.1 D4) → name / phone / photo form (`PUT /me/profile`, all fields already accepted) → join → a success state pointing at the app, since the web has no attendee surface. New test asserts the page **can run** its Alpine, not merely that the markup exists. No schema change. es/ca at 100% parity. **PENDING:** merge. | Built — pending merge |
| BE-NF-37 | **Phone-frame preview of the app profile** | ✅ BUILT (branch `feat/mobile-phone-preview`, spec `docs/superpowers/specs/2026-08-20-mobile-phone-preview-design.md`). BE-NF-36's Preview tab showed the public profile as a **web page**, but the profile most people see is the one in the Flutter app — so someone reordering a gallery could not tell which photo would lead on a phone. Adds `webapp/partials/phone-preview.blade.php`: a 340×700 device frame pinned beside all four Profile tabs, rendering a read-only replica of `public_profile_screen.dart` (header 180px + yellow gradient, 64px avatar, reputation / about / gallery 112px thumbs / past events 180×220 rail / collaborations / links) from the same `/profiles/{me}/public-profile` payload. `kbPhonePreview()` exposes `refreshPreview()`, called after every gallery and past-event mutation, so the phone is never stale. Gated to `xl` and up. **Known trade-off, documented in backend map §17.5:** this is a second rendering of the profile UI — unavoidable since a Flutter screen cannot be embedded — so it is kept read-only, in one file, with the three mirrored Dart files named. No API, schema or role change. es/ca at 100% parity. **PENDING:** merge. | Built — pending merge |
| BE-NF-35 | **Admin CRM: community lead-management (pipeline + activity)** | Turns `/admin/crm` from a data list into a worked pipeline (interactive design spec = the approved lead-management preview). **M1 (this PR, `feat/crm-pipeline-lifecycle`):** canonical community stages `Target→Contacted→Interested→Negotiating→Onboarded` + a terminal **Rejected/Dead** lane, held on `crm_accounts.status`; new **`crm_activities`** table + `CrmActivity` model + `CrmPipelineService` (every stage move / note writes an immutable timeline row and refreshes `last_activity_at`); lead **detail page** `GET /admin/crm/{account}` (contact block, forward-funnel stepper, advance + set-any-stage, activity timeline with add-note); index community name → detail link, community `status` → coloured stage pill. Routes `POST /crm/{account}/stage` + `/crm/{account}/activity`. 9 tests (7 feature + 2 unit), pint clean; box lacks `pdo_sqlite` → suite runs in CI. **M2 (`feat/crm-pipeline-board`):** Kanban **pipeline board** at `GET /admin/crm/board` — one column per stage (incl. Rejected), community cards grouped by `currentStage()`, **HTML5 drag-and-drop** persisting via a JSON mode added to `moveStage` (`POST /crm/{account}/stage`, `Accept: application/json` → `{ok,id,stage}`); city/owner/confidence/search filters; Table↔Pipeline toggle on the index. 4 feature tests. **M3 (`feat/crm-firsttouch-export`):** one-click **first-touch** draft on the detail page (deterministic, personalised from the lead; “Log as sent” records a `first_touch` activity + moves Target→Contacted) via `POST /crm/{account}/first-touch`; **CSV export** of the filtered community set (`GET /crm/export`, streamed); **funnel counters** on the index (leads per stage + Onboarded rate, click-to-filter); **column reorder** (picker now toggles visibility AND reorders ▲▼, order persisted — fixed `visibleColumns()`/`saveColumns()` to keep submitted order). 8 feature tests. | M1 merged (#185); M2 (PR); M3 built (PR) — all three shipped |
| BE-NF-36 | **Public portfolio panel — past events, photos, gallery** | ✅ BUILT (branch `feat/profile-portfolio-panel`, spec `docs/superpowers/specs/2026-08-20-profile-portfolio-panel-design.md`, plan `docs/superpowers/plans/2026-08-20-profile-portfolio-panel.md`). Businesses and communities could publish a portfolio but had no way to manage it: no web UI for events or the gallery existed at all. Three defects closed: (1) **the gallery could not be arranged** — `caption` and `sort_order` had existed since the table was created with no endpoint writing either, and `event_photos.sort_order` was only set at insert; new `PATCH /me/gallery/{photo}`, `PUT /me/gallery/order` and `PUT /events/{event}/photos/order`, all sharing one `PhotoOrderingService` rule (foreign ids ignored, omitted ids keep their relative order — a partial list can never hide a photo); (2) **60 past events and 173 photographs were invisible** — the public profile read only `kolabs.past_events` (7 kolabs in prod) while the `events` table held the rest (business 26/76, community 34/97); `buildCommunityPastEvents()` now merges both stores with additive `source`/`source_event_id`/`attendee_count` keys, newest-first, undated last, deduped on name+date in favour of the event-sourced copy, in **two queries regardless of event count** (query-count test); (3) **the light `GET /profiles/{id}` had no portfolio** — now emits `gallery`/`past_events`/`past_events_count` via a narrower `hydratePublicPortfolio()` (the full detail hydration would have added count queries this endpoint does not emit, and would have **404'd every attendee profile** since it throws for them). Web: `/account` becomes a tabbed Profile section (Profile · Gallery · Past events · Preview) with drag-to-reorder, inline captions, chunked uploads matching the 5-per-request cap, a per-event photo manager, a public-profile Preview with copy-link, and a `past_events` repeater on the Kolab form's review step. No new table, no migration. es/ca at 100% key parity. **PENDING:** merge. | Built — pending merge |
| BE-NF-34 | **Community members web panel (Community Hub)** | ✅ BUILT (branch `feat/community-members-hub`, spec `docs/superpowers/specs/2026-08-19-community-members-web-panel-design.md`, plan `docs/superpowers/plans/2026-08-19-community-members-web-panel.md`). The NF-6 members/tiers backend was complete and **unreachable by a human** — mobile has no Community tab and the web app had zero community pages. Fixes five real defects found while building: (1) the roster was `orderBy(created_at)->paginate()` with no search/filter/sort; (2) it never filtered `status`, so soft-**removed** members rendered as members (default now excludes them, `?status=all` opts back in); (3) `POST /communities/{id}/members` 404s for anyone without a Kolabing account, so a leader could not get their real roster in — new `community_invitations` table + endpoints + queued mail + **claim-on-register** (`AuthService::afterRegistration`, guarded); (4) **`Community::inviteUrl()` has always emitted `/c/{slug}` and that route never existed — every invite link ever shared 404'd** — added the public join landing (tier ladder, upcoming *public* events only, `noindex` when invite-only); (5) `CommunityMemberResource` read the extended profile for a display name but `attendee_profiles` has no `name` column, so every member rendered as their email prefix. Also: roster gains per-row engagement (`points`, `events_attended`, `last_active_at`, `tenure_days`) via left-joined grouped aggregates with a **query-count test** proving O(1)-in-members (BE-NF-15's pattern); `GET /communities/{id}/stats`; member-detail drawer endpoint; bulk tier/status (cross-community ids skipped, never written); and the roster is **now manage-gated** because it carries emails. Web: seven-tab hub at `/community` (Overview / Members / Requests / Tiers / Economy / Leaderboard / Settings), nav gated on **owner OR `can_manage`** (never `user_type` — managers are attendee accounts, ROLES §8.3 D1). `kb.rows()` fixed: it read only `data`/`data.data`, so the roster's `data.members` envelope silently rendered empty (the BE-NF-21 bug class). es/ca at 100% key parity. ROLES §8.7 + backend map §12.6 written; `CommunityNeverPaywalledTest` is a standing §8.4 guard. **DEPLOYED 2026-08-19** (PR #178 → master → Laravel Cloud): `community_invitations` migrated on prod, `/community/*` and `/c/{slug}` verified live (a real community slug renders name + tier ladder + CTA), queue worker confirmed draining (0 pending jobs). Outbound mail confirmed healthy: `POSTMARK_API_KEY` is set on prod and there have been **zero** `failed_jobs` since 07:27 on 2026-08-19 — through and after this deploy. Optional: `COMMUNITIES_INVITATION_TTL_DAYS` (defaults to 30). **Deferred by decision:** CSV import/export, attendee registration on web, tier-permission *enforcement* (ROLES §8.3 D3 keeps it Phase 2). | Deployed & verified end-to-end |
| BE-NF-28 | **Chat in the web panel (app.kolabing.com)** | **DONE:** `/chats` two-pane inbox in all three locales, `Messages` nav + unread badge, read/send across every thread type (Kolab / community main / custom channel / event), channel create-rename-delete + member block list, deep links from My Kolabs and notifications, self-hosted Pusher-protocol client with a polling fallback, `wss:` in the web-app CSP. 17 new tests (12 shell + 5 API), and every call the page makes replayed against a live server. **MISSING:** only the CSS has not been eyeballed in a browser at desktop or ≤900px. The socket path was **verified end-to-end on 2026-08-18** (see BE-IF-18): the panel's client config already resolves to Laravel Cloud's managed Reverb, that endpoint accepts `Origin: https://app.kolabing.com`, and the full handshake → auth → subscribe → `message.sent` loop was exercised against a local daemon. No code change is needed for real-time to light up on deploy. Branch `feat/webapp-chat`. | Shipped on polling — real-time blocked on BE-IF-18 |
| BE-NF-29 | **Public profile pages (panel + open web)** | **DONE:** `app.kolabing.com/profiles/{id}` in all three locales — identity, verification tick, contact links, about, five-category rating breakdown, paginated reviews *with their authors* linking onward, past events with photos, paginated past collaborations, gallery + lightbox, own-profile edit/share; entry points from Explore, the Kolab overlay, application rows, collaboration partners, notification actors, the chat header, the sidebar and /account. Public teaser at `kolabing.com/p/{slug}` with JSON-LD, canonical, sitemap inclusion and a sign-up wall enforced by absence from the HTML. Additive API: `GET /profiles/{id}/public-profile` (both roles), `public_url`, `actor_profile_id`, participant `id`. 29 tests. **MISSING:** no browser pass on either page (the Chrome reachable from this machine cannot load its local server), and the opt-out question in BE-NF-30 is unanswered. Branch `feat/public-profiles`. | Shipped in code — needs a visual pass + BE-NF-30 decision |
| BE-NF-37 | **Attendee'nin web yüzeyi: herkese açık etkinlik akışı + kayıt + RSVP** | **DONE:** Attendee'nin bugüne kadar webde hiç girişi yoktu — tüm yüzeyi yayınlanmamış mobil uygulamadaydı ve kayıt ekranında rol olarak bile yoktu. Eklenenler: **`kolabing.com/events`** (herkese açık, indekslenebilir akış; şehir filtresi yalnızca dolu şehirleri listeliyor) ve **`/events/{slug}`** (tek etkinlik: `Event` JSON-LD + `isAccessibleForFree`, fotoğraflar, aynı şehirde başka etkinlikler). Kapı yalnızca `EventVisibility::Public`'e açık ve bu **iki yerde** zorlanıyor — listeleme *ve* çözümleme; "listelenmiyor" ile "okunamıyor" ayrı özellikler ve özel bir topluluğun takvimi slug tahminiyle okunamamalı. Detay açık, duvar eylemde: **"I'm going"** panele `?rsvp=1` ile devrediyor, çünkü herkese açık origin bearer token'ı okuyamaz; login niyeti `?next=` ile taşıyor. Kayıt ekranına üçüncü rol kartı geldi (iki çağrı: `register/attendee` + `onboarding/attendee` — ad + handle, satıcı detayı sorulmuyor) ve panelde ev sahibi olmayan kullanıcı artık RSVP / bekleme listesi / iptal görüyor. **Yeni endpoint ve şema değişikliği yok** — `EventDiscoveryService` (etkin şehir kuralı dahil) ve `EventSignupService::assertEligible()` (public etkinlik üyelik istemiyor) zaten doğruydu, yeniden yazılmadı. 16 yeni test. **MISSING:** hiçbir sayfa tarayıcıda görülmedi. ROLES §7.5 + backend map §18. | Kodda tamam — görsel geçiş bekliyor |
| BE-NF-35 | **Etkinlik + kapıda check-in yüzeyi (panel + mobil senkron)** | **DONE:** Check-in API'si 13 uç noktayla yazılmıştı ama **hiçbir istemcisi yoktu** — etkinlik, kayıt, QR ve check-in yalnızca mobildeydi, iki mağazada da yayınlanmamış uygulamada. Panelde artık: `/events` (yaklaşan/geçmiş, kayıtlı–gelen–katılım oranı, modal ile etkinlik oluşturma), `/events/{id}` = **kapı** (ekrana basılan QR, elle yazılabilir 8 karakterlik kod, 6 saniyede kendini yenileyen sayaç, gelenler listesi), ve `/checkin/{token}` = QR'ın açtığı tek ekranlık katılımcı sayfası — giriş yoksa `?next=` ile login'e gönderip **geri getiriyor**. QR, **bağımlılık eklenmeden** yazılan bir kodlayıcıyla (`App\Support\QrCode`, bayt modu / ECC-M / sürüm 1–10) sunucuda SVG olarak üretiliyor ve JSON'la geliyor; panel bearer token kullandığı için `<img src>` kimlik doğrulayamaz. Güvenlik: token artık **süreli** (etkinlik penceresi + 1 saat) ve token/kod/QR **yalnızca ev sahibine** dönüyor — ikisi de "buradaydım" iznidir. 22 yeni test; QR kodlayıcı, bağımsız bir çözücünün (jsQR) okuduğu dondurulmuş matrislerle doğrulandı. **Mobil senkron (2026-08-22):** aynı yüzey mobilde de açılacağı için sözleşme sabitlendi — URL'i kuran tek yer `CheckinLink` (istemci `checkin_url`'i render eder, kendi URL'ini kurmaz), **kapıyı açmak artık idempotent** (bir gün canlı kalan tuzak: ev sahibi ikinci cihazda açınca ilk ekrandaki QR sessizce ölüyordu; retire etmek artık `rotate: true` ve panel önce onay istiyor), gelenler `private-event.{id}.door` kanalında **yayınlanıyor** (varış + yürüyen toplam, düşen mesaj sayacı kaydırmasın diye) ve polling yedek olarak kalıyor, kanal yetkisi yeni `Event::isHostedBy()` ile — sahiplik kontrolü üç yerde tekrarlanıyordu. App host'una Universal Links / App Links dosyaları eklendi: tek QR, uygulama kuruluysa uygulamayı, değilse tarayıcıyı açıyor; mobil kimlikler set edilene kadar ikisi de 404 (Apple association dosyasını cache'liyor, placeholder beklemek zorunda bırakır). **MISSING:** hiçbir sayfa tarayıcıda gözle görülmedi (bu ortamdan erişilebilen Chrome yerel sunucuya bağlanamıyor) ve kapı gerçek bir telefonla ucu uca hiç denenmedi — QR'ın *okunduğu* kanıtlı, ama "tarayıcıda aç → check-in oldu" akışını bir insanın bir kez yapması gerekiyor. Dal `feat/event-checkin-qr`. | Kodda tamam — bir telefonla saha testi gerekiyor |
| BE-IF-18 | **Real-time chat — Reverb Part A (ops)** | **The ticket is largely already done and its "MISSING" list is stale.** Verified 2026-08-18: `.env` carries real credentials for **Laravel Cloud's managed Reverb** (`REVERB_HOST=ws-a0f4ad70-…-reverb.laravel.cloud`, `REVERB_PORT=443`, `REVERB_SCHEME=https`, `BROADCAST_CONNECTION=reverb`, `QUEUE_CONNECTION=database`), so there is no self-hosted daemon or nginx/TLS work left — the managed instance terminates TLS on 443. A handshake against that endpoint with `Origin: https://app.kolabing.com` returns `pusher:connection_established` (`activity_timeout: 30`), so `REVERB_ALLOWED_ORIGINS` already covers the web panel; a no-Origin connection is correctly rejected `4009`. The whole loop (handshake → `/broadcasting/auth` → `pusher:subscribe` → `message.sent` with the right `thread_id`) was exercised against a local `reverb:start` + `queue:work`. **STILL OPEN:** confirm a **queue worker daemon runs in production** — `NewChatMessage` is `ShouldBroadcast` and the queue is `database`, so with no worker every broadcast is enqueued and never delivered (the ticket calls this the #1 cause of "it's silent"); and confirm the Flutter client (app IF-18) has been handed the key and switched on. Ticket: `docs/tickets/2026-06-09-reverb-realtime-chat-PART-A-ops.md` — its env/daemon/TLS steps should be rewritten for managed Reverb. | Deployed — verify the prod queue worker + flip the Flutter client |
| BE-IF-47 | **Legacy `collab_opportunities` → Kolab consolidation** | PR #32 removed table-level legacy code (archive table), but the app still routes System-A edits to legacy rows and the split persists conceptually. Full data migration + route/model consolidation remains open-ended (needs a migration plan). App audit item #47. | Partial — migration plan needed |
| BE-NF-27 | **Marketing → web-app funnel (kolabing.com → app.kolabing.com)** | ✅ BUILT (branch `feat/marketing-webapp-funnel`, stacked on `feat/kolabing-web-app`): the marketing site had **zero links to the web app** — the homepage hero's App Store/Google Play buttons, the nav "download" button and both final-CTA buttons were all `href="#"` or in-page anchors, the shared `marketing-page` layout header had no CTA at all, and on mobile the whole nav was `display:none` behind a hamburger that had no JS. Added `config('webapp.url')` (`WEBAPP_URL`, default `https://app.kolabing.com`) as the single source for cross-host CTAs and wired every surface: homepage nav (`log in` + `get started — free`, now visible on mobile; dead hamburger removed), hero (dead store badges → one `start free` pill + log-in link; badge `iOS + Android` → `free · works in your browser` since neither app is published), final CTA (`?type=business` / `?type=community`, which the register page already reads to skip its role-picker step), a mobile sticky bottom CTA bar that slides in after the hero, a `Create your free account` link in both newsletter pop-up states, the shared layout header + footer (covers blog/support/careers/privacy/terms), role-prefilled CTA bands on `/for-businesses` + `/for-communities`, and an end-of-article CTA on blog posts. New `tests/Feature/Marketing/WebAppFunnelTest.php` (8 cases); `MarketingSeoTest`'s `href="#cta"` assertion updated (that nav slot is now the web-app CTA). **PENDING:** set `WEBAPP_URL` on prod; marketing site stays en-only (no links to the web app's `/es` `/ca` prefixes yet); `/privacy` + `/terms` (+ `/es` copies) still describe subscriptions as Apple-IAP-only and need a Stripe paragraph now that web checkout exists. | Built — pending prod env + legal copy |
| BE-IF-48 | **Landing-page newsletter + book-a-call pop-up** | ✅ BUILT: `newsletter_subscribers` table + `NewsletterSubscriber` model + `NewsletterAudience` enum + `NewsletterSubscribeRequest` (email + optional community/business audience + honeypot) + `NewsletterController@store` (idempotent per email) + `POST /newsletter` (throttle 10/min) + pop-up UI in `welcome.blade.php` (exit-intent/18s/45%-scroll, once per session, segmented audience, book-a-call CTA). 7 feature tests green, pint clean. **PENDING:** run migration on prod; set `KOLABING_BOOK_A_CALL_URL` env to the real Cal.com link (default is a placeholder). | Built — pending deploy + real Cal.com link |
| BE-NF-16 | **Blog engine + SEO/GEO** | ✅ BUILT (branch `feat/blog-engine-seo-geo`): `blog_posts` table + `BlogPost` model/factory + public `/blog` + `/blog/{slug}` (Blade on the `marketing-page` layout, per-post canonical/OG) + admin `/admin/blog/*` CRUD (maintainer-gated, sidebar entry) + **Article/Organization/WebSite/FAQPage JSON-LD** + homepage `welcome.blade.php` meta/canonical/OG fix + `/sitemap.xml` & `/llms.txt` now include published posts. 2 feature test files (public + admin). Owned by Clark going forward (blog + SEO/GEO workstream). **PENDING:** merge (Volkan) + prod migrate (the `master` deploy runs `migrate --force`); then Phase 2 = first Community-Commerce articles + a Barcelona city page. | Built (PR) - pending merge/deploy + content |
| BE-NF-17 | **Stripe web checkout (the missing web half of the paywall)** | ✅ BUILT (branch `feat/stripe-web-checkout`): implements `source=stripe` for real — previously only `apple_iap` + `maintainer` existed in code. `StripeService` (Checkout Session in `subscription` mode + webhook signature verify, price ids resolved server-side from `config/subscriptions.php`, never client input) + `CreateCheckoutSessionRequest` (business-only, return-URL allowlist = `kolabing://` scheme OR `config('services.stripe.allowed_return_hosts')`, anti-open-redirect) + `POST /me/subscription/checkout` (auth+business, returns `{data:{checkout_url}}`, matches the Flutter `subscription_paywall.dart` contract that was 404ing) + public `POST /webhooks/stripe` (`checkout.session.completed` → `SubscriptionService::activateFromStripeSession`, `customer.subscription.updated/deleted` → sync; upserts `BusinessSubscription` `source=stripe`, rewards referral on first paid sub). No migration (stripe columns already on `business_subscriptions`). 5-case `StripeCheckoutTest` + legacy-endpoint test updated. **PENDING:** merge (Volkan) + set `STRIPE_MONTHLY_PRICE_ID`/`STRIPE_THREE_MONTHS_PRICE_ID`/`STRIPE_SECRET_KEY`/`STRIPE_WEBHOOK_SECRET` on prod + register the webhook endpoint in the Stripe dashboard. Box lacks `pdo_sqlite` → suite runs in CI. | Built (PR) - pending merge + prod env/webhook |
| BE-NF-26 | **Web payment flow completion (sell subscriptions easily)** | ✅ BUILT on branch `feat/kolabing-web-app`, spec `docs/superpowers/specs/2026-08-17-web-payment-flow-design.md`. Closes the six gaps that made the web checkout unsellable: (1) **activation no longer depends on the webhook** — `success_url` carries `{CHECKOUT_SESSION_ID}`, new `POST /me/subscription/checkout/confirm` verifies session ownership (403 for someone else's session, 409 `pending` while unpaid, 502 on Stripe error) and runs the *same* idempotent `activateFromStripeSession` the webhook runs (which now returns the row); new `/subscription/success` page retries on 409 with backoff and never claims success falsely; (2) Checkout Session gains `customer`-or-`customer_email`, **`allow_promotion_codes`** (sales discount campaigns, distinct from the referrer-rewarding referral code) and `locale` (`ca → es`); payload split into `StripeService::checkoutSessionParams()`; (3) two selectable plan cards with a derived `Save 12%` badge (prices + per-month + saving all from `config/subscriptions.php`); (4) paywall hops carry `?reason=publish\|accept\|apply\|welcome` → contextual banner; (5) **`past_due` alert** in the app shell → Billing Portal, backed by additive business-only `subscription_status` + `subscription_cancel_at_period_end` on `GET /auth/me`; (6) public **`kolabing.com/pricing` + `/es/pricing`** (Product/Offer/FAQPage JSON-LD, hreflang, sitemap + llms.txt, CTA → `/register?type=business&plan=…` → preselected checkout). 19 new tests (`CheckoutConfirmTest`, `CheckoutSessionParamsTest`, `PricingPageTest`, + `WebAppRoutesTest`/`SubscriptionControllerTest` extensions); full suite **1486 passed**, pint clean. es/ca at 100%. ROLES docs §2.12 + backend map §14 written. **PENDING:** PR + merge; then the §14.6 prod env/webhook/portal setup — note the confirm endpoint means sales works even before the webhook is registered. | Built — pending PR/merge + prod Stripe setup |
| BE-NF-19 | **Subscription manage/cancel (Stripe Billing Portal)** | ✅ BUILT (branch `feat/subscription-billing-portal`, stacked on `feat/stripe-web-checkout`): `POST /api/v1/me/subscription/portal` (business-only) → `StripeService::createBillingPortalSession` → `{data:{portal_url}}`; Stripe customer id read server-side from the caller's own subscription (no IDOR), 409 when there is no Stripe sub; `return_url` via the shared `ValidatesReturnUrl` trait (also refactored `CreateCheckoutSessionRequest` onto it). `BillingPortalTest` (5 cases) + legacy `/portal` 404 assertion removed. Web-app enabler #3 for #128. **PENDING:** #127 merges first (dependency), then merge this; in the Stripe dashboard enable the Billing Portal + configure cancellation. | Built (PR, stacked) - pending #127 then merge |

---

## 🐛 Fixes
_Backend bugs / gaps. Add when detected; strike through with a date once confirmed, then remove._

| # | Bug / gap | Status |
|---|-----------|--------|
| BE-FX-31 | **Panelin "linki kopyala" butonu, kapalı olan açık-web yüzeyine link veriyordu** — production'dan alınan `kolabing.com/kolabs/{id}` 404 verdi. Kolab'da sorun yok: production'da `published()`, `withSelectableDates()` ve 20 karakter açıklama eşiğinin hepsini geçiyor, ve `PublicKolabLink::resolve()` UUID formunu da çözüyor. Sorun `kolab-modals.blade.php`'deki `copyLink()`: koşulu `k.status === 'published' && marketing` idi, yani `kolabing.public_kolabs.enabled` bayrağını hiç sormuyordu — o bayrak production'da kapalı (BE-FX-24) ve `PublicKolabPageController::assertEnabled()` her iki public route'u 404'lıyor. Marketing tarafındaki bütün linkler (nav, footer, anasayfa şeridi, sitemap) bayrağı zaten sorguluyordu; kontrolsüz tek yayıcı buydu. **Düzeltme:** `KB_CONFIG.publicKolabs` panele veriliyor, public URL üç koşulu birden istiyor, kapalıyken in-app URL'e düşüyor. 3 test (mevcut source-pin testi yeni ifadeye güncellendi + bayrağın iki yönü). | ~~Düzeltildi 2026-08-25~~ — cihazda/tarayıcıda kopyala butonu ile doğrulanması gerekiyor |
| BE-FX-30 | ~~**The web app's consent checkbox could not be ticked — in dark theme only.** The Terms + Privacy box on `webapp/register.blade.php` (business sign-up, step 3) carried `text-ink`, and `webapp/layout.blade.php` loads **@tailwindcss/forms** from the Tailwind CDN. That plugin paints an unchecked box a hard-coded `background-color: #fff` and a checked one `background-color: currentColor` behind a tick that is hard-coded `fill="white"` — both rules written for a light page. Dark theme inverts `--kb-ink` to `243 239 231`, so a ticked box was a **white tick on a near-white fill**, pixel-identical to the unchecked white chip. Nothing was broken in the DOM: the input toggled, `form.accepted_terms` flipped, `x-model` was fine — the control simply never showed it, so a user saw an unchangeable box and then "Create account" refusing with *err_terms*. Light theme was never affected (ink is near-black there), which is exactly why it passed review. Fix: a themed `.kb-checkbox` in the layout binds both states to palette tokens — brand-yellow fill with an `--kb-on-primary` tick, matching the venue cards directly above it — at specificity (0,3,1), which beats the plugin's (0,2,0)/(0,3,0) rules whatever order the CDN injects them in. The guard test then found the **same** `text-ink` checkbox on `account.blade.php`'s notification preferences; fixed in the same change.~~ | ✅ FIXED 2026-08-25 — `WebappCheckboxContrastTest`: a render assertion plus a repo-wide guard that fails on any webapp checkbox carrying a `text-*` colour; 185 WebApp tests green |
| BE-FX-29 | ~~**A community never saw the applications it received.** Communities post Kolabs too (`IntentType::CommunitySeeking`) and businesses apply to them, but `applications_received` was computed only in `getBusinessDashboard()` — despite `getReceivedApplicationStats()` being role-neutral all along (it scopes by `creator_profile_id`). It was simply never called for a community. The sharp edge is on mobile: `CommunityDashboard.fromJson` in `kolabing-app` has parsed `applications_received` since it was written and falls back to `ApplicationsReceivedStats()` — all zeros — when the key is absent, so a community with businesses waiting on an answer was shown "0" with no error anywhere. Fixed by calling the existing helper; guarded by `CommunityDashboardParityTest`.~~ — confirmed 2026-08-25 (BE-NF-46) | ✅ FIXED |
| BE-FX-28 | ~~**The panel's phone preview was showing the app wrong.** `webapp/partials/phone-preview.blade.php` claims to mirror the Flutter public-profile screen and names the Dart files it tracks; it had drifted from all of them, and worse it was fed by the wrong endpoint. It read `GET /profiles/{id}/public-profile` → `CommunityPublicProfileResource`, which carries no `reputation`, no `recent_reviews`, no `completed_kolabs_count` and no `type_label` — so the reputation card read "— · 0 reviews · 0 completed" on every profile however many reviews existed, the "Recent Reviews" card could not exist at all, and the header printed the raw slug (`run_club`) instead of "Run Club". Visual drift on top of that: event cards were a white card with a 120px image (the app draws a 180×220 full-bleed cover with the text over a three-stop gradient, a date badge and a photo-count badge); past Kolabs were three plain titles in a list (the app draws 240-wide cards in a 110 rail with a partner avatar, a month and a green Completed pill, plus an empty state, and it renders the section unconditionally); section titles were 14px where the app uses titleMedium 20/700; counts were plain grey text where the app uses pills; and every secondary colour was a `black/xx` approximation of the app's palette, with the phone body on #FAF7F0 instead of #FAF5EA. Fix: the preview now makes the same **three** calls the app makes (`/profiles/{id}`, `/profiles/{id}/collaborations`, `/events?profile_id=`) and six cards were rewritten off the Dart measurements. Deliberately out of scope, and stated in the spec: attendee profiles (a different screen and a different payload), live-as-you-type preview, and visibility below xl. Numbered 28 because BE-FX-26/27 are taken by the in-flight attendee-panel branch.~~ | ✅ FIXED 2026-08-24 — `WebAppPhonePreviewParityTest` (15 tests) + the WebApp suite green; visual sign-off against a device still worth doing |
| BE-FX-27 | ~~**Every auto-created event was hosted by "Partner".** `CollaborationQrCodeController` set `partner_name` from `$profile->display_name`, an attribute Profile does not have, so the value was always null and always fell back to the literal string. Now resolved off `business_profiles` / `community_profiles` / `profiles.name` in `CollaborationHappeningService`.~~ Fixed 2026-08-22, covered by `CollaborationHappeningTest::test_the_partner_is_named_properly`. | ✅ FIXED |
| BE-FX-26 | ~~**Nobody could ever sign up to a Kolab's happening.** `EventSignupService::signup()` threw `event_not_signup_enabled` for any event with `community_id === null`, and a collaboration cannot reliably have one — `events.community_id` points at the NF-6 `communities` table while a collaboration's community side is a *profile*. So every Kolab happening refused every attendee. Guard is now `community_id === null && visibility !== public`, which still excludes the portfolio/past-event rows it was written for.~~ Fixed 2026-08-22, covered by `CollaborationHappeningTest` + `EventTicketTest`. | ✅ FIXED |
| BE-FX-24 | **Production holds test Kolabs that a public page would publish.** Of the 17 currently-applicable published Kolabs, ~8 are test rows (`test` / desc `ghasgvavgag`, `Clark Biz Test`, `WebBusiness`, `[TEST] Finish-flow collaboration`, desc `tessdfasfsdfsdfs`). Proved unfilterable in code: the worst offenders are *structurally complete* because the app itself generates the description ("Collaborate with {name} to promote our product."), and `profiles.is_test_user` is inverted here — the 4 flagged profiles hold some of the **best** content while the junk sits on unflagged ones. So no query can separate them; a human must. Until then `config('kolabing.public_kolabs.indexable')` ships **false**: `/kolabs` + `/kolabs/{slug}` serve `noindex,follow` and stay out of `sitemap.xml`. Needs: clear/close the test rows, then flip one config value. | 🔴 OPEN — data, not code |
| BE-FX-25 | **`KolabFactory` writes a shape the API would reject.** `needs` / `offers_in_return` are emitted as associative booleans (`['food_drink' => true]`), but `CreateKolabRequest` validates `needs.*` as a *string* from the slug vocabulary and production stores lists (`["food_drink"]`). Any new test relying on the factory's shape silently exercises data the API cannot produce (label lookups render empty rather than failing). Fix the factory to emit slug lists; audit tests that assert on those columns. | 🔴 OPEN |
| BE-FX-20 | **İş birliği QR kodu çalışmayan bir URL üretiyordu.** `CollaborationQrCodeController` şunu kuruyordu: `url("/api/v1/events/{id}/checkin?token=…")`. Üç ayrı sorun: **(a)** o route hiç yok — check-in `POST /api/v1/checkin`, token gövdede; `route:list` yalnızca `POST api/v1/checkin` ve `GET events/{event}/checkins` gösteriyor, yani QR'ı okutan telefon **404** alıyordu. **(b)** GET biçiminde kurulmuş bir link, gövde bekleyen bir POST uç noktasını hiçbir zaman tetikleyemez. **(c)** Sırrı query string'e koyuyordu — sunucu loglarına, tarayıcı geçmişine, referrer'lara sızar. Ayrıca aynı controller etkinliği `'checkin_token' => Str::random(64)` ile elle kuruyordu, bu da `generateCheckinToken()`'ı atlıyor ve ardından gelen `if (! $event->checkin_token)` koruması hiçbir zaman çalışmıyordu — yani kod ve süre hiç atanmıyordu. Artık token üretimi tek yerde (`CheckinService`) ve QR panelin `/checkin/{token}` sayfasını taşıyor. | Düzeltildi 2026-08-21 — BE-NF-35 ile aynı PR'da |
| BE-FX-23 | ~~**`EventDiscoveryService`'s production distance path was never executed by CI.** The service branched on the driver: SQLite got a bounding box plus a PHP calculation, Postgres got an `acos` law-of-cosines expression in SQL. `phpunit.xml` pins the suite to SQLite, so the only branch that ever ran in production had no test — the same structural blind spot as BE-FX-12 (`max(uuid)` passed on SQLite and 500'd on Postgres). **Three real defects were hiding behind that:** (1) the SQLite branch paginated *before* filtering and sorting, so ordering was per-page and `total` counted bounding-box corners; (2) the Postgres `acos` form raises `input is out of range` for an event **at** the query point — a live 500 for a zero-distance match; (3) the radius bind needed `CAST(? AS double precision)`, because PDO binds floats as strings and SQLite ranks storage classes above values, making `179.3 <= '50'` TRUE. Fix: one portable SQL expression (`asin`/`power`, SQLite >= 3.35) for both drivers, so CI now executes the production path; the SQLite branch is deleted. Moving the maths to PHP was considered and rejected — `paginate()` is a COUNT + LIMIT/OFFSET and `radius_km` goes to 200, so PHP would load the whole radius per request.~~ | Fixed 2026-08-22 (tested) — 24 discovery tests + 3 new dialect tests |
| BE-FX-19 | **SEO denetimi düzeltmeleri — kolabing.com** (rapor: 19 Ağustos 2026, 16 sayfa canlı ölçüldü). Sekiz bulgu düzeltildi. **(1)** Ana sayfa **24,58 MB** görsel yüklüyordu — biri tek başına 9,1 MB olan yedi sıkıştırılmamış PNG. Hepsi 1600px WebP'ye çevrildi (**860 KB**, %96,5 azalma), tanımlayıcı isimler verildi, `width`/`height` eklendi (CLS), logo 56 KB PNG → 12 KB WebP; referanssız kalan 52 MB'lık PNG arşivi silindi. **(2)** `www.kolabing.com` 200 dönüyor **ve** canonical'ı kendine işaret ediyordu — iki host bağımsız kanonik, gerçek çift içerik ayrışması; yeni `CanonicalUrl` middleware'i www→apex ve sondaki eğik çizgiyi 301'liyor (query string korunur, POST'a dokunulmaz). **(3)** Ana sayfa hiçbir ticari sayfaya link vermiyordu — tek iç linkleri terms/privacy/support/careers'dı; nav + footer artık for-businesses, for-communities, pricing, blog ve dizine bağlanıyor. **(4)** Her pazarlama isteği `no-cache, private` ile geliyordu (`cf-cache-status: BYPASS`) — `CacheMarketingPage` `s-maxage=300, stale-while-revalidate` veriyor; çerez taşıyan veya 200 olmayan yanıta dokunmuyor (admin login test edildi). **(5)** Boş `/blog` ve **(6)** yayınlanmış şehri olmayan `/communities` indekslenebilir ince sayfalardı; layout'a `noindex` prop'u eklendi ve içerik geldiğinde kendiliğinden geri dönüyorlar, sitemap de aynı kurala bağlandı. **(7)** Sitemap eşiği "tamamlanmış iş birliği"nden "en az bir yorum **veya** üç fotoğraf"a yükseltildi — prod'daki `/p/test-af5c48` seed hesabını eleyen ve yüzlerce ince profil kümesini önleyen kural; eşiği geçmeyen profil sayfası `noindex` oluyor ve puanı yoksa meta açıklama hakkında metninden tamamlanıyor (56 → 155 karakter). **(8)** Boşa giden başlıklar tanımlandı (`Blog` → *Local partnership playbooks*, `Support`/`Careers`/`Pricing` benzeri). 13 yeni koruma testi + mevcut iki test güncellendi. | Düzeltildi 2026-08-19 — **deploy bekliyor**; 1582 test yeşil |
| BE-FX-18 | ~~**Prod outbound email is dead — `POSTMARK_API_KEY` is not set**~~ — **NOT A BUG.** The 13 rows in `failed_jobs` are historical: the most recent is 2026-08-19 07:27, the key was set on prod after that, and there have been **zero** failures since (verified 13:15, i.e. through and after the BE-NF-34 deploy). Logged in error — a stale `failed_jobs` backlog was read as a live outage. Only remaining optional cleanup: retry/prune the 13 old rows. | Withdrawn 2026-08-19 — no action needed |
| BE-FX-17 | **Every hand-written JSON-LD block on the marketing site was invalid — Blade ate the `@context` key.** Laravel 12 ships an **`@context` Blade directive** (for the Context facade), and Blade compiles directives inside `{!! !!}` echo expressions. Four templates built their schema inline as `{!! json_encode(['@context' => 'https://schema.org', …]) !!}`, so the literal `'@context'` was replaced with compiled PHP and the emitted object's first key became `"<?php $__contextArgs = []; if (context()->has(…)) …?>"`. The document still *parsed* as JSON, so nothing looked broken — but with no `@context` the vocabulary is undefined and **Google could not read any of it**: `Organization` on every page via the shared layout, the homepage `@graph` (Organization + WebSite), and `Product` + `FAQPage` on `/pricing` and `/es/pricing`. Verified against production before and after. Fix: build each array in the file's `@php` block (where the compiler leaves directives alone — which is why `blog/show` and the new profile page were never affected) and echo the encoded string. Guard: `tests/Feature/Marketing/StructuredDataTest.php` parses **every** ld+json block on ten marketing URLs and asserts `@context === 'https://schema.org'`, that no key contains raw PHP, and that each block has `@type` or `@graph`; reverting any one template makes it fail. | Fixed 2026-08-19 — **needs deploy**; 14 guard tests green |
| BE-FX-16 | ~~**All transactional email is failing in production — `POSTMARK_API_KEY` is not configured.**~~ **WITHDRAWN 2026-08-19 — same non-bug as BE-FX-18.** I read four `SendTransactionalEmail` failures out of `failed_jobs` (14–18 Aug) and concluded the key was unset in the deployed environment. Those rows were **historical**: the key IS set on prod, and a parallel session verified outbound mail end-to-end (a real BE-NF-34 invitation delivered, no new failures after 07:27 on 19 Aug). Lesson worth keeping: `failed_jobs` is an append-only log, not a statement about the current config — a row proves a job failed once, not that it would fail now. |
| BE-FX-13 | ~~**A Kolab message sent through the generic thread endpoint notifies nobody.** `ChatService::sendThreadMessage()` broadcasts and dispatches `FanOutThreadMessageNotifications`, but `threadRecipientIds()` returns `[]` for `ChatThreadType::Collaboration` (by design — collaboration threads notify via `NotificationService::notifyNewMessage()`, which only the application path calls). So `POST /chats/{thread}/messages` on a Kolab thread stores the message, updates `last_message_at`, and delivers **no in-app notification, no push, and no unread-message reminder** to the other party. Nothing is broken today because both clients post Kolab messages to `POST /applications/{id}/messages` (the web panel does this deliberately, pinned by `WebAppChatPageTest`), but the endpoint is a trap for the next client. Fix: have `sendThreadMessage()` delegate to the collaboration notification path when the thread has an `application_id`, so both routes behave identically.~~ | Fixed 2026-08-22 (tested) — `sendThreadMessage()` delegates to the collaboration path; exactly-once proven on both routes |
| BE-FX-14 | **The §2.8 subscription-lapse re-gate is not enforced for chat.** `ROLES-AND-PERMISSIONS.md` §2.8 says a lapsed business loses access to its ongoing collaborations **and chats** until it resubscribes. `ChatService::canParticipate()` checks only that the application is not declined/withdrawn and that the viewer is the applicant or the Kolab creator — it never looks at subscription state — and the chat routes in `routes/api.php` carry `auth:sanctum` alone, with no subscription middleware. A lapsed business therefore keeps full read/write access to every Kolab chat. Either the doc or the code is wrong; this needs a product decision before either is changed (and if the gate is added, the community counterpart must stay unaffected — re-gating is one-sided). | Detected 2026-08-18 — doc/code drift, needs a decision |
| BE-FX-15 | ~~**`GET /me/communities` hides communities a profile manages but does not own.** The endpoint returns `$profile->ownedCommunities()` only, so a member with `community_members.can_manage = true` cannot list the community they co-run. Every management action they are otherwise authorized for (`ChatService::canManageCommunity()` accepts them, as do the channel + ban endpoints) is unreachable in any client, because no client can discover the community id. The web panel's channel management is scoped to owned communities for exactly this reason. Fix: include managed communities in `CommunityController::index()` and emit a `my_can_manage` flag on `CommunityResource` (additive) so clients can tell the two apart.~~ | Fixed 2026-08-22 (tested) — `Community::scopeManageableBy()` + additive `my_can_manage` |
| BE-FX-12 | **`GET /chats` was a hard 500 in production — the chat inbox was dead** (app report, issue #146). BE-FX-8's preview relation was `latestMessage() = hasOne(...)->latestOfMany()`, and `latestOfMany()`/`ofMany()` **always** add `MAX(<primary key>)` to their sub-query (`CanBeOneOfMany::ofMany()` force-inserts the key, whatever column you pass). `chat_messages.id` is a `uuid` and Postgres has no `max(uuid)` → `SQLSTATE[42883] function max(uuid) does not exist`, thrown from `ChatService::visibleCollaborationThreads()` while eager-loading, for **any** viewer with ≥1 thread. `unread-count` survived because it never touches the relation. **CI could not catch it:** `phpunit.xml` runs the whole suite on SQLite, which evaluates `MAX(<uuid>)` happily. Fix: `latestMessage()` is now a plain `->latest('chat_messages.created_at')` (cheap on the lazy path — `HasOne` resolves with `first()`), the three `with('latestMessage')` eager loads are gone, and a new `ChatService::attachLatestMessages()` fills the relation in **two grouped queries** (`MAX(created_at) GROUP BY thread_id`, then fetch those rows) — no N+1, both served by the existing `(thread_id, created_at)` index. Guard test asserts the relation SQL never aggregates the uuid PK (fails on the old definition even on SQLite). Verified read-only against the **production** DB for the reporting business profile: `visibleThreads` 3 threads + resource serialises with real `last_message` previews (was throwing). Branch `fix/chat-latest-message-postgres-max-uuid`. | Fixed in code 2026-08-18 — **needs deploy**; 42/42 chat tests + guard green |
| BE-FX-22 | **`GET /profiles/{id}` hands an unsubscribed business the identity ROLES §2.5 says it cannot open** (detected 2026-08-19 during BE-NF-39's security review, which asked what a blurred viewer can still infer). ROLES §2.5 lists "open a community's full profile" among the things a free business **cannot** do. `PublicProfileResource` contains **zero** subscription checks (verified by grep) and returns `display_name`, `avatar_url`/`logo_url`, `handle`, `about`, `instagram`, `tiktok`, `website` and `city_name` to any authenticated caller. So the identity a business is meant to pay for is one request away for anyone holding a profile id. Together with BE-FX-21 (the Explore blur being client-side only) this means the two-action paywall's *visible* half — "subscribing reveals who you are talking to" — is not enforced anywhere on the server. BE-NF-39's `SuggestionResource` masks server-side and additionally withholds `counterpart.id` from a blurred viewer so it cannot be used as a lookup key; that is the pattern. Fix: apply viewer-dependent masking in `PublicProfileResource` for the fields §2.5 protects, with tests for both subscription states, and check with the app team which fields the mobile client assumes are always present. | Detected 2026-08-19 — not started |
| BE-FX-21 | **The Explore identity blur is client-side only, so the business paywall's most visible effect is trivially bypassable** (detected 2026-08-19 while implementing BE-NF-39's suggestion surface, which does the same masking server-side). ROLES §2.4 states that a free (non-subscribed) business sees Explore with the community name and logo **blurred** — that identity is precisely what subscribing reveals. But `DiscoveryOpportunityResource` emits `creator_profile.display_name` and `creator_profile.avatar_url` unconditionally, with no subscription check anywhere in the resource: verified by grep, the only occurrence of `display_name` is the unguarded assignment. So the blur exists only in the clients, and any unsubscribed business calling `GET /api/v1/discovery/opportunities` with its normal token receives every community's identity in plain JSON. Not a privacy breach (community profiles are discoverable by design) — it is a **monetisation gate that does not hold**, which is the same category as serving a paywalled field and hoping the client hides it. BE-NF-39's `SuggestionResource` masks `name` + `avatar_url` server-side for exactly this reason and can serve as the pattern. Fix: apply the same viewer-dependent masking in `DiscoveryOpportunityResource`, with tests for both subscription states, and confirm with the app team that the mobile client reads the masked shape rather than assuming the field is always present. | Detected 2026-08-19 — not started |
| BE-FX-11 | **Event host profile 404 (public-profile F1)** — the app opened an event's host-community public profile by pushing `events.community_id` (a `communities.id`) into `/profiles/{id}`, which binds `profiles.id` → 404 on profile/collaborations/gallery. `EventResource` now emits `host_profile_id` (the eager-loaded community owner's `profiles.id`, falling back to `events.profile_id` — always a valid `profiles.id`). Branch `fix/qa-batch-host-profile-chat-preview-avatars`; app half = kolabing-app PR #108. | Fixed in code 2026-08-02 — pending review + e2e |
| BE-FX-8 | **Chat inbox has no message preview** (app audit #8) — `GET /chats` / `ChatThread` exposed only `last_message_at`, never body text, so the app showed a "Tap to open" placeholder. Added `ChatThread::latestMessage()` (`latestOfMany`), eager-loaded in all 3 `ChatService` list loaders (no N+1), and emit `last_message: {content, created_at}` on `ChatThreadResource`. +1 feature test (`ChatActiveListTest`). App reads `last_message.content` (kolabing-app PR #108). | Fixed in code 2026-08-02 — pending review + e2e |
| BE-FX-9 | **Same stock avatar for multiple profiles** (app audit #9) — the QA read was "same waterfall stock photo everywhere". **Ground-truth:** `RealisticDataSeeder` already assigns DISTINCT per-name picsum photos (`.../seed/profile-<slug>/...`) — not the culprit. The real identical-image source was the **factories** (`Profile/Business/CommunityProfileFactory`), whose `fake()->imageUrl()` returns one shared `via.placeholder` image for every row → fixed to a distinct per-row picsum seed (or null → the app's initials placeholder). **Still open:** confirm prod isn't holding a shared stock URL (`SELECT avatar_url FROM profiles WHERE avatar_url LIKE 'https://picsum.photos/%'` + the two `profile_photo` columns) and the app-side avatar-source inconsistency Jace saw (stock in offer-detail vs initials in Explore card) needs a device re-check. | Factory source fixed in code 2026-08-02 — prod-data + app-consistency check still open |
| BE-FX-10 | ~~**Explore feed returns date-exhausted Kolabs** — the browse feed (`GET /kolabs` + `/opportunities` shim → `KolabService::browse()`) surfaced Kolabs whose application dates had all passed, so applicants hit an empty date picker ("No available dates for this kolab"). Added `Kolab::scopeWithSelectableDates()` on the discovery path, mirroring the apply-time guard; saved list unaffected.~~ | Fixed 2026-07-28 (tested) |

---

## Maintenance rules (mandatory — per CLAUDE.md)

- A **New Feature** you begin → move to **Incomplete Features**.
- An **Incomplete Feature** verified working end-to-end → remove it.
- A **bug** you detect → add to **Fixes** immediately; once the fix is **confirmed**
  (tested, not just written), strike it through with the date, then remove later.
- Update the **`Last updated:`** date whenever you edit this file.
- Keep **backend** work here; app work stays in `kolabing-app/BACKLOG.md`.
